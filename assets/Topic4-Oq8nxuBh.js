import{b as d,j as e}from"./vendor-react-core-Doz9nIC6.js";import{T as b}from"./TeacherSukantaHui-DDN87fI5.js";import{F as g}from"./FAQTemplate-BHhlgA96.js";import{P as y}from"./PlainTextPrint-C08xhKA4.js";import{E as v}from"./EditableCCodeBlock-BfqJ1usG.js";import"./vendor-icons-DEsnU7fn.js";import"./vendor-monaco-Bv7hoEkV.js";const w=[{id:1,question:"What is a 'Memory Leak' in C systems programming?",options:["Dynamically allocated heap memory that is no longer needed by the program but was never released with `free()`, making that RAM unusable by the system","A hardware short-circuit in RAM chips","A variable that loses its value after a loop","A stack variable exceeding 4 bytes"],answer:"Dynamically allocated heap memory that is no longer needed by the program but was never released with `free()`, making that RAM unusable by the system",explanation:"Heap memory remains allocated until explicitly returned via `free()` or the process exits. Leaking memory in long-running services (e.g. web servers) eventually causes Out-Of-Memory (OOM) crashes."},{id:2,question:"What is a 'Dangling Pointer' in C?",options:["A pointer variable that holds a memory address to a heap chunk or stack frame that has ALREADY been freed or deallocated","A pointer set to NULL","A pointer to a constant string","A pointer stored in CPU registers"],answer:"A pointer variable that holds a memory address to a heap chunk or stack frame that has ALREADY been freed or deallocated",explanation:"Calling `free(ptr)` deallocates the underlying buffer, but does not modify `ptr` itself. `ptr` still contains the old address (a dangling pointer), and reading/writing to it causes undefined behavior (Use-After-Free)."},{id:3,question:"What is a 'Double Free' bug and why is it considered a critical security vulnerability (CWE-415)?",options:["Calling `free()` twice on the exact same allocated pointer without reallocating, which corrupts the heap allocator's internal free-list bins and enables arbitrary code execution exploits","Allocating memory twice","Freeing two different pointers","Using two threads"],answer:"Calling `free()` twice on the exact same allocated pointer without reallocating, which corrupts the heap allocator's internal free-list bins and enables arbitrary code execution exploits",explanation:"Double-free corrupts memory metadata in glibc ptmalloc, allowing attackers to manipulate heap bin pointers and overwrite arbitrary function pointers."},{id:4,question:"What simple programming practice in C completely eliminates dangling pointer bugs?",options:["Immediately neutralizing the pointer with `ptr = NULL;` after calling `free(ptr)`","Setting the variable to 0","Calling malloc immediately","Using static variables"],answer:"Immediately neutralizing the pointer with `ptr = NULL;` after calling `free(ptr)`",explanation:"Setting `ptr = NULL` ensures that any future accidental dereference triggers an instant, clean, predictable crash (SIGSEGV at 0x0) instead of silent heap corruption, and `free(NULL)` is a safe no-op."},{id:5,question:"What is a 'Wild Pointer' in C?",options:["An uninitialized pointer variable that holds random garbage bits from the stack, pointing to an arbitrary memory location","A pointer to a function","A pointer to a double","A pointer in a loop"],answer:"An uninitialized pointer variable that holds random garbage bits from the stack, pointing to an arbitrary memory location",explanation:"Declaring `int* p;` without assigning `= NULL` or `= malloc(...)` leaves `p` containing arbitrary stack garbage bits."},{id:6,question:"What is an 'Orphaned Heap Block' memory leak?",options:["A heap memory block whose only referencing pointer is overwritten, lost, or falls out of scope before `free()` is called","A block allocated by the OS kernel","A block shared between two threads","A block marked as const"],answer:"A heap memory block whose only referencing pointer is overwritten, lost, or falls out of scope before `free()` is called",explanation:"For example: `int* p = malloc(100); p = malloc(200);` overwrites the address of the first 100-byte block, leaving it permanently orphaned with zero pointers referencing it."},{id:7,question:"What happens when returning a pointer to a local automatic stack variable from a function (`int* f() { int x = 10; return &x; }`)?",options:["Undefined Behavior: The stack frame is destroyed upon return, leaving the returned pointer dangling pointing to reclaimed stack space that will be overwritten by the next function call","The variable x is moved to the heap automatically","x persists forever in global memory","The compiler creates a copy on disk"],answer:"Undefined Behavior: The stack frame is destroyed upon return, leaving the returned pointer dangling pointing to reclaimed stack space that will be overwritten by the next function call",explanation:"Local variables exist only during the lifetime of their activation frame. Returning `&x` returns an address to dead stack memory."},{id:8,question:"What tool in the LLVM/GCC compiler toolchain is specifically designed to catch memory leaks, buffer overruns, and use-after-free bugs at runtime?",options:["AddressSanitizer (ASan) enabled with `-fsanitize=address -g`","GDB debugger only","Make utility","Git version control"],answer:"AddressSanitizer (ASan) enabled with `-fsanitize=address -g`",explanation:"AddressSanitizer instruments memory reads/writes with shadow memory checks, intercepting out-of-bounds accesses and memory leaks with exact file and line numbers."},{id:9,question:"What is Valgrind Memcheck and how does it diagnose C programs?",options:["A dynamic binary instrumentation framework that simulates CPU execution to track every byte of allocated memory and detect leaks, uninitialized reads, and invalid frees","A static code analyzer","A C compiler optimizer","A text editor"],answer:"A dynamic binary instrumentation framework that simulates CPU execution to track every byte of allocated memory and detect leaks, uninitialized reads, and invalid frees",explanation:"`valgrind --leak-check=full ./program` runs the binary in a virtual CPU environment, tracking every allocation and reporting exact leak traces."},{id:10,question:"What is the difference between Valgrind's 'definitely lost' vs 'still reachable' memory leak classifications?",options:["'Definitely lost' means no pointer references the block (unrecoverable leak); 'still reachable' means pointers still reference the block when the program exited without calling free()","'Still reachable' is worse than definitely lost","Both indicate a segmentation fault","Neither is a memory leak"],answer:"'Definitely lost' means no pointer references the block (unrecoverable leak); 'still reachable' means pointers still reference the block when the program exited without calling free()",explanation:"'Definitely lost' means total loss of pointer references during program runtime. 'Still reachable' typically occurs when global pointers are not explicitly freed before `exit()`."},{id:11,question:"What is a 'Buffer Overread' security vulnerability?",options:["Reading memory beyond the boundaries of an allocated array, potentially leaking secret keys, passwords, or memory layout addresses (e.g. Heartbleed OpenSSL bug)","Writing too much data to a disk file","Overclocking the CPU memory controller","Reading from a closed file"],answer:"Reading memory beyond the boundaries of an allocated array, potentially leaking secret keys, passwords, or memory layout addresses (e.g. Heartbleed OpenSSL bug)",explanation:"Out-of-bounds reads do not always crash; they can read sensitive data located in adjacent heap or stack structures (like the infamous Heartbleed flaw)."},{id:12,question:"Why should `free(NULL)` be safe to call in ISO C?",options:["The C standard explicitly mandates that passing NULL to `free()` must perform no action and safely return immediately","Because NULL memory is always 0 bytes","Because compilers delete free(NULL) statements","Because NULL is stored in ROM"],answer:"The C standard explicitly mandates that passing NULL to `free()` must perform no action and safely return immediately",explanation:"ISO C99/C11/C17 §7.22.3.4 explicitly states: 'If ptr is a null pointer, no action occurs.'"},{id:13,question:"What is Heap Fragmentation in long-running C applications?",options:["Allocating and freeing blocks of varying sizes over time creates small scattered gaps of free memory in the heap, causing future large allocations to fail even when total free RAM is sufficient","Disk sectors becoming corrupted","Stack frames overlapping","CPU registers splitting"],answer:"Allocating and freeing blocks of varying sizes over time creates small scattered gaps of free memory in the heap, causing future large allocations to fail even when total free RAM is sufficient",explanation:"External fragmentation occurs when total free memory is large but no single contiguous free block is big enough to satisfy a `malloc(large_size)` request."},{id:14,question:"How can dynamic array implementations minimize heap fragmentation?",options:["By using a memory pool (slab allocator) or standard geometric doubling with reserve capacity rather than thousands of tiny random allocations","By rebooting the server every hour","By avoiding all pointers","By using global variables only"],answer:"By using a memory pool (slab allocator) or standard geometric doubling with reserve capacity rather than thousands of tiny random allocations",explanation:"Memory pools pre-allocate large contiguous arenas and carve out uniform chunks, preventing heap scattering and fragmentation."},{id:15,question:"What is a 'Use-After-Free' (UAF) exploit?",options:["An attacker accesses or writes to a freed memory location after the allocator has reassigned that memory block to a different object, hijacking control flow","Using a trial version of software past the trial date","Reading from standard input twice","A compiler warning"],answer:"An attacker accesses or writes to a freed memory location after the allocator has reassigned that memory block to a different object, hijacking control flow",explanation:"UAF is one of the most severe cybersecurity vulnerabilities. If an attacker controls the newly allocated data at the old address, they can overwrite function pointers."},{id:16,question:"What is an Allocation Guard Macro commonly used in production C code?",options:["A macro or wrapper function like `SAFE_FREE(ptr)` that expands to `{ free(ptr); (ptr) = NULL; }`","A macro that disables malloc","A macro that reboots the computer","A macro that encrypts pointers"],answer:"A macro or wrapper function like `SAFE_FREE(ptr)` that expands to `{ free(ptr); (ptr) = NULL; }`",explanation:"Encapsulating deallocation ensures `free()` and neutralization to `NULL` always happen atomically."},{id:17,question:"What happens if a program allocates memory inside an infinite loop without calling `free()`?",options:["The process consumes all available physical RAM and swap space until the OS Out-Of-Memory (OOM) Killer forcibly terminates the process with SIGKILL","The program runs at infinite speed","The loop automatically pauses","The CPU enters sleep mode"],answer:"The process consumes all available physical RAM and swap space until the OS Out-Of-Memory (OOM) Killer forcibly terminates the process with SIGKILL",explanation:"Unchecked heap growth exhausts virtual memory, triggering OS kernel OOM killer intervention."},{id:18,question:"What is a Memory Canary (Guard Byte) pattern?",options:["Placing known sentinel byte values (e.g. `0xDEADBEEF`) immediately before and after an allocated buffer to detect buffer overruns when checked during teardown","A sound played by the motherboard speaker","A hardware firewall","A compression algorithm"],answer:"Placing known sentinel byte values (e.g. `0xDEADBEEF`) immediately before and after an allocated buffer to detect buffer overruns when checked during teardown",explanation:"Canaries are sentinel values placed at buffer boundaries. If memory is written out of bounds, the canary value is modified, immediately exposing the bug."},{id:19,question:"In C, what is the 'Resource Acquisition Is Initialization' (RAII) idiom equivalent since C does not have destructors?",options:["Strict `init_struct()` and `destroy_struct()` cleanup function pairs, or GNU C `__attribute__((cleanup(func)))` scope guards","Garbage collection in C runtime","Automatic stack unwinding","Static analysis tools only"],answer:"Strict `init_struct()` and `destroy_struct()` cleanup function pairs, or GNU C `__attribute__((cleanup(func)))` scope guards",explanation:"Disciplined C engineering enforces explicit constructor and destructor pairs for all composite heap data structures."},{id:20,question:"What is the consequence of passing an invalid pointer (e.g. stack address or offset inside a heap block `ptr + 5`) to `free()`?",options:["Undefined Behavior: The heap allocator attempts to parse metadata headers preceding `ptr + 5`, causing immediate allocator corruption or crash","It frees only 5 bytes","It safely returns -1","It does nothing"],answer:"Undefined Behavior: The heap allocator attempts to parse metadata headers preceding `ptr + 5`, causing immediate allocator corruption or crash",explanation:"`free()` expects the exact pointer returned by `malloc()`. Passing an interior pointer reads garbage metadata, corrupting heap chunks."},{id:21,question:"How does `valgrind --tool=helgrind` differ from `valgrind --tool=memcheck`?",options:["Memcheck detects memory leaks and out-of-bounds memory bugs, while Helgrind detects multi-threading race conditions and data synchronization deadlocks","Helgrind is for graphics memory only","Memcheck is for assembly code only","There is no difference"],answer:"Memcheck detects memory leaks and out-of-bounds memory bugs, while Helgrind detects multi-threading race conditions and data synchronization deadlocks",explanation:"Valgrind is a multi-tool suite: Memcheck inspects heap allocations, Helgrind monitors POSIX thread synchronization."},{id:22,question:"Why should `malloc(sizeof(struct Node))` be written as `malloc(sizeof(*ptr))` in idiomatic C?",options:["`sizeof(*ptr)` automatically adapts if the type of `ptr` is ever changed in the declaration, eliminating type-mismatch allocation bugs","Because `sizeof(*ptr)` is 2x faster","Because `struct Node` is invalid syntax in C99","To enable hardware multithreading"],answer:"`sizeof(*ptr)` automatically adapts if the type of `ptr` is ever changed in the declaration, eliminating type-mismatch allocation bugs",explanation:"`ptr = malloc(sizeof(*ptr))` is type-safe and refactor-proof. If `ptr` is changed from `int*` to `double*`, `sizeof(*ptr)` updates automatically."},{id:23,question:"What happens when `free()` is called on memory allocated with `alloca()`?",options:["Undefined Behavior and fatal crash: `alloca()` allocates memory on the physical Call Stack, which must NEVER be passed to the heap `free()` function","It safely releases stack memory","It frees the entire function","It returns 0"],answer:"Undefined Behavior and fatal crash: `alloca()` allocates memory on the physical Call Stack, which must NEVER be passed to the heap `free()` function",explanation:"`alloca()` allocates temporary memory on the stack frame which automatically vanishes when the function returns. Passing it to `free()` corrupts the heap."},{id:24,question:"What is UndefinedBehaviorSanitizer (UBSan) in modern GCC and Clang?",options:["A compiler runtime sanitizer (`-fsanitize=undefined`) that catches signed integer overflow, null pointer arithmetic, misaligned pointer reads, and divide by zero at runtime","A virus scanner","A code beautifier","A memory compression tool"],answer:"A compiler runtime sanitizer (`-fsanitize=undefined`) that catches signed integer overflow, null pointer arithmetic, misaligned pointer reads, and divide by zero at runtime",explanation:"UBSan detects non-portable or standard-violating operations that compilers might otherwise silently miscompile."},{id:25,question:"What is the golden rule of dynamic memory management in C?",options:["Every allocation (`malloc`, `calloc`, `realloc`) must have exactly ONE clearly defined owner responsible for exactly ONE matching `free()` call during its lifecycle","Never use pointers in C","Always allocate 10MB minimum","Free memory only when the computer shuts down"],answer:"Every allocation (`malloc`, `calloc`, `realloc`) must have exactly ONE clearly defined owner responsible for exactly ONE matching `free()` call during its lifecycle",explanation:"Clear ownership semantics prevent both memory leaks (missing free) and double-free vulnerabilities (multiple owners freeing the same block)."}],N=`================================================================================\r
CODER & ACCOTAX - PRINTABLE STUDY NOTE\r
Subject: Data Structures & Algorithms in C\r
Module: Array Data Structure, Dynamic Arrays & Matrix Algorithms\r
Topic 4: Memory Leak Prevention & Dangling Pointer Traps during Array Reallocation\r
Mentor: Sukanta Hui (Barrackpore, West Bengal)\r
================================================================================\r
\r
1. THE 4 CRITICAL HEAP MEMORY HAZARDS IN C\r
--------------------------------------------------------------------------------\r
Hazard                  Formal Definition & Physical Cause\r
--------------------------------------------------------------------------------\r
1. Memory Leak          Heap memory allocated via malloc()/calloc()/realloc() \r
                        whose pointer reference is lost or overwritten before \r
                        calling free(). That RAM remains locked and unavailable\r
                        to the operating system until the process terminates.\r
\r
2. Dangling Pointer     A pointer variable that continues to hold the memory \r
                        address of a heap block or stack frame that has ALREADY\r
                        been deallocated via free().\r
\r
3. Use-After-Free       Dereferencing (reading/writing through) a dangling pointer.\r
   (CWE-416)            Can lead to silent data corruption, security exploits,\r
                        or arbitrary code execution.\r
\r
4. Double-Free          Calling free(ptr) twice on the exact same allocated pointer.\r
   (CWE-415)            Corrupts the heap allocator's internal free-list bin \r
                        metadata and is a major security vulnerability.\r
\r
\r
2. THE REALLOC() OVERWRITE TRAP (THE MOST COMMON NOVICE BUG)\r
--------------------------------------------------------------------------------\r
🚫 FATAL PATTERN:\r
    int *arr = (int*)malloc(100 * sizeof(int));\r
    // ...\r
    arr = (int*)realloc(arr, 1000000000 * sizeof(int)); // FATAL TRAP!\r
\r
Why is this a fatal trap?\r
If realloc() fails (e.g. system is out of memory), it returns NULL.\r
Because \`arr\` is directly overwritten with NULL, the address of the ORIGINAL \r
100-integer buffer is permanently erased! The original 400 bytes can NEVER be \r
freed, causing an immediate, unrecoverable MEMORY LEAK.\r
\r
✅ THE SAFE 3-STEP REALLOCATION PATTERN:\r
    // Step 1: Allocate into a temporary pointer\r
    int *temp = (int*)realloc(arr, new_capacity * sizeof(int));\r
\r
    // Step 2: Validate temp before touching original pointer\r
    if (temp == NULL) {\r
        // Handle allocation failure gracefully\r
        fprintf(stderr, "Reallocation failed! Original buffer is safe.\\n");\r
        // arr still points to original valid buffer!\r
    } else {\r
        // Step 3: Success! Update original pointer to point to new buffer\r
        arr = temp;\r
        capacity = new_capacity;\r
    }\r
\r
\r
3. POINTER NEUTRALIZATION DISCIPLINE (DEFENSIVE PROGRAMMING)\r
--------------------------------------------------------------------------------\r
Calling \`free(ptr)\` only marks the underlying heap chunk as free in glibc \r
ptmalloc metadata. It DOES NOT modify the \`ptr\` variable in CPU registers/stack!\r
\`ptr\` still holds the old address (it becomes a Dangling Pointer).\r
\r
The Golden Rule:\r
    free(ptr);\r
    ptr = NULL; // Neutralize immediately!\r
\r
Why does this guarantee safety?\r
1. In C, \`free(NULL)\` is explicitly guaranteed by the C99/C11 standard to be \r
   a safe NO-OP (no action taken, no crash).\r
2. If you accidentally dereference a NULL pointer (\`*ptr = 10;\`), the CPU \r
   hardware Memory Management Unit (MMU) triggers an instant, deterministic \r
   SIGSEGV segmentation fault at address 0x0 instead of silently corrupting \r
   re-allocated heap memory!\r
\r
\r
4. INDUSTRIAL MEMORY DIAGNOSTICS & SANITIZERS\r
--------------------------------------------------------------------------------\r
Tool                    How to Use & Purpose\r
--------------------------------------------------------------------------------\r
1. AddressSanitizer     Compile with: \`gcc -fsanitize=address -g program.c\`\r
   (ASan)               Instruments binary at compile time. Catches Out-of-Bounds,\r
                        Use-After-Free, and Double-Free with 0% false positives.\r
\r
2. Valgrind Memcheck    Run with: \`valgrind --leak-check=full ./program\`\r
                        Simulates virtual CPU to track every single byte of heap \r
                        memory and reports exact line numbers of memory leaks.\r
\r
3. Static Analyzers     \`scan-build gcc -c program.c\` or Clang-Tidy.\r
\r
\r
5. DYNAMIC ARRAY LIFECYCLE INVARIANT CHECKLIST\r
--------------------------------------------------------------------------------\r
[ ] 1. Every malloc()/calloc() has exactly one corresponding free().\r
[ ] 2. realloc() is always captured in a temporary pointer first (\`temp\`).\r
[ ] 3. After calling free(p), immediately assign \`p = NULL;\`.\r
[ ] 4. Check for NULL return values after every dynamic allocation.\r
[ ] 5. Encapsulate dynamic arrays in a struct tracking \`{ data, size, capacity }\`.\r
\r
================================================================================\r
Coder & AccoTax Computer Education Center · Barrackpore, West Bengal, India\r
================================================================================\r
`,u=`/**\r
 * ============================================================================\r
 * SAFE DYNAMIC ARRAY REALLOCATION & MEMORY HAZARD PREVENTION IN C\r
 * Author: Sukanta Hui (Coder & AccoTax Barrackpore Lab)\r
 * Description: Demonstrates proper dynamic array resizing, safe realloc() \r
 *              idioms, memory leak prevention, and dangling pointer neutralization.\r
 * ============================================================================\r
 */\r
\r
#include <stdio.h>\r
#include <stdlib.h>\r
#include <stdbool.h>\r
\r
typedef struct {\r
    int *data;\r
    size_t size;\r
    size_t capacity;\r
} DynamicVector;\r
\r
/**\r
 * Initialize dynamic vector with initial capacity.\r
 */\r
DynamicVector* createVector(size_t initial_capacity) {\r
    if (initial_capacity == 0) initial_capacity = 4;\r
    \r
    DynamicVector *vec = (DynamicVector*)malloc(sizeof(DynamicVector));\r
    if (!vec) {\r
        fprintf(stderr, "[ERROR] Heap allocation failed for vector struct!\\n");\r
        return NULL;\r
    }\r
\r
    vec->data = (int*)malloc(initial_capacity * sizeof(int));\r
    if (!vec->data) {\r
        fprintf(stderr, "[ERROR] Heap allocation failed for vector data buffer!\\n");\r
        free(vec); // Clean up container to prevent memory leak\r
        return NULL;\r
    }\r
\r
    vec->size = 0;\r
    vec->capacity = initial_capacity;\r
    printf("[INIT] Vector created at %p with capacity = %zu (%zu bytes)\\n", \r
           (void*)vec->data, vec->capacity, vec->capacity * sizeof(int));\r
    return vec;\r
}\r
\r
/**\r
 * SAFE REALLOCATION IDIOM:\r
 * Never do: vec->data = realloc(vec->data, new_cap * sizeof(int));\r
 * If realloc fails and returns NULL, the original pointer is overwritten,\r
 * and the existing heap block becomes an unrecoverable MEMORY LEAK!\r
 */\r
bool resizeVector(DynamicVector *vec, size_t new_capacity) {\r
    if (!vec) return false;\r
\r
    printf("[REALLOC] Resizing vector from capacity %zu to %zu...\\n", vec->capacity, new_capacity);\r
    \r
    // Step 1: Allocate to temporary pointer\r
    int *temp = (int*)realloc(vec->data, new_capacity * sizeof(int));\r
    \r
    // Step 2: Validate temp before overwriting original pointer\r
    if (!temp) {\r
        fprintf(stderr, "[ERROR] realloc() failed! Original buffer at %p is SAFE and intact.\\n", (void*)vec->data);\r
        return false; // Original memory is NOT leaked!\r
    }\r
\r
    // Step 3: Successfully reallocated - update pointer and capacity\r
    vec->data = temp;\r
    vec->capacity = new_capacity;\r
    printf("[SUCCESS] Reallocated successfully. New buffer address: %p\\n", (void*)vec->data);\r
    return true;\r
}\r
\r
/**\r
 * Push an element into the vector, doubling capacity if needed (Geometric Growth).\r
 */\r
bool pushBack(DynamicVector *vec, int value) {\r
    if (!vec) return false;\r
\r
    if (vec->size >= vec->capacity) {\r
        size_t new_cap = vec->capacity * 2;\r
        if (!resizeVector(vec, new_cap)) {\r
            return false;\r
        }\r
    }\r
\r
    vec->data[vec->size++] = value;\r
    printf("[PUSH] Inserted %d at index [%zu]. Size = %zu / %zu\\n", value, vec->size - 1, vec->size, vec->capacity);\r
    return true;\r
}\r
\r
/**\r
 * SAFE DEALLOCATION & DANGLING POINTER NEUTRALIZATION:\r
 * Free internal buffer, free struct, and neutralize caller pointer to NULL.\r
 */\r
void freeVector(DynamicVector **vec_ptr) {\r
    if (!vec_ptr || !*vec_ptr) return;\r
\r
    DynamicVector *vec = *vec_ptr;\r
    \r
    if (vec->data) {\r
        printf("[FREE] Releasing data buffer at %p (%zu bytes)...\\n", (void*)vec->data, vec->capacity * sizeof(int));\r
        free(vec->data);\r
        vec->data = NULL; // Neutralize internal pointer\r
    }\r
\r
    printf("[FREE] Releasing vector struct at %p...\\n", (void*)vec);\r
    free(vec);\r
    \r
    // Neutralize external caller pointer to prevent Dangling Pointer / Use-After-Free\r
    *vec_ptr = NULL;\r
    printf("[CLEAN] Pointer neutralized to NULL. Dangling pointer hazard eliminated!\\n");\r
}\r
\r
int main() {\r
    printf("=================================================================\\n");\r
    printf("  CODER & ACCOTAX: SAFE ARRAY REALLOCATION & LEAK PREVENTION IN C\\n");\r
    printf("=================================================================\\n\\n");\r
\r
    DynamicVector *v = createVector(2);\r
    if (!v) return 1;\r
\r
    pushBack(v, 10);\r
    pushBack(v, 20);\r
    pushBack(v, 30); // Triggers geometric doubling 2 -> 4\r
    pushBack(v, 40);\r
    pushBack(v, 50); // Triggers geometric doubling 4 -> 8\r
\r
    printf("\\nVector Contents: [ ");\r
    for (size_t i = 0; i < v->size; i++) {\r
        printf("%d ", v->data[i]);\r
    }\r
    printf("]\\n\\n");\r
\r
    // Clean up memory safely\r
    freeVector(&v);\r
\r
    // Verify pointer is neutralized\r
    if (v == NULL) {\r
        printf("[VERIFIED] v is safely NULL. Accidental dereference will trigger instant predictable fault instead of silent memory corruption.\\n");\r
    }\r
\r
    return 0;\r
}\r
`;function R(){const h=d.useRef([]),[x,i]=d.useState("normal"),[r,c]=d.useState({address:12288,capacity:4,size:3,data:[10,20,30,0],isAllocated:!0,isLeaked:!1,isFreed:!1,isCorrupted:!1}),[n,p]=d.useState(12288),[f,o]=d.useState("Heap buffer allocated at 0x3000 (16 bytes). Pointer `arr` safely references 0x3000.");d.useEffect(()=>{const a=new IntersectionObserver(t=>{t.forEach(m=>{m.isIntersecting&&m.target.classList.add("is-visible")})},{threshold:.08});return h.current.forEach(t=>{t&&a.observe(t)}),()=>a.disconnect()},[]);const s=a=>{a&&!h.current.includes(a)&&h.current.push(a)},l=a=>{a==="reset"?(i("normal"),c({address:12288,capacity:4,size:3,data:[10,20,30,0],isAllocated:!0,isLeaked:!1,isFreed:!1,isCorrupted:!1}),p(12288),o("Buffer reset. Dynamic array initialized at 0x3000 with capacity = 4.")):a==="leak"?(i("leak"),p(0),c(t=>({...t,isLeaked:!0})),o("CRITICAL MEMORY LEAK! `arr` pointer was overwritten or went out of scope without calling free(). 16 bytes remain permanently locked in Heap memory until program exits!")):a==="free_without_null"?(i("dangling"),c(t=>({...t,isAllocated:!1,isFreed:!0,isCorrupted:!1})),o("DANGLING POINTER CREATED! `free(arr)` executed, releasing memory back to OS, but `arr` STILL holds address 0x3000! `arr` is now a Dangling Pointer.")):a==="use_after_free"?(i("dangling"),c(t=>({...t,data:[999,999,999,999],isCorrupted:!0})),o("USE-AFTER-FREE BUG (CWE-416)! Attempted write `*arr = 999` through dangling pointer! This corrupts arbitrary heap chunks or causes unpredictable security vulnerabilities!")):a==="free_with_null"?(i("neutralized"),c(t=>({...t,isAllocated:!1,isFreed:!0,isCorrupted:!1})),p(0),o("SAFE POINTER NEUTRALIZATION: `free(arr); arr = NULL;` executed. Pointer neutralized to 0x0. Any accidental dereference will trigger a clean hardware MMU fault instead of silent memory corruption!")):a==="unsafe_realloc_fail"?(i("unsafe_realloc"),p(0),c(t=>({...t,isLeaked:!0})),o("FATAL REALLOC TRAP: `arr = realloc(arr, 1000000000);` executed! System ran out of memory, realloc() returned NULL, and directly overwrote `arr`. The original 0x3000 buffer is now an UNRECOVERABLE MEMORY LEAK!")):a==="safe_realloc_fail"&&(i("safe_realloc"),o("SAFE REALLOCATION IDIOM: `int *temp = realloc(arr, 1000000000);` failed and returned NULL. But because we checked `if (temp != NULL)`, our original buffer at 0x3000 is 100% SAFE and intact!"))};return e.jsxs(e.Fragment,{children:[e.jsx("style",{children:`
        .reveal-section {
          opacity: 0.99;
          transform: translateY(0);
          transition: opacity 0.4s ease-out, transform 0.4s ease-out;
        }
        .reveal-section.is-visible {
          opacity: 1;
          transform: translateY(0);
        }
      `}),e.jsxs("div",{className:"min-h-screen bg-slate-950 text-slate-100 p-4 sm:p-6 md:p-10 font-sans selection:bg-cyan-500/30 selection:text-cyan-200",children:[e.jsxs("header",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10 text-center space-y-4",children:[e.jsxs("div",{className:"inline-flex items-center gap-2 px-3.5 py-1.5 rounded-full bg-cyan-950/70 border border-cyan-700/60 text-cyan-300 text-xs font-semibold uppercase tracking-wider shadow-lg",children:[e.jsx("span",{children:"⚡"}),e.jsx("span",{children:"DSA Segment 1 · Topic 4"})]}),e.jsx("h1",{className:"text-xl sm:text-2xl md:text-3xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-sky-300 to-indigo-300 tracking-tight leading-tight",children:"Memory Leak Prevention, Dangling Pointer Traps & Safe Array Reallocation in C"}),e.jsxs("p",{className:"text-sm sm:text-base md:text-lg text-slate-300 max-w-3xl mx-auto leading-relaxed",children:["Deep architectural analysis of dynamic heap memory lifecycle, glibc allocator mechanics, Use-After-Free vulnerabilities (CWE-416), Double-Free corruption (CWE-415), the fatal ",e.jsx("code",{children:"realloc()"})," overwrite trap, and defensive pointer neutralization."]}),e.jsxs("div",{className:"flex flex-wrap items-center justify-center gap-4 text-xs font-mono text-slate-400 pt-2",children:[e.jsx("span",{className:"px-2.5 py-1 rounded-md bg-slate-900 border border-slate-800 text-cyan-400",children:"Course Code: DSA-C-104"}),e.jsx("span",{className:"px-2.5 py-1 rounded-md bg-slate-900 border border-slate-800 text-sky-400",children:"Center: Coder & AccoTax (Barrackpore Lab)"}),e.jsx("span",{className:"px-2.5 py-1 rounded-md bg-slate-900 border border-slate-800 text-emerald-400",children:"Mentor: Sukanta Hui"})]})]}),e.jsx("section",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10 space-y-6",children:e.jsxs("div",{className:"bg-gradient-to-br from-slate-900 via-slate-900/95 to-indigo-950/40 border border-indigo-500/30 rounded-3xl p-6 md:p-8 shadow-2xl space-y-6",children:[e.jsxs("div",{className:"flex items-center gap-3 border-b border-slate-800 pb-4",children:[e.jsx("div",{className:"w-12 h-12 rounded-2xl bg-indigo-500/10 border border-indigo-500/30 flex items-center justify-center text-2xl",children:"🛡️"}),e.jsxs("div",{children:[e.jsx("h2",{className:"text-xl sm:text-2xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-indigo-300 via-sky-300 to-cyan-300",children:"What Are Dynamic Memory Hazards? (The 4 Fatal Traps in C)"}),e.jsx("p",{className:"text-xs text-slate-400 font-mono",children:"Formal Definitions, Hardware Root Causes & Industrial Prevention Strategies"})]})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-5",children:[e.jsxs("div",{className:"p-5 rounded-3xl bg-slate-950/90 border border-rose-500/30 space-y-3 shadow-lg",children:[e.jsxs("div",{className:"flex items-center justify-between",children:[e.jsxs("span",{className:"text-sm font-bold text-rose-400 flex items-center gap-2",children:[e.jsx("span",{children:"💧"})," 1. Memory Leak (Unreleased Heap RAM)"]}),e.jsx("span",{className:"text-[10px] font-mono px-2 py-0.5 rounded-full bg-rose-950 text-rose-300 border border-rose-800",children:"Resource Exhaustion"})]}),e.jsxs("p",{className:"text-xs text-slate-300 leading-relaxed font-sans",children:["Occurs when dynamically allocated heap memory (via ",e.jsx("code",{children:"malloc/calloc/realloc"}),") loses all pointer references before calling ",e.jsx("code",{children:"free()"}),". That RAM remains permanently occupied and unusable by the operating system until the process dies."]}),e.jsx("div",{className:"p-2.5 bg-slate-900 rounded-xl border border-slate-800 font-mono text-[11px] text-rose-300",children:e.jsx("code",{children:"int *p = malloc(100); p = malloc(200); // 100B leaked!"})})]}),e.jsxs("div",{className:"p-5 rounded-3xl bg-slate-950/90 border border-amber-500/30 space-y-3 shadow-lg",children:[e.jsxs("div",{className:"flex items-center justify-between",children:[e.jsxs("span",{className:"text-sm font-bold text-amber-400 flex items-center gap-2",children:[e.jsx("span",{children:"🪝"})," 2. Dangling Pointer & Use-After-Free (CWE-416)"]}),e.jsx("span",{className:"text-[10px] font-mono px-2 py-0.5 rounded-full bg-amber-950 text-amber-300 border border-amber-800",children:"Security Exploit"})]}),e.jsxs("p",{className:"text-xs text-slate-300 leading-relaxed font-sans",children:["Calling ",e.jsx("code",{children:"free(ptr)"})," frees the memory chunk, but ",e.jsx("b",{children:"does not change the pointer variable"}),"! ",e.jsx("code",{children:"ptr"})," still holds the old address. Dereferencing it (Use-After-Free) corrupts newly allocated memory or enables remote code execution exploits."]}),e.jsx("div",{className:"p-2.5 bg-slate-900 rounded-xl border border-slate-800 font-mono text-[11px] text-amber-300",children:e.jsx("code",{children:"free(p); *p = 50; // Use-After-Free Corruption!"})})]}),e.jsxs("div",{className:"p-5 rounded-3xl bg-slate-950/90 border border-purple-500/30 space-y-3 shadow-lg",children:[e.jsxs("div",{className:"flex items-center justify-between",children:[e.jsxs("span",{className:"text-sm font-bold text-purple-400 flex items-center gap-2",children:[e.jsx("span",{children:"💥"})," 3. Double-Free Vulnerability (CWE-415)"]}),e.jsx("span",{className:"text-[10px] font-mono px-2 py-0.5 rounded-full bg-purple-950 text-purple-300 border border-purple-800",children:"Heap Metadata Corruption"})]}),e.jsxs("p",{className:"text-xs text-slate-300 leading-relaxed font-sans",children:["Calling ",e.jsx("code",{children:"free(ptr)"})," twice on the exact same address corrupts the memory allocator's internal free-list bins (e.g. glibc ptmalloc tcache / fastbins), allowing malicious actors to manipulate chunk headers."]}),e.jsx("div",{className:"p-2.5 bg-slate-900 rounded-xl border border-slate-800 font-mono text-[11px] text-purple-300",children:e.jsx("code",{children:"free(p); free(p); // glibc: double free or corruption!"})})]}),e.jsxs("div",{className:"p-5 rounded-3xl bg-slate-950/90 border border-cyan-500/30 space-y-3 shadow-lg",children:[e.jsxs("div",{className:"flex items-center justify-between",children:[e.jsxs("span",{className:"text-sm font-bold text-cyan-400 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," 4. The ",e.jsx("code",{children:"realloc()"})," Overwrite Trap"]}),e.jsx("span",{className:"text-[10px] font-mono px-2 py-0.5 rounded-full bg-cyan-950 text-cyan-300 border border-cyan-800",children:"Novice Antipattern"})]}),e.jsxs("p",{className:"text-xs text-slate-300 leading-relaxed font-sans",children:["Writing ",e.jsx("code",{children:"arr = realloc(arr, new_size);"})," directly overwrites ",e.jsx("code",{children:"arr"})," with ",e.jsx("code",{children:"NULL"})," if allocation fails! The old buffer's address is erased forever, creating an instant unrecoverable memory leak."]}),e.jsx("div",{className:"p-2.5 bg-slate-900 rounded-xl border border-slate-800 font-mono text-[11px] text-cyan-300",children:e.jsx("code",{children:"int *temp = realloc(arr, sz); if (temp) arr = temp;"})})]})]})]})}),e.jsx("section",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10 space-y-6",children:e.jsxs("div",{className:"bg-gradient-to-br from-slate-900 via-slate-900/90 to-cyan-950/30 border border-cyan-500/30 rounded-3xl p-6 md:p-8 shadow-xl relative overflow-hidden",children:[e.jsxs("div",{className:"flex items-center gap-3 mb-6",children:[e.jsx("div",{className:"w-12 h-12 rounded-xl bg-cyan-500/10 border border-cyan-500/30 flex items-center justify-center text-2xl",children:"👨‍🏫"}),e.jsxs("div",{children:[e.jsx("h2",{className:"text-xl sm:text-2xl font-bold text-cyan-300",children:"Teacher's Desk: Physical Mental Model of Dynamic Memory"}),e.jsx("p",{className:"text-xs text-slate-400 font-mono",children:"Sukanta Hui & Barrackpore Lab Classroom Dialogue"})]})]}),e.jsxs("div",{className:"space-y-6 text-slate-300 leading-relaxed text-sm sm:text-base",children:[e.jsxs("div",{className:"bg-slate-950/70 border border-slate-800 rounded-2xl p-5 space-y-3",children:[e.jsxs("h3",{className:"text-cyan-400 font-bold flex items-center gap-2 text-base",children:[e.jsx("span",{children:"💡"})," The Hotel Keycard Metaphor"]}),e.jsxs("p",{children:["Think of the ",e.jsx("b",{children:"Heap"})," as a 5-star hotel with 10,000 rooms, and a ",e.jsx("b",{children:"Pointer variable"})," as a digital keycard with room number ",e.jsx("code",{children:"304"})," written on it:"]}),e.jsxs("ul",{className:"text-xs text-slate-300 space-y-2 list-disc list-inside",children:[e.jsxs("li",{children:[e.jsx("b",{className:"text-rose-400",children:"Memory Leak:"})," You walk out of the hotel, throw the keycard in the river, but never check out at the front desk. Room 304 stays reserved forever—nobody else can ever use it!"]}),e.jsxs("li",{children:[e.jsx("b",{className:"text-amber-400",children:"Dangling Pointer:"})," You check out at the front desk (",e.jsx("code",{children:"free(arr)"}),"), but keep a duplicate copy of the keycard in your pocket. The hotel assigns Room 304 to a new guest, but you still unlock the door and mess with their belongings (",e.jsx("b",{children:"Use-After-Free Data Corruption!"}),")."]}),e.jsxs("li",{children:[e.jsx("b",{className:"text-emerald-400",children:"Pointer Neutralization:"})," Immediately upon checking out, you erase your keycard to blank (",e.jsx("code",{children:"arr = NULL;"}),"). Now if you accidentally try to swipe it, nothing happens!"]})]})]}),e.jsxs("div",{className:"bg-slate-950/70 border border-slate-800 rounded-2xl p-5 space-y-3",children:[e.jsxs("h3",{className:"text-sky-400 font-bold flex items-center gap-2 text-base",children:[e.jsx("span",{children:"💬"})," Barrackpore Lab Classroom Discussion"]}),e.jsxs("div",{className:"space-y-3 text-xs sm:text-sm font-sans border-l-2 border-cyan-500/40 pl-4 py-1",children:[e.jsxs("p",{children:[e.jsx("strong",{className:"text-emerald-400",children:"Swadeep:"})," ",e.jsx("em",{children:`"Sir, why does C allow dangling pointers to exist at all? Why doesn't free() automatically set our pointer to NULL?"`})]}),e.jsxs("p",{children:[e.jsx("strong",{className:"text-cyan-300",children:"Sukanta Sir:"})," ",e.jsxs("em",{children:['"Because in C, functions receive arguments ',e.jsx("b",{children:"by value"}),"! ",e.jsx("code",{children:"free(void *ptr)"})," receives a copy of the memory address. It can notify the OS heap manager to release the chunk, but it has no access to the caller's stack frame variable to change it to NULL. That is why YOU, the systems programmer, must write ",e.jsx("code",{children:"ptr = NULL;"})," right after ",e.jsx("code",{children:"free(ptr);"}),'!"']})]}),e.jsxs("p",{children:[e.jsx("strong",{className:"text-emerald-400",children:"Tuhina:"})," ",e.jsx("em",{children:'"And what about realloc()? Why is `arr = realloc(arr, new_size);` considered a deadly trap?"'})]}),e.jsxs("p",{children:[e.jsx("strong",{className:"text-cyan-300",children:"Sukanta Sir:"})," ",e.jsx("em",{children:'"Because if the OS is out of memory, realloc() returns NULL. If you assign that directly to `arr`, you erase the ONLY pointer to your existing buffer! Always use a temporary pointer `temp = realloc(...)` first!"'})]})]})]})]})]})}),e.jsx("section",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10 space-y-6",children:e.jsxs("div",{className:"bg-slate-900/90 border border-cyan-500/30 rounded-3xl p-6 md:p-8 shadow-2xl space-y-6",children:[e.jsxs("div",{className:"flex flex-wrap items-center justify-between gap-4 border-b border-slate-800 pb-5",children:[e.jsxs("div",{children:[e.jsxs("h2",{className:"text-xl sm:text-2xl font-bold text-cyan-300 flex items-center gap-2",children:[e.jsx("span",{children:"🔬"})," Interactive Memory Hazard & Reallocation Studio"]}),e.jsx("p",{className:"text-xs text-slate-400 font-mono mt-1",children:"Live Stack Pointer vs. Heap Buffer State Machine · Simulate Leaks, Dangling Pointers & Safe Realloc"})]}),e.jsxs("div",{className:"flex items-center gap-2",children:[e.jsxs("span",{className:"text-xs font-mono px-3 py-1.5 rounded-xl bg-slate-950 border border-slate-800 text-cyan-300",children:["Pointer State: ",e.jsx("strong",{className:n===0?"text-slate-400":"text-emerald-300",children:n===0?"NULL (0x0)":`0x${n.toString(16).toUpperCase()}`})]}),e.jsxs("span",{className:`text-xs font-mono px-3 py-1.5 rounded-xl border ${r.isLeaked?"bg-rose-950 border-rose-700 text-rose-300 animate-pulse font-bold":r.isFreed?"bg-slate-950 border-slate-800 text-slate-400":"bg-emerald-950 border-emerald-700 text-emerald-300 font-bold"}`,children:["Heap Chunk: ",r.isLeaked?"LEAKED 💀":r.isFreed?"FREED (0x0)":"ALLOCATED (ACTIVE)"]})]})]}),e.jsxs("div",{className:"p-4 rounded-2xl bg-slate-950/80 border border-slate-800 space-y-3",children:[e.jsx("span",{className:"text-xs font-semibold text-slate-400 uppercase tracking-wider block",children:"Trigger Real-World Scenarios:"}),e.jsxs("div",{className:"flex flex-wrap gap-2.5",children:[e.jsx("button",{onClick:()=>l("reset"),className:"px-3.5 py-1.5 rounded-xl bg-slate-900 hover:bg-slate-800 text-cyan-300 border border-slate-800 text-xs font-semibold transition cursor-pointer",children:"🔄 1. Fresh Allocation (`malloc`)"}),e.jsx("button",{onClick:()=>l("leak"),className:"px-3.5 py-1.5 rounded-xl bg-rose-950/60 hover:bg-rose-900/80 text-rose-300 border border-rose-800/60 text-xs font-semibold transition cursor-pointer",children:"💧 2. Simulate Memory Leak (`arr = NULL`)"}),e.jsx("button",{onClick:()=>l("free_without_null"),className:"px-3.5 py-1.5 rounded-xl bg-amber-950/60 hover:bg-amber-900/80 text-amber-300 border border-amber-800/60 text-xs font-semibold transition cursor-pointer",children:"🪝 3. `free(arr)` without `arr = NULL` (Dangling Pointer)"}),e.jsx("button",{onClick:()=>l("use_after_free"),disabled:!r.isFreed,className:"px-3.5 py-1.5 rounded-xl bg-rose-900/60 hover:bg-rose-800 disabled:opacity-40 disabled:cursor-not-allowed text-rose-200 border border-rose-700 text-xs font-semibold transition cursor-pointer",children:"💥 4. Write `*arr = 999` (Use-After-Free)"}),e.jsx("button",{onClick:()=>l("free_with_null"),className:"px-3.5 py-1.5 rounded-xl bg-emerald-950/60 hover:bg-emerald-900/80 text-emerald-300 border border-emerald-800/60 text-xs font-semibold transition cursor-pointer",children:"🛡️ 5. Safe Free (`free(arr); arr = NULL;`)"}),e.jsx("button",{onClick:()=>l("unsafe_realloc_fail"),className:"px-3.5 py-1.5 rounded-xl bg-rose-950/60 hover:bg-rose-900/80 text-rose-300 border border-rose-800/60 text-xs font-semibold transition cursor-pointer",children:"🚫 6. Unsafe `arr = realloc(...)` Fail"}),e.jsx("button",{onClick:()=>l("safe_realloc_fail"),className:"px-3.5 py-1.5 rounded-xl bg-sky-950/60 hover:bg-sky-900/80 text-sky-300 border border-sky-800/60 text-xs font-semibold transition cursor-pointer",children:"✅ 7. Safe `temp = realloc(...)` Idiom"})]})]}),e.jsxs("div",{className:`p-4 rounded-2xl border font-mono text-xs leading-relaxed transition-all shadow-inner ${r.isLeaked||r.isCorrupted?"bg-rose-950/80 border-rose-500/60 text-rose-200":x==="dangling"?"bg-amber-950/80 border-amber-500/60 text-amber-200":"bg-slate-950 border-cyan-500/40 text-cyan-200"}`,children:[e.jsx("div",{className:"flex items-center gap-2 font-bold mb-1",children:e.jsx("span",{children:r.isLeaked||r.isCorrupted?"🚨 HAZARD DETECTED:":"ℹ️ SYSTEM STATUS:"})}),e.jsx("p",{children:f})]}),e.jsxs("div",{className:"grid grid-cols-1 lg:grid-cols-12 gap-6 items-start",children:[e.jsxs("div",{className:"lg:col-span-4 bg-slate-950/90 border border-slate-800 rounded-3xl p-5 space-y-4 shadow-lg",children:[e.jsxs("div",{className:"flex items-center justify-between border-b border-slate-800 pb-3",children:[e.jsxs("div",{children:[e.jsxs("h3",{className:"text-sm font-bold text-sky-300 flex items-center gap-2",children:[e.jsx("span",{children:"📦"}),e.jsx("span",{children:"Stack Frame (Local Scope)"})]}),e.jsxs("span",{className:"text-[11px] text-slate-400",children:["Pointer Variable ",e.jsx("code",{children:"int *arr"})]})]}),e.jsx("span",{className:"text-xs font-mono px-2 py-0.5 rounded bg-slate-900 border border-slate-800 text-slate-400",children:"8 Bytes (64-bit)"})]}),e.jsxs("div",{className:"p-4 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-3 font-mono text-center",children:[e.jsxs("div",{className:"text-xs text-slate-400",children:["Variable: ",e.jsx("code",{children:"int *arr"})]}),e.jsx("div",{className:`p-4 rounded-2xl border transition-all text-base font-bold ${n===0?"bg-slate-950 border-slate-800 text-slate-500":n===r.address&&r.isFreed?"bg-amber-950/80 border-amber-400 text-amber-300 ring-2 ring-amber-400 animate-pulse shadow-lg shadow-amber-950":"bg-cyan-950/60 border-cyan-400 text-cyan-200 ring-2 ring-cyan-400 shadow-lg shadow-cyan-950"}`,children:n===0?"NULL (0x00000000)":`0x${n.toString(16).toUpperCase()}`}),e.jsx("div",{className:"text-[11px] text-slate-400 font-sans",children:n===0?"Pointer is neutralized to NULL. MMU safe.":n===r.address&&r.isFreed?"⚠️ DANGLING POINTER! Holds address of freed heap memory.":"Valid pointer referencing active Heap allocation."})]})]}),e.jsxs("div",{className:"lg:col-span-8 bg-slate-950/90 border border-slate-800 rounded-3xl p-5 space-y-4 shadow-lg",children:[e.jsxs("div",{className:"flex items-center justify-between border-b border-slate-800 pb-3",children:[e.jsxs("div",{children:[e.jsxs("h3",{className:"text-sm font-bold text-emerald-300 flex items-center gap-2",children:[e.jsx("span",{children:"🧱"}),e.jsx("span",{children:"Heap Memory Space (Dynamic Chunk)"})]}),e.jsxs("span",{className:"text-[11px] text-slate-400",children:["Address: 0x",r.address.toString(16).toUpperCase()," · Capacity: ",r.capacity," ints (16B)"]})]}),e.jsx("span",{className:`text-xs font-mono px-2.5 py-1 rounded-lg border ${r.isLeaked?"bg-rose-950 text-rose-300 border-rose-700":r.isFreed?"bg-slate-900 text-slate-500 border-slate-800":"bg-emerald-950 text-emerald-300 border-emerald-700"}`,children:r.isLeaked?"Orphaned / Unreachable":r.isFreed?"Deallocated by OS":"Active Allocation"})]}),e.jsx("div",{className:"grid grid-cols-2 sm:grid-cols-4 gap-3 font-mono",children:r.data.map((a,t)=>{const m=r.address+t*4;return e.jsxs("div",{className:`p-3 rounded-2xl border transition-all text-center flex flex-col justify-between ${r.isLeaked?"bg-rose-950/50 border-rose-600 text-rose-300 border-dashed":r.isCorrupted?"bg-rose-900/70 border-rose-400 text-white ring-2 ring-rose-400 animate-pulse":r.isFreed?"bg-slate-950/60 border-slate-850 text-slate-700":"bg-slate-900/90 border-cyan-500/50 text-cyan-200 shadow-md shadow-cyan-950/40"}`,children:[e.jsxs("div",{className:"text-[10px] text-slate-500 font-bold",children:["0x",m.toString(16).toUpperCase()]}),e.jsx("div",{className:"my-2",children:e.jsx("div",{className:`text-lg font-bold ${r.isFreed&&!r.isCorrupted?"text-slate-700 line-through":"text-emerald-300"}`,children:a})}),e.jsxs("div",{className:"text-[9px] text-slate-500 font-sans",children:["arr[",t,"] (",t*4,"B)"]})]},t)})}),e.jsxs("div",{className:"p-3 rounded-2xl bg-slate-900/80 border border-slate-800 text-xs text-slate-400 space-y-1",children:[e.jsxs("div",{className:"font-bold text-slate-300 flex items-center gap-1.5",children:[e.jsx("span",{children:"💡"})," Heap Allocator Rule:"]}),e.jsxs("p",{children:["Heap buffers do not vanish when functions return. They require explicit destruction via ",e.jsx("code",{children:"free()"}),". If the pointer is lost before calling ",e.jsx("code",{children:"free()"}),", those bytes remain allocated in RAM until process death."]})]})]})]})]})}),e.jsxs("section",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10 space-y-6",children:[e.jsxs("h2",{className:"text-2xl font-bold text-cyan-300 flex items-center gap-2",children:[e.jsx("span",{children:"📚"})," The 5 Golden Invariants of Heap Memory in C"]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"bg-slate-900/80 border border-slate-800 rounded-3xl p-6 space-y-3 shadow-lg",children:[e.jsxs("h3",{className:"text-lg font-bold text-cyan-400 flex items-center gap-2",children:[e.jsx("span",{children:"1️⃣"})," The 3-Step ",e.jsx("code",{children:"realloc()"})," Temporary Pointer Idiom"]}),e.jsxs("p",{className:"text-sm text-slate-300 leading-relaxed",children:["Always capture the return value of ",e.jsx("code",{children:"realloc()"})," into a temporary pointer before assigning it to your master array pointer:"]}),e.jsx("div",{className:"p-3 bg-slate-950 rounded-2xl border border-slate-800 font-mono text-xs text-emerald-400",children:e.jsxs("code",{children:["int *temp = (int*)realloc(arr, new_cap * sizeof(int));",e.jsx("br",{}),"if (!temp) { /* handle error; arr is SAFE */ }",e.jsx("br",{}),"else { arr = temp; capacity = new_cap; }"]})})]}),e.jsxs("div",{className:"bg-slate-900/80 border border-slate-800 rounded-3xl p-6 space-y-3 shadow-lg",children:[e.jsxs("h3",{className:"text-lg font-bold text-sky-400 flex items-center gap-2",children:[e.jsx("span",{children:"2️⃣"})," Immediate Pointer Neutralization"]}),e.jsxs("p",{className:"text-sm text-slate-300 leading-relaxed",children:["Immediately assign ",e.jsx("code",{children:"ptr = NULL;"})," after calling ",e.jsx("code",{children:"free(ptr)"}),". In C:"]}),e.jsxs("ul",{className:"text-xs text-slate-400 space-y-1.5 list-disc list-inside font-mono",children:[e.jsxs("li",{children:[e.jsx("code",{children:"free(NULL)"})," is explicitly guaranteed to be a safe NO-OP."]}),e.jsx("li",{children:"Dereferencing NULL triggers a clean, deterministic MMU crash (SIGSEGV at 0x0) instead of silent heap corruption."})]})]}),e.jsxs("div",{className:"bg-slate-900/80 border border-slate-800 rounded-3xl p-6 space-y-3 shadow-lg",children:[e.jsxs("h3",{className:"text-lg font-bold text-emerald-400 flex items-center gap-2",children:[e.jsx("span",{children:"3️⃣"})," Industrial Sanitizers (ASan & Valgrind)"]}),e.jsx("p",{className:"text-sm text-slate-300 leading-relaxed",children:"Modern compilers integrate hardware-assisted sanitizers:"}),e.jsxs("div",{className:"space-y-2 text-xs font-mono",children:[e.jsxs("div",{className:"p-2.5 bg-slate-950 rounded-xl border border-slate-800",children:[e.jsx("span",{className:"text-emerald-300 font-bold block",children:"GCC / Clang AddressSanitizer:"}),e.jsx("code",{children:"gcc -fsanitize=address -g main.c -o app"})]}),e.jsxs("div",{className:"p-2.5 bg-slate-950 rounded-xl border border-slate-800",children:[e.jsx("span",{className:"text-sky-300 font-bold block",children:"Valgrind Leak Profiling:"}),e.jsx("code",{children:"valgrind --leak-check=full ./app"})]})]})]}),e.jsxs("div",{className:"bg-slate-900/80 border border-slate-800 rounded-3xl p-6 space-y-3 shadow-lg",children:[e.jsxs("h3",{className:"text-lg font-bold text-indigo-400 flex items-center gap-2",children:[e.jsx("span",{children:"4️⃣"})," Struct Encapsulation & Clear Ownership"]}),e.jsx("p",{className:"text-sm text-slate-300 leading-relaxed",children:"Never pass loose raw pointers across modules without their capacity metadata. Encapsulate dynamic arrays in a dedicated struct with designated constructor and destructor functions:"}),e.jsx("div",{className:"p-3 bg-slate-950 rounded-2xl border border-slate-800 font-mono text-xs text-indigo-300",children:e.jsxs("code",{children:["typedef struct { int *data; size_t size; size_t capacity; } Vector;",e.jsx("br",{}),"Vector* createVector(size_t cap);",e.jsx("br",{}),"void freeVector(Vector **v);"]})})]})]})]}),e.jsxs("section",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10 space-y-4",children:[e.jsxs("h2",{className:"text-2xl font-bold text-cyan-400 flex items-center gap-2",children:[e.jsx("span",{children:"🛠️"})," Runnable Production C Implementation: Safe Dynamic Array Reallocation"]}),e.jsx(v,{code:u,initialCode:u,title:"safe_dynamic_array_realloc.c"})]}),e.jsx("section",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10",children:e.jsx(g,{questions:w})}),e.jsx("section",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10",children:e.jsx(y,{content:N,title:"DSA Topic Note: Memory Leak Prevention and Dangling Pointer Traps in C"})}),e.jsx("section",{ref:s,className:"reveal-section max-w-7xl mx-auto mb-10",children:e.jsx(b,{})})]})]})}export{R as default};
