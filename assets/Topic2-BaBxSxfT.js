import{b as c,j as e,bg as r}from"./vendor-react-core-Doz9nIC6.js";import{T as m}from"./TeacherSukantaHui-DDN87fI5.js";import{F as d}from"./FAQTemplate-BHhlgA96.js";import{P as p}from"./PlainTextPrint-C08xhKA4.js";import"./vendor-icons-DEsnU7fn.js";const h=[{question:"What is the primary difference between Asynchronous Replication and Semi-Synchronous Replication in MySQL?",shortAnswer:"In **Asynchronous Replication**, the Source commits locally and immediately returns success to the client without waiting for replicas; in **Semi-Synchronous Replication**, the Source pauses the client's `COMMIT` until at least one replica acknowledges receiving the transaction into its local Relay Log.",explanation:"Asynchronous replication risks data loss (RPO > 0) if the Source crashes before binlogs are transmitted; Semi-Synchronous replication guarantees RPO = 0.",hint:"Async does not wait for replica acknowledgment; Semi-sync waits for >= 1 replica to receive the transaction.",level:"basic",codeExample:`# Asynchronous: Commit → Return → Stream in background
# Semi-Synchronous: Write Binlog → Stream → Wait for Replica ACK → Commit → Return`},{question:"What are the two wait-point modes for Semi-Synchronous Replication in MySQL, and which is the default in MySQL 8.0?",shortAnswer:"1. **`AFTER_SYNC`** (Lossless Semi-Sync - **Default in MySQL 8.0**), and 2. **`AFTER_COMMIT`** (Legacy Semi-Sync).",explanation:"`AFTER_SYNC` waits for replica acknowledgment *before* committing the transaction to the InnoDB engine, eliminating phantom reads.",hint:"AFTER_SYNC (Lossless default) and AFTER_COMMIT (legacy).",level:"basic",codeExample:"SHOW VARIABLES LIKE 'rpl_semi_sync_master_wait_point'; -- Value: AFTER_SYNC"},{question:"What is the critical architectural flaw of legacy `AFTER_COMMIT` semi-synchronous replication?",shortAnswer:"In `AFTER_COMMIT`, the transaction is committed in the Source's storage engine *before* waiting for the replica ACK; if the Source crashes while waiting, other concurrent sessions have already seen data that was never replicated, causing **Phantom Reads** and data loss upon failover.",explanation:"`AFTER_SYNC` solves this by withholding local InnoDB commit until the replica confirms receipt.",hint:"Committing locally before ACK allows concurrent clients to read un-replicated data that vanishes on failover.",level:"intermediate",codeExample:`-- AFTER_COMMIT Flaw:
-- Thread 1: Inserts & Commits locally → Thread 2 reads new balance → Source crashes → Standby promoted WITHOUT transaction!`},{question:"How does `AFTER_SYNC` (Lossless Semi-Sync) guarantee zero data loss (RPO = 0)?",shortAnswer:"Because the Source flushes the transaction to its binary log, sends it to the replica, and pauses until the replica writes it to its Relay Log *before* the transaction is committed in InnoDB or returned to the client.",explanation:"If the Source crashes during the wait, the transaction is already safe in the replica's relay log for failover.",hint:"Transaction is guaranteed to reside in the replica's relay log before the client receives success.",level:"intermediate",codeExample:`# Transaction Flow:
# 1. InnoDB Prepare → 2. Binlog Flush → 3. Send to Replica → 4. Replica Writes Relay Log & ACKs → 5. InnoDB Commit → 6. Client OK`},{question:"In Mamata & Susmita's Barrackpore retail store, POS billing server crashed during a lightning surge. How did Lossless Semi-Sync prevent losing ₹45,000 in customer invoice data?",shortAnswer:"Because `rpl_semi_sync_master_wait_point = 'AFTER_SYNC'` was enabled, the last batch of 12 invoice transactions had been acknowledged and saved into the standby replica's relay log before the crash, ensuring zero lost sales across ₹1.2 Crores in inventory.",explanation:"Promoting the standby replica restored all ₹45,000 in invoices with exact transactional consistency.",hint:"Invoices were safely buffered in the standby relay log before the crash occurred.",level:"moderate",codeExample:`# Barrackpore Semi-Sync Verification:
SHOW STATUS LIKE 'Rpl_semi_sync_master_yes_tx';`},{question:"In Abhronila & Debangshu's Kolkata fintech bank, how did configuring `rpl_semi_sync_master_wait_for_num_slaves = 2` protect ₹500 Crores in daily banking ledgers against dual-node failures?",shortAnswer:"It required transactions to be acknowledged by **two independent standby replicas** before committing on the primary, ensuring data survived even if the primary and one replica simultaneously suffered power loss.",explanation:"Guarantees multi-replica redundancy for tier-1 financial regulatory compliance.",hint:"Requires 2 replicas to acknowledge receipt before commit completes on Source.",level:"expert",codeExample:"SET GLOBAL rpl_semi_sync_master_wait_for_num_slaves = 2;"},{question:"What happens if a semi-synchronous replica fails or the network connection drops?",shortAnswer:"The Source pauses write commits for up to `rpl_semi_sync_master_timeout` milliseconds (e.g. 10,000ms); if no ACK is received, the Source automatically **falls back to Asynchronous mode** to prevent freezing write transactions permanently.",explanation:"Ensures database availability is preserved if all replicas go offline.",hint:"Waits up to timeout, then automatically degrades to Asynchronous replication.",level:"intermediate",codeExample:"SET GLOBAL rpl_semi_sync_master_timeout = 10000; -- 10 Seconds Timeout"},{question:"What happens when a disconnected semi-synchronous replica reconnects and catches up with the Source?",shortAnswer:"The Source automatically **re-enables Semi-Synchronous replication** mode without requiring any manual DBA commands or server restarts.",explanation:"MySQL dynamically toggles semi-sync status based on live replica acknowledgments.",hint:"Source automatically restores semi-synchronous mode once the replica reconnects.",level:"basic",codeExample:"-- Automatic recovery: Rpl_semi_sync_master_status transitions back to ON."},{question:"What plugin library file must be loaded on the Source server to enable Semi-Synchronous replication in MySQL 8.0?",shortAnswer:"`semisync_master.so` (or `semisync_source.so` / `semisync_master.dll` on Windows).",explanation:"Installed via `INSTALL PLUGIN rpl_semi_sync_master SONAME 'semisync_master.so';`.",hint:"semisync_master.so (or semisync_source.so).",level:"basic",codeExample:"INSTALL PLUGIN rpl_semi_sync_master SONAME 'semisync_master.so';"},{question:"What plugin library file must be loaded on the Replica server to enable Semi-Synchronous replication?",shortAnswer:"`semisync_slave.so` (or `semisync_replica.so` / `semisync_slave.dll` on Windows).",explanation:"Installed via `INSTALL PLUGIN rpl_semi_sync_slave SONAME 'semisync_slave.so';`.",hint:"semisync_slave.so (or semisync_replica.so).",level:"basic",codeExample:"INSTALL PLUGIN rpl_semi_sync_slave SONAME 'semisync_slave.so';"},{question:"What does `Rpl_semi_sync_master_status` indicate in `SHOW GLOBAL STATUS` on the Source?",shortAnswer:"A binary flag (`ON` / `OFF` or `1` / `0`) indicating whether the Source is currently operating in semi-synchronous mode or has degraded to asynchronous mode due to timeout.",explanation:"The primary health metric for monitoring semi-sync replication status.",hint:"Indicates whether semi-sync replication is currently active (ON) or degraded to async (OFF).",level:"basic",codeExample:"SHOW GLOBAL STATUS LIKE 'Rpl_semi_sync_master_status'; -- Value: ON"},{question:"What is `Rpl_semi_sync_master_yes_tx` vs `Rpl_semi_sync_master_no_tx` in `SHOW GLOBAL STATUS`?",shortAnswer:"`yes_tx` counts the number of write transactions successfully acknowledged by a semi-sync replica; `no_tx` counts transactions committed in asynchronous fallback mode after timeout.",explanation:"A rising `no_tx` counter indicates network issues or offline replicas.",hint:"yes_tx is acknowledged transactions; no_tx is unacknowledged async fallback transactions.",level:"intermediate",codeExample:`SELECT VARIABLE_NAME, VARIABLE_VALUE 
FROM performance_schema.global_status 
WHERE VARIABLE_NAME IN ('Rpl_semi_sync_master_yes_tx', 'Rpl_semi_sync_master_no_tx');`},{question:"What is `Rpl_semi_sync_master_clients` in `SHOW GLOBAL STATUS`?",shortAnswer:"The total number of currently connected replicas that have semi-synchronous replication enabled and are sending acknowledgments to the Source.",explanation:"Verifies how many active semi-sync replicas are currently paired with the Source.",hint:"Count of connected replicas sending semi-sync acknowledgments.",level:"basic",codeExample:"SHOW GLOBAL STATUS LIKE 'Rpl_semi_sync_master_clients'; -- Value: 1 or 2"},{question:"What client latency overhead is introduced by Semi-Synchronous replication?",shortAnswer:"The network Round-Trip Time (RTT) between the Source and the acknowledging replica (typically **0.5ms to 1.5ms in a LAN / same datacenter**; 20ms to 50ms across WAN/regions).",explanation:"Semi-sync should ideally be configured between servers located in the same low-latency datacenter or cloud Availability Zone.",hint:"Adds network Round-Trip Time (RTT) per write transaction commit (~1ms in LAN).",level:"intermediate",codeExample:"-- LAN latency impact: ~1ms per commit. Read queries experience 0ms latency."},{question:"Does Semi-Synchronous replication add any latency to read-only `SELECT` queries?",shortAnswer:"No, read-only `SELECT` queries do not write to the binary log and commit without waiting for replica acknowledgments, experiencing **0ms latency overhead**.",explanation:"Only write transactions (`INSERT`, `UPDATE`, `DELETE`, `DDL`) require replica acknowledgment.",hint:"No latency added to SELECT queries; only write transactions wait.",level:"basic",codeExample:"-- SELECT queries execute with 0ms replication overhead."},{question:"What is the recommended value for `rpl_semi_sync_master_timeout` in an enterprise production cluster?",shortAnswer:"Between **5,000ms (5s) and 10,000ms (10s)**.",explanation:"Setting it too low (e.g. 100ms) causes frequent false fallback to asynchronous mode during minor network jitter; setting it too high (e.g. 60s) freezes client writes for too long during an outage.",hint:"5,000ms to 10,000ms (5 to 10 seconds).",level:"intermediate",codeExample:"SET GLOBAL rpl_semi_sync_master_timeout = 10000;"},{question:"How do you enable semi-synchronous replication dynamically without restarting the MySQL server?",shortAnswer:"Install the plugins and set `rpl_semi_sync_master_enabled = 1` on Source, set `rpl_semi_sync_slave_enabled = 1` on Replica, and restart the replica I/O thread (`STOP REPLICA IO_THREAD; START REPLICA IO_THREAD;`).",explanation:"Restarting the I/O thread forces the connection handshake to negotiate semi-sync capabilities.",hint:"Enable variables dynamically and restart the replica I/O thread.",level:"basic",codeExample:`# On Replica:
SET GLOBAL rpl_semi_sync_slave_enabled = 1;
STOP REPLICA IO_THREAD;
START REPLICA IO_THREAD;`},{question:"What is `Rpl_semi_sync_master_wait_sessions` in `SHOW GLOBAL STATUS`?",shortAnswer:"The number of client sessions currently paused and waiting for replica acknowledgments at that exact instant.",explanation:"A high number indicates the replica or network is bottlenecking transaction commits.",hint:"Count of client sessions currently blocked waiting for replica ACK.",level:"intermediate",codeExample:"SHOW GLOBAL STATUS LIKE 'Rpl_semi_sync_master_wait_sessions';"},{question:"What is `Rpl_semi_sync_master_time_avg` in `SHOW GLOBAL STATUS`?",shortAnswer:"The average time in microseconds spent waiting for replica acknowledgments per transaction.",explanation:"Measures the network latency and replica relay log write performance.",hint:"Average time in microseconds spent waiting for replica ACK.",level:"basic",codeExample:"SHOW GLOBAL STATUS LIKE 'Rpl_semi_sync_master_time_avg';"},{question:"Why should `rpl_semi_sync_master_wait_point` NEVER be set to `AFTER_COMMIT` in modern MySQL environments?",shortAnswer:"Because `AFTER_COMMIT` suffers from the phantom read anomaly where uncommitted data is visible to other sessions before replica acknowledgment, violating strict ACID transactional durability.",explanation:"`AFTER_SYNC` is strictly superior in data consistency guarantees with identical latency performance.",hint:"AFTER_COMMIT causes phantom reads on primary crash; always use AFTER_SYNC.",level:"intermediate",codeExample:`-- Always enforce AFTER_SYNC:
SET GLOBAL rpl_semi_sync_master_wait_point = 'AFTER_SYNC';`},{question:"What happens if a client executes `ROLLBACK` on a transaction under semi-synchronous replication?",shortAnswer:"Rolled-back transactions do not write changes to the Binary Log, so no events are sent to the replica and no semi-synchronous acknowledgment wait occurs.",explanation:"Only committed transactions generate binary log events and trigger semi-sync waits.",hint:"Rollbacks do not write to binlog and do not trigger semi-sync waits.",level:"basic",codeExample:"-- ROLLBACK returns immediately with 0ms replication wait."},{question:"Can an asynchronous read replica and a semi-synchronous standby replica connect to the same Source simultaneously?",shortAnswer:"Yes, MySQL supports mixed topologies where the Source replicates to 1 semi-sync standby for HA failover and 5 async read replicas for query offloading.",explanation:"The Source only waits for the semi-sync replica to acknowledge before committing.",hint:"Yes, MySQL supports mixing semi-sync and async replicas on the same Source.",level:"basic",codeExample:`# Mixed Topology:
# Source → Replica 1 (Semi-Sync Standby for HA)
#        → Replica 2 (Async Read Replica)
#        → Replica 3 (Async Read Replica)`},{question:"What is the role of `rpl_semi_sync_master_wait_no_slave` in MySQL?",shortAnswer:"When set to `ON` (default), the Source will wait for timeout even if zero semi-sync replicas are currently connected; when `OFF`, the Source degrades to async immediately if no replicas are registered.",explanation:"Controls fallback behavior when all replicas disconnect.",hint:"Controls whether Source waits for timeout when zero replicas are connected.",level:"expert",codeExample:"SHOW VARIABLES LIKE 'rpl_semi_sync_master_wait_no_slave';"},{question:"How do you permanently configure semi-synchronous replication in `my.cnf` so it survives server reboots?",shortAnswer:"Add `plugin-load = 'rpl_semi_sync_master=semisync_master.so;rpl_semi_sync_slave=semisync_slave.so'` and set `rpl_semi_sync_master_enabled = 1` and `rpl_semi_sync_master_wait_point = AFTER_SYNC` under `[mysqld]`.",explanation:"Ensures the plugins load automatically during server initialization.",hint:"Configure plugin-load and enabled flags in my.cnf under [mysqld].",level:"intermediate",codeExample:`[mysqld]
plugin-load-add = semisync_master.so
plugin-load-add = semisync_slave.so
rpl_semi_sync_master_enabled = 1
rpl_semi_sync_master_timeout = 10000
rpl_semi_sync_master_wait_point = AFTER_SYNC
rpl_semi_sync_slave_enabled = 1`},{question:"What is the difference between MySQL Semi-Synchronous Replication and MySQL Group Replication?",shortAnswer:"Semi-Sync is a primary-standby topology where the Source waits for 1 replica ACK into relay log; Group Replication is a fully distributed Paxos-based consensus cluster where a majority quorum must agree on transaction global certification before commit.",explanation:"Group Replication provides automated conflict detection and built-in cluster membership.",hint:"Semi-sync is primary-replica ACK; Group Replication is Paxos-based majority consensus.",level:"intermediate",codeExample:`-- Semi-Sync: 1 ACK required
-- Group Replication: Paxos majority consensus (e.g. 2 of 3 nodes)`},{question:"What is `Rpl_semi_sync_slave_status` in `SHOW GLOBAL STATUS` on the replica?",shortAnswer:"A binary flag (`ON` / `OFF`) indicating whether the replica is actively functioning as a semi-synchronous replica and sending acknowledgments back to the Source.",explanation:"Verifies that the replica has registered as a semi-sync receiver.",hint:"Indicates whether the replica is actively sending semi-sync acknowledgments.",level:"basic",codeExample:"SHOW GLOBAL STATUS LIKE 'Rpl_semi_sync_slave_status'; -- Value: ON"},{question:"How does Semi-Synchronous replication behave during bulk data imports (e.g. `LOAD DATA INFILE`)?",shortAnswer:"Because `LOAD DATA INFILE` executes as a single large transaction, the Source sends the entire bulk binlog event to the replica upon completion and waits for 1 ACK, adding only one network round-trip overhead for the entire bulk batch.",explanation:"Large transactions amortize the network RTT across millions of rows.",hint:"Adds only 1 network RTT for the entire batch upon transaction commit.",level:"intermediate",codeExample:"-- Bulk import generates 1 semi-sync ACK wait at COMMIT."},{question:"What is the recommended monitoring alert for Semi-Synchronous replication in Prometheus?",shortAnswer:"Trigger a critical alert when `mysql_global_status_rpl_semi_sync_master_status == 0` while the master should be in semi-sync mode, and alert if `rate(mysql_global_status_rpl_semi_sync_master_no_tx[1m]) > 0`.",explanation:"Alerts the on-call team immediately when the cluster loses its RPO = 0 guarantee.",hint:"Alert on master status == 0 or no_tx counter incrementing.",level:"basic",codeExample:`alert: MySQLSemiSyncDegradedToAsync
expr: mysql_global_status_rpl_semi_sync_master_status == 0
for: 1m
labels:
  severity: critical`},{question:"Why does Semi-Synchronous replication NOT guarantee that the replica's database tables are immediately updated when the client receives `COMMIT`?",shortAnswer:"Because Semi-Sync only waits for the replica's **I/O thread** to write events to the **Relay Log**; the replica's **SQL thread** may still take a few milliseconds or seconds to apply the transaction to InnoDB tables.",explanation:"The data is durable on disk in the relay log (RPO = 0), but reading immediately from the replica may still experience brief replication lag.",hint:"ACK guarantees data is in the replica's Relay Log, not necessarily applied in InnoDB yet.",level:"expert",codeExample:"-- Data is durable in Relay Log (RPO = 0), but SQL thread applier may have minor lag."},{question:"What is the primary operational takeaway of Topic 2 in Module 004_006?",shortAnswer:"Lossless Semi-Synchronous Replication (`rpl_semi_sync_master_wait_point = AFTER_SYNC`) bridges the gap between performance and durability: it guarantees **RPO = 0** by ensuring transactions reside in the standby replica's relay log before client commits complete, adds negligible sub-millisecond LAN latency, eliminates phantom reads on Source crashes, and automatically falls back to Asynchronous mode upon timeout to preserve database availability.",explanation:"Enabling Lossless Semi-Sync is the single most effective way to eliminate data loss on MySQL primary-replica topologies.",hint:"Summarize AFTER_SYNC for RPO = 0, zero phantom reads, sub-ms LAN latency, and automated async fallback on timeout.",level:"basic",codeExample:`-- Master Lossless Semi-Sync Recipe:
# 1. Source:
INSTALL PLUGIN rpl_semi_sync_master SONAME 'semisync_master.so';
SET GLOBAL rpl_semi_sync_master_enabled = 1;
SET GLOBAL rpl_semi_sync_master_wait_point = 'AFTER_SYNC';
SET GLOBAL rpl_semi_sync_master_timeout = 10000;

# 2. Replica:
INSTALL PLUGIN rpl_semi_sync_slave SONAME 'semisync_slave.so';
SET GLOBAL rpl_semi_sync_slave_enabled = 1;
STOP REPLICA IO_THREAD; START REPLICA IO_THREAD;`}],u=`================================================================================\r
MYSQL 8.0 REPLICATION, HIGH AVAILABILITY & FAILOVER TOPOLOGIES\r
TOPIC 2: Asynchronous Replication vs Lossless Semi-Synchronous Replication\r
================================================================================\r
\r
1. REPLICATION MODES COMPARED\r
--------------------------------------------------------------------------------\r
Feature               Asynchronous Replication          Lossless Semi-Sync Replication (AFTER_SYNC)\r
--------------------------------------------------------------------------------\r
Commit Wait Point     Zero wait; commits locally        Waits for >= 1 replica ACK before\r
                      and returns immediately.          committing to InnoDB storage engine.\r
\r
Data Loss Risk (RPO)  RPO > 0 (Committed transactions   RPO = 0 (Transactions guaranteed in\r
                      can be lost if Source crashes).   replica relay log before commit returns).\r
\r
Phantom Reads Risk    None locally, but un-replicated   Zero phantom reads on Source crash.\r
                      data disappears on failover.\r
\r
Client Latency        0ms replication latency overhead  Network Round-Trip Time (RTT) added\r
                      added to client commits.          to each write transaction commit (~1ms).\r
\r
2. LOSSLESS SEMI-SYNC WAIT POINTS: AFTER_SYNC VS AFTER_COMMIT\r
--------------------------------------------------------------------------------\r
1. \`AFTER_SYNC\` (MySQL 8.0 Default / Recommended):\r
   - Flow: Prepare in InnoDB → Flush to Binlog → Send to Replica → WAIT FOR REPLICA ACK → Commit in InnoDB → Return to Client.\r
   - Benefit: Other concurrent sessions cannot see the data until it is safely acknowledged by the replica (Lossless!).\r
\r
2. \`AFTER_COMMIT\` (Legacy Semi-Sync):\r
   - Flow: Prepare in InnoDB → Flush to Binlog → Commit in InnoDB → Send to Replica → WAIT FOR REPLICA ACK → Return to Client.\r
   - Flaw: If Source crashes while waiting for ACK, other sessions already saw data that was never replicated (Phantom Reads!).\r
\r
3. CONFIGURATION RUNBOOK FOR MYSQL 8.0\r
--------------------------------------------------------------------------------\r
-- On Source (Primary):\r
INSTALL PLUGIN rpl_semi_sync_master SONAME 'semisync_master.so';\r
SET GLOBAL rpl_semi_sync_master_enabled = 1;\r
SET GLOBAL rpl_semi_sync_master_wait_point = 'AFTER_SYNC';\r
SET GLOBAL rpl_semi_sync_master_timeout = 10000; -- 10 seconds before fallback to async\r
SET GLOBAL rpl_semi_sync_master_wait_for_num_slaves = 1;\r
\r
-- On Replica (Standby):\r
INSTALL PLUGIN rpl_semi_sync_slave SONAME 'semisync_slave.so';\r
SET GLOBAL rpl_semi_sync_slave_enabled = 1;\r
STOP REPLICA IO_THREAD; START REPLICA IO_THREAD;\r
\r
4. PRODUCTION BEST PRACTICES\r
--------------------------------------------------------------------------------\r
1. Always enforce \`rpl_semi_sync_master_wait_point = 'AFTER_SYNC'\` for RPO = 0.\r
2. Size \`rpl_semi_sync_master_timeout\` (e.g. 10000ms) to prevent permanent write freezes.\r
3. Monitor \`Rpl_semi_sync_master_status\` to ensure semi-sync is actively ON (1).\r
4. Alert when \`Rpl_semi_sync_master_yes_tx\` stops incrementing and \`no_tx\` rises.\r
================================================================================\r
`,S=()=>{const[i,o]=c.useState("mode1_after_sync"),a={mode1_after_sync:{modeName:"1. Lossless Semi-Sync (AFTER_SYNC)",title:"1. Lossless Semi-Sync: AFTER_SYNC (MySQL 8.0 Default)",badge:"RPO = 0 Guaranteed",badgeColor:"emerald",sqlSnippet:`-- 🛡️ LOSSLESS SEMI-SYNCHRONOUS FLOW (AFTER_SYNC):
-- 1. Client sends COMMIT
-- 2. Source prepares in InnoDB & flushes to Binary Log
-- 3. Source sends binlog event to Replica over TCP
-- 4. Replica writes event to Relay Log & sends ACK
-- 5. Source commits in InnoDB & returns OK to Client!

-- ⚡ Source Configuration:
SET GLOBAL rpl_semi_sync_master_enabled = 1;
SET GLOBAL rpl_semi_sync_master_wait_point = 'AFTER_SYNC';
SET GLOBAL rpl_semi_sync_master_timeout = 10000; -- 10s fallback`,explanation:"AFTER_SYNC guarantees zero data loss (RPO = 0) and eliminates phantom reads because the Source delays committing the transaction in InnoDB until at least one replica confirms saving the event into its local Relay Log.",keyTakeaways:["Guarantees RPO = 0 with zero data loss on primary crash.","Eliminates phantom reads by holding local InnoDB commit until ACK.","Adds only network RTT latency (~1ms in LAN) to write transactions."]},mode2_async_replication:{modeName:"2. Asynchronous Replication",title:"2. Asynchronous Replication (Zero Commit Latency)",badge:"RPO > 0 (Risk)",badgeColor:"rose",sqlSnippet:`-- ⚡ ASYNCHRONOUS REPLICATION FLOW:
-- 1. Client sends COMMIT
-- 2. Source writes to Binary Log & commits immediately in InnoDB
-- 3. Source returns OK to Client (0ms replication wait!)
-- 4. Binlog Dump thread streams events to Replica in background

-- ⚠️ Risk: If Source crashes at step 3, un-streamed transactions are LOST!`,explanation:"Asynchronous replication provides maximum write performance with 0ms commit latency overhead, but risks losing committed transactions (RPO > 0) if the primary server crashes before transmitting events over the network.",keyTakeaways:["Zero replication latency overhead added to write transactions.","RPO > 0: Committed data can be lost if the primary crashes.","Best suited for read-replica scaling where minor lag is acceptable."]},mode3_legacy_after_commit:{modeName:"3. Legacy Semi-Sync (AFTER_COMMIT)",title:"3. Legacy Semi-Sync: AFTER_COMMIT (Phantom Read Flaw)",badge:"Legacy Flawed",badgeColor:"purple",sqlSnippet:`-- 🛑 LEGACY SEMI-SYNC FLOW (AFTER_COMMIT):
-- 1. Source prepares & COMMITS in InnoDB immediately!
-- 2. Concurrent sessions can now SEE the new un-replicated data!
-- 3. Source sends event to Replica & waits for ACK
-- 4. 💥 CRASH SCENARIO: Source crashes before ACK!
-- → Failover promotes Replica, but transaction is GONE (Phantom Read)!`,explanation:"In legacy AFTER_COMMIT, the Source commits to storage engine before waiting for replica acknowledgment. If the Source crashes during the wait, concurrent sessions saw phantom data that disappears upon replica promotion.",keyTakeaways:["Commits locally in InnoDB before waiting for replica ACK.","Allows concurrent sessions to see un-replicated data (Phantom Reads).","Obsolete in MySQL 8.0; always use AFTER_SYNC instead."]},mode4_fallback_and_telemetry:{modeName:"4. Dynamic Fallback & Telemetry",title:"4. Automated Async Fallback & Telemetry Monitoring",badge:"Fail-Safe Telemetry",badgeColor:"cyan",sqlSnippet:`-- 📊 MONITORING SEMI-SYNC STATUS ON SOURCE:
SHOW GLOBAL STATUS LIKE 'Rpl_semi_sync_master_%';

-- Key Telemetry Metrics:
-- Rpl_semi_sync_master_status: ON (Actively operating in semi-sync)
-- Rpl_semi_sync_master_yes_tx: 145829 (ACKed transactions)
-- Rpl_semi_sync_master_no_tx: 0 (Un-ACKed async fallback transactions)
-- Rpl_semi_sync_master_clients: 2 (Connected semi-sync replicas)`,explanation:"If all replicas fail to acknowledge within rpl_semi_sync_master_timeout, the Source automatically degrades to Asynchronous mode to prevent blocking writes, and seamlessly resumes Semi-Sync when replicas reconnect.",keyTakeaways:["Automatically falls back to async mode on timeout to preserve availability.","Seamlessly restores semi-sync mode when replicas reconnect.","Monitor Rpl_semi_sync_master_no_tx to detect degraded async operation."]}},s=a[i];return e.jsxs("div",{className:"bg-slate-950 text-slate-100 min-h-screen py-10 px-4 sm:px-6 lg:px-8 font-sans",children:[e.jsxs("header",{className:"max-w-6xl mx-auto mb-10 text-center sm:text-left border-b border-slate-800 pb-8",children:[e.jsxs("div",{className:"flex flex-wrap items-center gap-3 mb-3",children:[e.jsx("span",{className:"px-3 py-1 bg-emerald-950/80 text-emerald-400 border border-emerald-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Module 004.6: Replication, High Availability & Failover"}),e.jsx("span",{className:"px-3 py-1 bg-cyan-950/80 text-cyan-400 border border-cyan-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Topic 2 of 14"})]}),e.jsxs("h1",{className:"text-xl sm:text-2xl md:text-3xl font-bold text-white tracking-tight leading-tight",children:[e.jsx("span",{className:"text-emerald-400",children:"Asynchronous"})," vs ",e.jsx("span",{className:"text-cyan-400",children:"Lossless Semi-Sync"})," Replication"]}),e.jsxs("p",{className:"mt-4 text-base sm:text-lg text-slate-400 max-w-4xl leading-relaxed",children:["Mastering replication synchronicity in MySQL 8.0: contrasting Asynchronous replication with Lossless Semi-Synchronous replication (",e.jsx("code",{children:"AFTER_SYNC"}),"), evaluating commit wait-point flows, preventing phantom reads, and monitoring automatic fallback telemetry."]})]}),e.jsxs("main",{className:"max-w-6xl mx-auto space-y-16",children:[e.jsxs("section",{id:"modes-pillars",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"1. The Four Pillars of Replication Synchronicity"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"How MySQL balances client write latency against transaction durability and RPO = 0 guarantees."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6",children:[e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-emerald-400 font-bold uppercase",children:"Mode 1"}),e.jsx("h3",{className:"font-bold text-white text-base",children:"AFTER_SYNC"}),e.jsx("p",{className:"text-xs text-slate-300 leading-relaxed",children:"Waits for replica ACK before committing in InnoDB; guarantees RPO = 0 and eliminates phantom reads."})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-bold uppercase",children:"Mode 2"}),e.jsx("h3",{className:"font-bold text-white text-base",children:"Asynchronous"}),e.jsx("p",{className:"text-xs text-slate-300 leading-relaxed",children:"Zero commit wait; maximum write throughput with potential data loss on unexpected primary crash."})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-purple-700/60 bg-purple-950/20 space-y-2 shadow-lg shadow-purple-950/40",children:[e.jsx("span",{className:"text-xs font-mono text-purple-400 font-bold uppercase",children:"Mode 3"}),e.jsx("h3",{className:"font-bold text-purple-300 text-base",children:"AFTER_COMMIT"}),e.jsx("p",{className:"text-xs text-slate-200 leading-relaxed",children:"Legacy flawed semi-sync mode where un-replicated data was visible locally before failover."})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-rose-700/60 bg-rose-950/20 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-rose-400 font-bold uppercase",children:"Mode 4"}),e.jsx("h3",{className:"font-bold text-rose-300 text-base",children:"Dynamic Fallback"}),e.jsx("p",{className:"text-xs text-slate-200 leading-relaxed",children:"Source automatically degrades to async upon timeout and restores semi-sync when replicas catch up."})]})]})]}),e.jsxs("section",{id:"interactive-workbench",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"2. Interactive Synchronicity Workbench"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Test and observe AFTER_SYNC lossless flows, asynchronous throughput, phantom read flaws, and telemetry status variables."})]}),e.jsx("div",{className:"flex flex-wrap gap-3",children:Object.keys(a).map(t=>{const n=a[t],l=i===t;return e.jsx("button",{onClick:()=>o(t),className:r("px-4 py-2.5 rounded-xl font-medium text-xs sm:text-sm transition-all duration-200 border",l?"bg-emerald-500/10 border-emerald-500 text-emerald-400 shadow-lg shadow-emerald-950/40":"bg-slate-900/60 border-slate-800 text-slate-400 hover:text-slate-200 hover:bg-slate-900"),children:n.modeName},t)})}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-6",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-3 border-b border-slate-800 pb-4",children:[e.jsxs("div",{children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-semibold uppercase tracking-wider",children:"Replication Synchronicity"}),e.jsx("h3",{className:"text-lg sm:text-xl font-bold text-white mt-1",children:s.title})]}),e.jsx("span",{className:r("px-3 py-1 rounded-full text-xs font-mono font-semibold border w-fit",s.badgeColor==="emerald"&&"bg-emerald-950/80 text-emerald-300 border-emerald-700",s.badgeColor==="cyan"&&"bg-cyan-950/80 text-cyan-300 border-cyan-700",s.badgeColor==="purple"&&"bg-purple-950/80 text-purple-300 border-purple-700",s.badgeColor==="rose"&&"bg-rose-950/80 text-rose-300 border-rose-700"),children:s.badge})]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:s.explanation}),e.jsxs("div",{className:"space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"SQL Commands & Architecture Flow:"}),e.jsx("pre",{className:"p-4 rounded-xl bg-slate-950 border border-slate-800 font-mono text-xs text-emerald-300 overflow-x-auto leading-relaxed",children:s.sqlSnippet})]}),e.jsxs("div",{className:"space-y-2 pt-2 border-t border-slate-800/80",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"Architectural Takeaways:"}),e.jsx("ul",{className:"grid grid-cols-1 md:grid-cols-3 gap-3 text-xs text-slate-300",children:s.keyTakeaways.map((t,n)=>e.jsxs("li",{className:"p-3 rounded-lg bg-slate-950/70 border border-slate-800/60 flex items-start gap-2",children:[e.jsx("span",{className:"text-emerald-400 font-bold",children:"✓"}),e.jsx("span",{children:t})]},n))})]})]})]}),e.jsxs("section",{id:"case-studies",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-purple-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"3. Real-World Engineering Scenarios in Bengal"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Synchronous replication case studies in Barrackpore and Kolkata demonstrating zero invoice data loss and multi-replica banking compliance."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-emerald-400"}),"Case 1: Mamata & Susmita – Zero Invoice Loss in Barrackpore Retail Crash"]}),e.jsx("span",{className:"text-xs font-mono text-emerald-400 bg-emerald-950/80 px-2.5 py-1 rounded-full border border-emerald-800",children:"RPO = 0 Protected"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Barrackpore, a primary server crashed during a lightning storm. Because Susmita had enabled Lossless Semi-Sync with ",e.jsx("code",{children:"AFTER_SYNC"}),", the last batch of 12 invoice transactions had been confirmed in the standby replica's relay log before the crash. Promoting the standby replica restored ₹45,000 in customer transactions with zero data loss across ₹1.2 Crores in store inventory."]})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-cyan-400"}),"Case 2: Abhronila & Debangshu – Multi-Slave Semi-Sync in Kolkata Bank"]}),e.jsx("span",{className:"text-xs font-mono text-cyan-400 bg-cyan-950/80 px-2.5 py-1 rounded-full border border-cyan-800",children:"2-Slave Consensus"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Kolkata, managing core banking across ₹500 Crores in daily volume required surviving dual-node failures. Debangshu configured ",e.jsx("code",{children:"rpl_semi_sync_master_wait_for_num_slaves = 2"}),", guaranteeing that every financial transaction was durable across at least two standby replicas before client commits completed, maintaining full RBI regulatory compliance."]})]})]})]}),e.jsxs("section",{id:"pitfalls-rules",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-rose-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"4. Senior Pitfalls & Production Best Practices"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Avoid dangerous legacy AFTER_COMMIT configurations and unmonitored async degradations."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 1: Using Legacy AFTER_COMMIT"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Setting ",e.jsx("code",{children:"rpl_semi_sync_master_wait_point = AFTER_COMMIT"})," allows concurrent sessions to see un-replicated data locally before Source crashes, triggering phantom read data loss."]}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Always configure rpl_semi_sync_master_wait_point = 'AFTER_SYNC'."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 2: Silent Async Degradation"]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:"When replicas disconnect, the Source silently degrades to asynchronous mode; without alerts, DBAs assume they have RPO = 0 protection when they do not."}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Alert when Rpl_semi_sync_master_status drops to OFF or no_tx increments."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 1: Size Semi-Sync Timeouts Properly"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Set ",e.jsx("code",{children:"rpl_semi_sync_master_timeout = 10000"})," (10 seconds) to prevent transient network jitter from causing premature fallback while avoiding permanent write freezes."]}),e.jsx("div",{className:"text-xs text-slate-400",children:"Balances data loss prevention with database write availability."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 2: Co-locate Semi-Sync in Same LAN"]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:"Keep the semi-synchronous standby replica in the same datacenter or cloud Availability Zone to keep commit latency overhead under 1ms."}),e.jsx("div",{className:"text-xs text-slate-400",children:"Use asynchronous replication for distant cross-region disaster recovery nodes."})]})]})]}),e.jsxs("section",{id:"printable-note",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"5. Printable Study Note & Teacher Advice"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Download complete printable notes and review key takeaways from Sir Sukanta Hui."})]}),e.jsx(p,{title:"Topic 2: Asynchronous Replication vs Semi-Synchronous Replication (Lossless Semi-Sync)",content:u}),e.jsx(m,{note:"Lossless Semi-Synchronous Replication is the gold standard for enterprise MySQL reliability! Always use rpl_semi_sync_master_wait_point = AFTER_SYNC to guarantee RPO = 0 and eliminate phantom reads on primary crashes. Keep your semi-sync standbys in the same low-latency LAN for sub-millisecond commits, and monitor Rpl_semi_sync_master_status with automated alerts so you know immediately if the cluster degrades to asynchronous mode!"})]}),e.jsxs("section",{id:"faq-section",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"6. Frequently Asked Questions (30 Deep-Dive Questions)"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Master the technical nuances of Asynchronous vs Lossless Semi-Sync replication, AFTER_SYNC vs AFTER_COMMIT wait points, fallback timeouts, and telemetry metrics."})]}),e.jsx(d,{title:"Asynchronous vs Semi-Sync Replication FAQs",questions:h})]})]})]})};export{S as default};
