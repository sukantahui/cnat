import{b as c,j as e,bg as i}from"./vendor-react-core-Doz9nIC6.js";import{T as d}from"./TeacherSukantaHui-CC0AKmkm.js";import{F as p}from"./FAQTemplate-CkSqDH4B.js";import{P as u}from"./PlainTextPrint-C08xhKA4.js";import"./vendor-icons-BtJHuk7w.js";const m=[{question:"What is the fundamental difference between a Logical Backup and a Physical Backup in MySQL?",shortAnswer:"A Logical Backup extracts data as plain text SQL statements (`CREATE TABLE`, `INSERT INTO`); a Physical Backup copies the raw binary database files (`.ibd`, redo logs, undo tablespaces) directly from disk.",explanation:"Logical backups are human-readable, editable, and highly portable across MySQL versions and operating systems. Physical backups are binary-bound, but restore orders of magnitude faster because they bypass SQL parsing and index rebuilding.",hint:"Logical = SQL text files; Physical = raw binary data files (.ibd).",level:"basic",codeExample:`# Logical Backup (mysqldump):
mysqldump -u root -p ecommerce > backup.sql

# Physical Backup (Percona XtraBackup):
xtrabackup --backup --target-dir=/backups/physical_2026/`},{question:"What is the difference between Cold, Warm, and Hot backups?",shortAnswer:"Cold: Taken while the database is stopped (offline). Warm: Taken while the database is read-only (`FLUSH TABLES WITH READ LOCK`). Hot: Taken online with concurrent reads and writes fully active.",explanation:"Cold backups require downtime. Warm backups block all data mutations. Hot backups use InnoDB MVCC snapshots or background redo log streaming to take consistent backups without impacting online user transactions.",hint:"Cold = offline; Warm = read-only; Hot = full online concurrent read/write.",level:"basic",codeExample:`-- Hot Backup Logical: mysqldump --single-transaction
-- Hot Backup Physical: xtrabackup --backup`},{question:"How does `mysqldump --single-transaction` perform an online Hot Backup of InnoDB tables without blocking writes?",shortAnswer:"It starts a transaction with `REPEATABLE READ` isolation (`START TRANSACTION WITH CONSISTENT SNAPSHOT`), reading a consistent MVCC point-in-time snapshot from the undo logs while concurrent transactions continue modifying tables.",explanation:"Because InnoDB uses Multi-Version Concurrency Control (MVCC), reads do not block writes and writes do not block reads. The dump process reads historical row versions from the undo logs as of the transaction start time.",hint:"Uses InnoDB MVCC consistent snapshot under REPEATABLE READ isolation.",level:"intermediate",codeExample:"mysqldump --single-transaction --quick --routines --triggers ecommerce > backup.sql"},{question:"How does Percona XtraBackup capture a consistent Hot Physical Backup while transactions are actively modifying `.ibd` files on disk?",shortAnswer:"It copies the raw `.ibd` files while running a background thread that continuously reads and records active InnoDB Redo Log changes, then applies those redo logs during the `--prepare` phase to reach a consistent state.",explanation:"Because physical file copying takes time, pages copied at the beginning may be modified before the end of the backup. XtraBackup records all page mutations in the redo log logstream and replays them during `--prepare`, bringing all data pages to a consistent Log Sequence Number (LSN).",hint:"Copies files while streaming redo logs, then applies redo logs during the prepare phase.",level:"expert",codeExample:`# Step 1: Online non-blocking backup:
xtrabackup --backup --target-dir=/data/backups/base

# Step 2: Prepare (apply redo logs to achieve crash consistency):
xtrabackup --prepare --target-dir=/data/backups/base`},{question:"What is an Incremental Backup in MySQL, and how does it differ from a Differential Backup?",shortAnswer:"An Incremental Backup copies only the data changed since the last backup (Full or Incremental); a Differential Backup copies all data changed since the last Full baseline backup.",explanation:"Incremental backups are faster to capture and use less storage, but require restoring the complete chain of incremental files in chronological order. Differential backups require only the base backup plus the latest differential file.",hint:"Incremental = delta since last backup; Differential = delta since last full baseline.",level:"intermediate",codeExample:`# Full Backup (Sunday):
xtrabackup --backup --target-dir=/backups/base

# Incremental Backup (Monday):
xtrabackup --backup --target-dir=/backups/inc1 --incremental-basedir=/backups/base`},{question:"How does Percona XtraBackup determine which 16KB data pages have changed when performing an Incremental Backup?",shortAnswer:"It inspects the Log Sequence Number (LSN) stored in the header of each 16KB InnoDB page, copying only pages whose LSN is greater than the previous backup's checkpoint LSN.",explanation:"Every time a page is modified in InnoDB, its header LSN is incremented. XtraBackup reads the page headers and skips unchanged pages, creating compact incremental backup archives.",hint:"Compares page header Log Sequence Numbers (LSNs) against previous checkpoint.",level:"expert",codeExample:"-- Incremental logic: IF page.LSN > last_backup.to_lsn THEN copy_page(page)"},{question:"Why are Logical Backups preferred over Physical Backups when performing cross-version upgrades (e.g. MySQL 5.7 to MySQL 8.0)?",shortAnswer:"Because physical data dictionary layouts and binary page structures differ between major versions, whereas logical SQL text statements (`CREATE TABLE`, `INSERT`) are version-agnostic and universally portable.",explanation:"Directly copying MySQL 5.7 physical files into MySQL 8.0 can lead to data dictionary corruption. A logical SQL dump re-creates tables using the new version's native SQL parser.",hint:"Logical SQL files are portable across major version and architecture boundaries.",level:"intermediate",codeExample:`# Exporting from MySQL 5.7 for MySQL 8.0 import:
mysqldump -h old-57-server -u root -p --single-transaction db > dump_57.sql
mysql -h new-80-server -u root -p db < dump_57.sql`},{question:"Why does restoring a 2TB logical SQL dump take 8+ hours, while restoring a 2TB physical backup takes under 15 minutes?",shortAnswer:"A logical restore requires the CPU to parse billions of SQL characters, execute transactions, and compute B-tree indexes from scratch; a physical restore is a direct block-level file copy at maximum NVMe disk I/O speed.",explanation:"Building secondary indexes on 2TB of data is CPU and I/O intensive. Physical restore simply moves pre-built `.ibd` files back into `/var/lib/mysql`, avoiding all computational overhead.",hint:"Block file copy is orders of magnitude faster than building indexes row-by-row.",level:"basic",codeExample:`-- Logical Restore: Parses 50,000,000 INSERT statements + builds 200 indexes (Slow!)
-- Physical Restore: Copies raw .ibd files to disk (NVMe Line-Rate Speed!)`},{question:"In Mamata & Susmita's Barrackpore retail store, a 15GB inventory database needed daily automated backups. Which backup classification and tool was optimal?",shortAnswer:"A Logical Hot Backup using `mysqldump --single-transaction --quick --routines --triggers`.",explanation:"For a 15GB database, `mysqldump` completes in under 3 minutes, provides a portable human-readable SQL file, and allows restoring individual tables easily without incurring the operational complexity of physical backup tooling.",hint:"Databases under 20-50GB are ideally suited for mysqldump logical hot backups.",level:"moderate",codeExample:`# Barrackpore 15GB Daily Backup Command:
mysqldump --single-transaction --quick --routines --triggers --events \\
  -u backup_admin -p barrackpore_store | gzip > /backups/store_$(date +%F).sql.gz`},{question:"In Abhronila & Debangshu's Kolkata fintech bank, managing a 5TB transaction cluster processing ₹500 Crores in volume required zero read/write downtime. Which backup tool was selected?",shortAnswer:"A Physical Hot Backup using Percona XtraBackup with daily incremental LSN snapshots.",explanation:"At 5TB scale, `mysqldump` would cause excessive memory bloat and take 12+ hours to restore. Percona XtraBackup takes non-blocking physical snapshots and satisfies their strict 15-minute RTO SLA.",hint:"Multi-terabyte databases require physical hot backups like Percona XtraBackup.",level:"expert",codeExample:`# Kolkata Fintech 5TB Daily Base Backup:
xtrabackup --backup --stream=xbstream --parallel=8 --target-dir=/backups/ | \\
  zstd -T8 > /backups/kolkata_bank_base.xbstream.zst`},{question:"What is the primary risk of using `mysqldump` WITHOUT the `--single-transaction` flag on a production InnoDB database?",shortAnswer:"It executes `FLUSH TABLES WITH READ LOCK;`, converting the backup into a Warm backup that blocks all write queries (`INSERT`, `UPDATE`, `DELETE`) across the entire server for the duration of the dump.",explanation:"On a large database, dumping without `--single-transaction` will lock all application writes for hours, causing immediate production application outages.",hint:"Without --single-transaction, mysqldump locks all tables for writing.",level:"basic",codeExample:`# DANGEROUS IN PRODUCTION (Locks all writes!):
# mysqldump ecommerce > backup.sql

# SAFE IN PRODUCTION (Non-blocking MVCC snapshot):
mysqldump --single-transaction ecommerce > backup.sql`},{question:"What is `mydumper` / `myloader`, and why is it faster than standard `mysqldump` for medium-sized databases (20GB - 100GB)?",shortAnswer:"It is a high-performance open-source logical backup tool that dumps and restores tables in parallel using multiple multi-threaded worker connections.",explanation:"Standard `mysqldump` runs on a single CPU thread. `mydumper` splits tables into chunks and dumps them concurrently across 8 or 16 threads, reducing backup and restore times by 70-80%.",hint:"Multi-threaded parallel logical dump and restore tool.",level:"intermediate",codeExample:`# Parallel logical backup using 8 CPU threads:
mydumper -B ecommerce -t 8 -o /backups/parallel_dump/
myloader -B ecommerce -t 8 -d /backups/parallel_dump/`},{question:"What happens if a `mysqldump --single-transaction` operation encounters non-InnoDB tables (like MyISAM or MEMORY)?",shortAnswer:"MyISAM tables do not support MVCC transactions; therefore, changes made to MyISAM tables during the dump can result in inconsistent data unless locked.",explanation:"Because `--single-transaction` relies on InnoDB transaction snapshots, non-transactional storage engines cannot guarantee consistency without applying table locks.",hint:"Non-transactional tables (MyISAM) cannot provide consistent MVCC snapshots.",level:"intermediate",codeExample:`-- Ensure all production tables use InnoDB:
SELECT TABLE_SCHEMA, TABLE_NAME, ENGINE 
FROM information_schema.TABLES 
WHERE ENGINE != 'InnoDB' AND TABLE_SCHEMA NOT IN ('mysql', 'information_schema', 'performance_schema');`},{question:"How does the `--quick` flag in `mysqldump` prevent out-of-memory (OOM) errors on large tables?",shortAnswer:"It forces `mysqldump` to retrieve rows from the server one row at a time instead of buffering the entire table into client RAM before writing to disk.",explanation:"Without `--quick`, dumping a 50GB table forces the `mysqldump` client process to load all 50GB into client RAM, causing memory exhaustion and process crashing.",hint:"Streams rows row-by-row instead of buffering whole tables in RAM.",level:"basic",codeExample:"mysqldump --single-transaction --quick ecommerce > backup.sql"},{question:"What is a 'Snapshot Backup' using storage-layer technologies (AWS EBS, ZFS, LVM)?",shortAnswer:"A point-in-time copy of storage disk blocks created almost instantaneously at the storage volume layer.",explanation:"Storage snapshots freeze the filesystem blocks in seconds. When restored, InnoDB performs standard crash recovery from the redo logs to ensure full transactional consistency.",hint:"Instant block-level storage volume snapshotting.",level:"intermediate",codeExample:`# LVM snapshot creation:
lvcreate --size 50G --snapshot --name mysql_snap /dev/vg01/mysql_data`},{question:"What is the role of the `--master-data` (or `--source-data` in MySQL 8.0+) flag in `mysqldump`?",shortAnswer:"It writes the exact binary log file name and position coordinates at the time of the snapshot into the SQL dump file as a `CHANGE MASTER TO` (or `CHANGE REPLICATION SOURCE TO`) statement.",explanation:"This coordinate is essential for setting up replication replicas and performing Point-in-Time Recovery (PITR) using subsequent binary logs.",hint:"Records binary log file coordinates inside the SQL dump for PITR and replication.",level:"intermediate",codeExample:`mysqldump --single-transaction --source-data=2 ecommerce > backup.sql
-- Output inside backup.sql:
-- CHANGE REPLICATION SOURCE TO SOURCE_LOG_FILE='binlog.000045', SOURCE_LOG_POS=1568;`},{question:"What is the sequence of commands to prepare a Percona XtraBackup base backup with two incremental backups for restoration?",shortAnswer:"1. `xtrabackup --prepare --apply-log-only --target-dir=/backups/base` 2. `xtrabackup --prepare --apply-log-only --target-dir=/backups/base --incremental-dir=/backups/inc1` 3. `xtrabackup --prepare --target-dir=/backups/base --incremental-dir=/backups/inc2`.",explanation:"Applying the incremental backups rolls forward page changes into the base backup directory. The final prepare step (without `--apply-log-only`) performs rollback of uncommitted transactions.",hint:"Apply incrementals sequentially with --apply-log-only, omitting it on the final step.",level:"expert",codeExample:`# Applying incremental chain:
xtrabackup --prepare --apply-log-only --target-dir=/backups/base
xtrabackup --prepare --apply-log-only --target-dir=/backups/base --incremental-dir=/backups/inc1
xtrabackup --prepare --target-dir=/backups/base --incremental-dir=/backups/inc2`},{question:"Why should `mysqldump` backups always include `--routines`, `--triggers`, and `--events`?",shortAnswer:"By default, `mysqldump` does NOT dump stored procedures, functions, or scheduled events unless these flags are explicitly supplied.",explanation:"Omitting these flags results in an incomplete database backup where business logic stored inside procedures, triggers, and scheduled jobs is lost upon restore.",hint:"Stored procedures and scheduled events are not backed up by default.",level:"basic",codeExample:"mysqldump --single-transaction --routines --triggers --events ecommerce > full_backup.sql"},{question:"Can an individual table be extracted from a large monolithic `mysqldump` SQL file?",shortAnswer:"Yes, using text parsing tools like `sed` or `awk` to extract the `CREATE TABLE` and `INSERT` block for that specific table, or using `myloader` for modular dumps.",explanation:"Because logical dumps are plain text SQL, scripts can parse out specific table DDL and DML statements without restoring the entire multi-gigabyte file.",hint:"Text parsing tools can extract specific table definitions and INSERTs from SQL dumps.",level:"intermediate",codeExample:"# Extracting 'orders' table from full dump:\nsed -n -e '/DROP TABLE.*`orders`/,/UNLOCK TABLES/p' full_backup.sql > orders_only.sql"},{question:"What is `mysqlpump`, and how does it differ from legacy `mysqldump` in MySQL 8.0?",shortAnswer:"It is an official MySQL utility that performs parallel multi-threaded logical dumps of multiple tables and databases concurrently.",explanation:"Introduced in MySQL 5.7/8.0, `mysqlpump` uses thread parallelism to speed up logical dumps. However, it is deprecated in MySQL 8.0.34 in favor of MySQL Shell Dump & Load utilities.",hint:"Parallel logical dump utility developed by Oracle for MySQL.",level:"intermediate",codeExample:"mysqlpump --default-parallelism=4 ecommerce > pump_backup.sql"},{question:"What is the MySQL Shell `util.dumpInstance()` / `util.dumpSchemas()` utility?",shortAnswer:"A modern, ultra-fast parallel logical dumping utility built into MySQL Shell that writes partitioned JSON/CSV/SQL chunks with built-in compression and cloud upload capabilities.",explanation:"MySQL Shell Dump Utility outperforms legacy `mysqldump` by over 10x by executing parallel reads, compression, and direct streaming to AWS S3, Azure Blob, or OCI Object Storage.",hint:"High-performance parallel dump utility inside MySQL Shell.",level:"expert",codeExample:`// In MySQL Shell (JS Mode):
util.dumpSchemas(['kolkata_retail'], '/backups/shell_dump', {threads: 8, compression: 'zstd'});`},{question:"What is the difference between Table-Level Locking and Row-Level MVCC during backup capture?",shortAnswer:"Table-level locking (`LOCK TABLES READ`) blocks all concurrent write queries on the table; Row-level MVCC reads historical versions from undo logs with zero locking on active writers.",explanation:"MVCC allows applications to process thousands of transactions per second uninterrupted while the backup process reads consistent historical snapshots.",hint:"MVCC provides non-blocking reads; table locks block all concurrent writing.",level:"basic",codeExample:"-- MVCC reads do not acquire table locks on InnoDB tables."},{question:"How do you verify the exit status of a `mysqldump` command in automated shell backup scripts?",shortAnswer:"Check the `$?` shell exit code immediately following the command (`if [ $? -eq 0 ]; then echo 'Success'; fi`).",explanation:"If `mysqldump` encounters a network timeout or disk error, it returns a non-zero exit code. Scripts must verify `$?` before marking backups as successful or compressing them.",hint:"Check the $? exit code to ensure the backup completed without errors.",level:"basic",codeExample:`mysqldump --single-transaction ecommerce > backup.sql
if [ $? -ne 0 ]; then
  echo "Backup FAILED!" | mail -s "Alert" dba@company.com
  exit 1
fi`},{question:"What is the impact of `foreign_key_checks = 0` during logical backup restoration?",shortAnswer:"It temporarily disables foreign key constraint validation, allowing child tables to be restored and populated before their parent tables without throwing foreign key violation errors.",explanation:"When restoring a logical dump, tables are imported alphabetically. If child `order_items` is imported before parent `orders`, foreign key checks would cause imports to fail. `mysqldump` automatically places `SET FOREIGN_KEY_CHECKS=0;` at the top of dump files.",hint:"Allows restoring child tables before parent tables during import.",level:"intermediate",codeExample:`-- mysqldump automatically includes at the top of dump files:
/*!40014 SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0 */;`},{question:"Why should `innodb_buffer_pool_dump_at_shutdown` and `innodb_buffer_pool_load_at_startup` be enabled alongside physical backup restores?",shortAnswer:"To preserve and quickly warm up the InnoDB Buffer Pool cache in memory following server restarts or disaster recovery restorations, preventing cold-cache query performance degradation.",explanation:"Warming the buffer pool pre-loads frequently accessed index and data pages into RAM, ensuring normal query response times immediately upon bringing restored databases back online.",hint:"Pre-loads cached pages into the buffer pool on startup for instant warm-cache performance.",level:"expert",codeExample:`SET PERSIST innodb_buffer_pool_dump_at_shutdown = ON;
SET PERSIST innodb_buffer_pool_load_at_startup = ON;`},{question:"What is the primary operational tradeoff between full backups and incremental backups?",shortAnswer:"Full backups take longer to run and consume more storage, but provide simple, fast, one-step restoration; incremental backups run quickly and use minimal storage, but require multi-step sequential chain restoration.",explanation:"If an intermediate incremental backup file is corrupted or missing, all subsequent incremental backups in that chain become unrecoverable.",hint:"Full = slower backup, simple fast restore; Incremental = fast small backup, complex multi-step restore.",level:"basic",codeExample:"-- Backup Time vs Restore Complexity Tradeoff"},{question:"How can you compress a `mysqldump` logical backup on-the-fly without creating a huge uncompressed intermediate file?",shortAnswer:"Pipe the `mysqldump` stdout directly into compression utilities like `gzip`, `zstd`, or `pigz` (`mysqldump ... | zstd -3 > backup.sql.zst`).",explanation:"Piping stdout directly to a streaming compressor eliminates the need to write hundreds of gigabytes of uncompressed text to disk first.",hint:"Pipe mysqldump stdout directly into gzip or zstd.",level:"basic",codeExample:"mysqldump --single-transaction --quick ecommerce | pigz -p 4 > ecommerce_backup.sql.gz"},{question:"What is the difference between Physical Hot Backup tools like Percona XtraBackup vs MySQL Enterprise Backup (MEB)?",shortAnswer:"Percona XtraBackup is open-source (GPL) and community-supported; MySQL Enterprise Backup (MEB) is a commercial proprietary tool provided with Oracle MySQL Enterprise Edition.",explanation:"Both tools perform non-blocking physical block-level hot backups using similar redo log streaming architectures.",hint:"Percona XtraBackup is open-source; MEB is Oracle's commercial enterprise backup tool.",level:"basic",codeExample:"-- Both tools achieve identical physical hot backup performance."},{question:"Why should `sql_log_bin = 0` be considered during large logical backup restoration onto a replication replica?",shortAnswer:"To prevent the massive flood of restore `INSERT` statements from being written to the replica's binary log and propagating redundant replication traffic.",explanation:"Executing `SET sql_log_bin = 0;` disables binlog generation for that specific restore session.",hint:"Disables binary log generation during restore to prevent duplicate replication traffic.",level:"expert",codeExample:`SET sql_log_bin = 0;
SOURCE /backups/ecommerce_restore.sql;
SET sql_log_bin = 1;`},{question:"What is the primary operational takeaway of Topic 1 in Module 004_004?",shortAnswer:"Select the optimal backup classification based on dataset size and SLA: use non-blocking logical hot backups (`mysqldump --single-transaction`) for small-to-medium datasets (<50GB) and cross-version migrations, and use physical hot backups (Percona XtraBackup) for large multi-terabyte production databases requiring low RTO.",explanation:"Understanding the dimensions of backup architecture—Logical vs Physical, Hot vs Cold, Full vs Incremental—allows database architects to design resilient, cost-effective backup strategies that meet enterprise RTO and RPO targets without disrupting production transactions.",hint:"Summarize dataset size matching, non-blocking MVCC snapshots, and physical backups for low RTO.",level:"basic",codeExample:`-- Decision Matrix:
-- < 50GB: mysqldump --single-transaction --quick --routines --triggers
-- > 100GB: Percona XtraBackup (Physical Hot Backup + LSN Incrementals)
-- Cross-Version / Cross-Cloud Migration: Logical SQL Dump / MySQL Shell Dump`}],h=`================================================================================
MYSQL 8.0 BACKUP, POINT-IN-TIME RECOVERY & DATA MIGRATION
TOPIC 1: Backup Classification – Logical vs Physical, Cold vs Warm vs Hot, Full vs Incremental
================================================================================

1. DIMENSION 1: LOGICAL VS PHYSICAL BACKUPS
--------------------------------------------------------------------------------
A. Logical Backups (mysqldump, mysqlpump, mydumper):
   - Definition: Extracts database schema and data as plain text SQL statements
     (\`CREATE TABLE\`, \`INSERT INTO\`) or CSV files.
   - Advantages:
     * Human-readable and editable (can fix a bad schema before restore).
     * Highly portable across MySQL versions (e.g. migrating 5.7 to 8.0), different OSs
       (Linux to Windows), and cloud database engines (RDS / Cloud SQL).
     * Granular restoration (can restore a single table or database easily).
   - Disadvantages:
     * Slow restore on large datasets (> 100GB): The database engine must parse SQL
       text, compile queries, and rebuild all secondary B-tree indexes from scratch.

B. Physical Backups (Percona XtraBackup, MySQL Enterprise Backup, Cold Copies):
   - Definition: Directly copies the raw binary database files (\`.ibd\`, data dictionary,
     undo tablespaces, and redo log files) at the filesystem block level.
   - Advantages:
     * Ultra-fast backup and restoration: Speed is limited only by physical disk I/O.
     * Zero SQL query parsing or index rebuilding overhead on restore.
     * Essential for multi-hundred gigabyte and multi-terabyte production databases.
   - Disadvantages:
     * Hardware & version bound: Must restore to identical MySQL minor versions and OS.
     * Binary format cannot be inspected or edited with a text editor.

2. DIMENSION 2: COLD VS WARM VS HOT BACKUPS
--------------------------------------------------------------------------------
A. Cold Backups (Offline):
   - Taken while the MySQL server daemon is completely shut down (\`systemctl stop mysqld\`).
   - Advantage: Guarantees 100% crash-consistent files without locking queries.
   - Disadvantage: Requires complete application downtime.

B. Warm Backups (Read-Only):
   - Taken while the server is running, but explicit read locks block all writes:
     \`FLUSH TABLES WITH READ LOCK;\`
   - Advantage: Client reads (\`SELECT\`) continue operating.
   - Disadvantage: All write transactions (\`INSERT\`, \`UPDATE\`, \`DELETE\`) stall in queues.

C. Hot Backups (Online Non-Blocking):
   - Taken while the database is fully online and active with concurrent reads AND writes.
   - Mechanism:
     * Logical: Uses InnoDB Multi-Version Concurrency Control (MVCC) snapshots via
       \`mysqldump --single-transaction\`.
     * Physical: Percona XtraBackup copies \`.ibd\` files while continuously streaming
       active InnoDB redo logs in the background, then applies the redo logs (\`--prepare\`)
       to achieve transaction consistency.

3. DIMENSION 3: FULL VS INCREMENTAL VS DIFFERENTIAL BACKUPS
--------------------------------------------------------------------------------
A. Full Backup:
   - Contains 100% of the database data at a specific snapshot point.
   - Serves as the mandatory anchor baseline for all incremental strategies.

B. Incremental Backup (LSN-Based or Binlog-Based):
   - Backs up only the data modified since the previous backup (Full or Incremental).
   - In XtraBackup, compares the Log Sequence Number (LSN) of each 16KB page, copying
     only pages whose LSN is greater than the previous backup checkpoint.
   - Restore sequence: Full Base + Incr 1 + Incr 2 + Incr 3...

C. Differential Backup:
   - Backs up all data modified since the last Full baseline backup.
   - Restore sequence: Full Base + Latest Differential.

4. SELECTION DECISION MATRIX
--------------------------------------------------------------------------------
Database Size       Target SLA (RTO)    Recommended Tool
--------------------------------------------------------------------------------
< 20 GB             < 30 minutes        mysqldump --single-transaction
20 GB - 100 GB      < 1 hour            mydumper / myloader (Parallel logical)
> 100 GB - Multi-TB < 15 minutes        Percona XtraBackup (Physical Hot Backup)
Cross-Cloud Move    Portability         Logical SQL Dump / CSV Outfile

5. PRODUCTION BEST PRACTICES
--------------------------------------------------------------------------------
1. Never use Cold backups on production customer-facing databases.
2. For databases under 50GB, use \`mysqldump --single-transaction --quick\`.
3. For databases over 100GB, mandate physical hot backups with Percona XtraBackup.
4. Always verify that backup files include all required triggers, routines, and events.
================================================================================
`,f=()=>{const[r,o]=c.useState("phase1_logical_vs_physical"),s={phase1_logical_vs_physical:{phaseNumber:"Phase 1: Logical vs Physical",title:"1. Logical SQL Dumps vs Physical Block Backups",badge:"Architecture Comparison",badgeColor:"emerald",sqlSnippet:`-- 📦 1. LOGICAL HOT BACKUP (mysqldump / MySQL Shell Dump):
-- Generates human-readable SQL DDL + INSERT statements:
mysqldump --single-transaction --quick --routines --triggers --events \\
  -u root -p kolkata_retail > /backups/retail_logical.sql

-- 📦 2. PHYSICAL HOT BACKUP (Percona XtraBackup):
-- Directly copies raw binary .ibd files & streams redo logs:
xtrabackup --backup --target-dir=/backups/base_physical/ \\
  --parallel=8 --compress=zstd`,explanation:"Logical backups export SQL statements, making them portable across major MySQL versions, operating systems, and cloud providers. Physical backups copy raw data pages directly at the disk block level, enabling sub-15 minute restore times on multi-terabyte production databases.",keyTakeaways:["Logical: Human-readable SQL, portable across versions, ideal for datasets < 50GB.","Physical: Raw binary data pages, ultra-fast restore, essential for datasets > 100GB.","Physical restores bypass expensive SQL query parsing and B-tree index rebuilds."]},phase2_cold_warm_hot:{phaseNumber:"Phase 2: Cold vs Warm vs Hot",title:"2. Offline Cold vs Read-Only Warm vs Online Hot Backups",badge:"Locking & Concurrency",badgeColor:"cyan",sqlSnippet:`-- ❄️ 1. COLD BACKUP (Offline - Server Stopped):
-- systemctl stop mysqld; tar -czf /backups/cold_mysql.tar.gz /var/lib/mysql

-- ⛅ 2. WARM BACKUP (Read-Only - Writes Blocked):
-- FLUSH TABLES WITH READ LOCK; (Stalls all INSERT/UPDATE/DELETE queries!)

-- 🔥 3. HOT BACKUP (Online Non-Blocking - 24/7 Production):
-- InnoDB MVCC Consistent Snapshot: Zero write locking!
mysqldump --single-transaction kolkata_retail > hot_backup.sql`,explanation:"Cold backups require full application downtime. Warm backups block all data mutations with table locks. Hot backups use InnoDB MVCC snapshots (--single-transaction) or continuous redo log streaming to take 100% consistent backups while concurrent writes continue without interruption.",keyTakeaways:["Cold requires full service shutdown; Warm locks all write transactions.","Hot backups use MVCC snapshots to ensure zero write contention.","Always use --single-transaction on production InnoDB workloads."]},phase3_full_vs_incremental:{phaseNumber:"Phase 3: Full vs Incremental LSN",title:"3. Full Baseline vs Page LSN-Based Incremental Chains",badge:"LSN Delta Tracking",badgeColor:"purple",sqlSnippet:`-- 🔄 INCREMENTAL PHYSICAL BACKUP & RESTORE SEQUENCE:

-- Day 0 (Sunday): Full Base Physical Backup:
xtrabackup --backup --target-dir=/backups/base

-- Day 1 (Monday): Incremental Backup (Copies only pages modified since Day 0):
xtrabackup --backup --target-dir=/backups/inc1 --incremental-basedir=/backups/base

-- 🛠️ PREPARATION & RESTORATION WORKFLOW:
-- Step 1: Apply redo log to base:
xtrabackup --prepare --apply-log-only --target-dir=/backups/base
-- Step 2: Roll forward Monday incremental into base:
xtrabackup --prepare --target-dir=/backups/base --incremental-dir=/backups/inc1`,explanation:"Incremental backups inspect the Log Sequence Number (LSN) in each 16KB data page header, copying only pages changed since the previous backup. Restoring requires applying incremental delta directories sequentially to the base backup before copying back to disk.",keyTakeaways:["Incremental backups save storage space and backup execution time.","XtraBackup tracks modified pages via header Log Sequence Numbers (LSN).","Preparation rolls forward all incremental deltas into the base directory."]},phase4_decision_matrix:{phaseNumber:"Phase 4: Tool Decision Matrix",title:"4. Production Tool Selection Matrix by Dataset Scale",badge:"Architecture Sizing",badgeColor:"rose",sqlSnippet:`-- 📊 PRODUCTION BACKUP SIZING & TOOL RECOMMENDATION:

-- Dataset < 20 GB:
-- Tool: mysqldump --single-transaction --quick
-- Expected Restore Time: < 15 minutes

-- Dataset 20 GB - 100 GB:
-- Tool: mydumper / myloader (Parallel Multi-threaded Logical Dump)
-- Expected Restore Time: < 30 minutes

-- Dataset > 100 GB - Multi-TB:
-- Tool: Percona XtraBackup (Physical Hot Backup + LSN Incrementals)
-- Expected Restore Time: < 15 minutes (Direct block file copy)`,explanation:"Choosing the right backup strategy depends on database size and RTO requirements. Small databases benefit from the simplicity of mysqldump, medium databases use parallel mydumper, and large enterprise databases mandate physical hot backups with Percona XtraBackup.",keyTakeaways:["< 20GB: mysqldump provides simplicity and universal portability.","20GB - 100GB: mydumper parallelizes dumps across multiple CPU threads.","> 100GB: Percona XtraBackup guarantees enterprise sub-15 minute RTO."]}},a=s[r];return e.jsxs("div",{className:"bg-slate-950 text-slate-100 min-h-screen py-10 px-4 sm:px-6 lg:px-8 font-sans",children:[e.jsxs("header",{className:"max-w-6xl mx-auto mb-10 text-center sm:text-left border-b border-slate-800 pb-8",children:[e.jsxs("div",{className:"flex flex-wrap items-center gap-3 mb-3",children:[e.jsx("span",{className:"px-3 py-1 bg-emerald-950/80 text-emerald-400 border border-emerald-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Module 004.4: Backup, PITR & Data Migration"}),e.jsx("span",{className:"px-3 py-1 bg-cyan-950/80 text-cyan-400 border border-cyan-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Topic 1 of 13"})]}),e.jsxs("h1",{className:"text-3xl sm:text-5xl font-extrabold text-white tracking-tight leading-tight",children:["Backup Classification: ",e.jsx("span",{className:"text-emerald-400",children:"Logical vs Physical"})," & ",e.jsx("span",{className:"text-cyan-400",children:"Hot vs Cold"})]}),e.jsx("p",{className:"mt-4 text-base sm:text-lg text-slate-400 max-w-4xl leading-relaxed",children:"Comprehensive breakdown of database backup methodologies in MySQL 8.0: contrasting logical SQL text dumps against raw binary physical block copies, evaluating non-blocking MVCC hot snapshots against offline cold backups, and mastering incremental LSN restore chains."})]}),e.jsxs("main",{className:"max-w-6xl mx-auto space-y-16",children:[e.jsxs("section",{id:"classification-dimensions",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"1. The Three Dimensions of Backup Classification"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"How database backups are categorized by format, execution state, and data scope."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-3 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-3",children:[e.jsx("span",{className:"text-xs font-mono text-emerald-400 font-bold uppercase",children:"Dimension 1"}),e.jsx("h3",{className:"text-lg font-bold text-white",children:"Logical vs Physical"}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:[e.jsx("strong",{children:"Logical:"})," SQL text statements (portable, human-readable).",e.jsx("br",{}),e.jsx("strong",{children:"Physical:"})," Raw binary block files (ultra-fast line-rate restore)."]}),e.jsx("div",{className:"pt-2 text-xs font-mono text-emerald-400",children:"Rule: Use physical for datasets > 100GB."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-3",children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-bold uppercase",children:"Dimension 2"}),e.jsx("h3",{className:"text-lg font-bold text-white",children:"Cold vs Warm vs Hot"}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:[e.jsx("strong",{children:"Cold:"})," Offline daemon shutdown.",e.jsx("br",{}),e.jsx("strong",{children:"Warm:"})," Read-only table locks.",e.jsx("br",{}),e.jsx("strong",{children:"Hot:"})," Non-blocking online MVCC snapshot."]}),e.jsx("div",{className:"pt-2 text-xs font-mono text-cyan-400",children:"Rule: Never use cold backups in 24/7 production."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/90 border border-purple-700/60 bg-purple-950/20 space-y-3 shadow-lg shadow-purple-950/40",children:[e.jsx("span",{className:"text-xs font-mono text-purple-400 font-bold uppercase",children:"Dimension 3"}),e.jsx("h3",{className:"text-lg font-bold text-purple-300",children:"Full vs Incremental"}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-200 leading-relaxed",children:[e.jsx("strong",{children:"Full:"})," Complete 100% baseline copy.",e.jsx("br",{}),e.jsx("strong",{children:"Incremental:"})," LSN-tracked page delta since last backup."]}),e.jsx("div",{className:"pt-2 text-xs font-mono text-purple-300",children:"Rule: Prepare incremental chains chronologically."})]})]})]}),e.jsxs("section",{id:"interactive-workbench",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"2. Interactive Backup Classification Workbench"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Inspect command structures, locking behaviors, incremental preparation workflows, and dataset sizing guidelines."})]}),e.jsx("div",{className:"flex flex-wrap gap-3",children:Object.keys(s).map(t=>{const n=s[t],l=r===t;return e.jsx("button",{onClick:()=>o(t),className:i("px-4 py-2.5 rounded-xl font-medium text-xs sm:text-sm transition-all duration-200 border",l?"bg-emerald-500/10 border-emerald-500 text-emerald-400 shadow-lg shadow-emerald-950/40":"bg-slate-900/60 border-slate-800 text-slate-400 hover:text-slate-200 hover:bg-slate-900"),children:n.phaseNumber},t)})}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-6",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-3 border-b border-slate-800 pb-4",children:[e.jsxs("div",{children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-semibold uppercase tracking-wider",children:a.phaseNumber}),e.jsx("h3",{className:"text-lg sm:text-xl font-bold text-white mt-1",children:a.title})]}),e.jsx("span",{className:i("px-3 py-1 rounded-full text-xs font-mono font-semibold border w-fit",a.badgeColor==="emerald"&&"bg-emerald-950/80 text-emerald-300 border-emerald-700",a.badgeColor==="cyan"&&"bg-cyan-950/80 text-cyan-300 border-cyan-700",a.badgeColor==="purple"&&"bg-purple-950/80 text-purple-300 border-purple-700",a.badgeColor==="rose"&&"bg-rose-950/80 text-rose-300 border-rose-700"),children:a.badge})]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:a.explanation}),e.jsxs("div",{className:"space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"CLI Command & Workflow Implementation:"}),e.jsx("pre",{className:"p-4 rounded-xl bg-slate-950 border border-slate-800 font-mono text-xs text-emerald-300 overflow-x-auto leading-relaxed",children:a.sqlSnippet})]}),e.jsxs("div",{className:"space-y-2 pt-2 border-t border-slate-800/80",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"Architectural Takeaways:"}),e.jsx("ul",{className:"grid grid-cols-1 md:grid-cols-3 gap-3 text-xs text-slate-300",children:a.keyTakeaways.map((t,n)=>e.jsxs("li",{className:"p-3 rounded-lg bg-slate-950/70 border border-slate-800/60 flex items-start gap-2",children:[e.jsx("span",{className:"text-emerald-400 font-bold",children:"✓"}),e.jsx("span",{children:t})]},n))})]})]})]}),e.jsxs("section",{id:"case-studies",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-purple-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"3. Real-World Engineering Scenarios in Bengal"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Classification case studies in Barrackpore and Kolkata demonstrating small-scale logical dumps and multi-terabyte physical hot backups."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-emerald-400"}),"Case 1: Mamata & Susmita – 12GB Retail Store Backup in Barrackpore"]}),e.jsx("span",{className:"text-xs font-mono text-emerald-400 bg-emerald-950/80 px-2.5 py-1 rounded-full border border-emerald-800",children:"Logical Hot Dump"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Barrackpore, a 12GB retail store database required nightly automated backups. Mamata configured ",e.jsx("code",{children:"mysqldump --single-transaction --quick --routines --triggers"})," piped into ",e.jsx("code",{children:"gzip"}),". The backup finished in 2 minutes 40 seconds without blocking a single billing terminal, producing a portable SQL file that allowed easy single-table extraction."]})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-cyan-400"}),"Case 2: Abhronila & Debangshu – 5TB Fintech Cluster in Kolkata"]}),e.jsx("span",{className:"text-xs font-mono text-cyan-400 bg-cyan-950/80 px-2.5 py-1 rounded-full border border-cyan-800",children:"Physical XtraBackup"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Kolkata, managing a 5TB core banking cluster processing ₹500 Crores in volume required non-blocking hot backups. When tested, ",e.jsx("code",{children:"mysqldump"})," caused massive buffer pool churn and took 11 hours. Debangshu deployed Percona XtraBackup with parallel compression, reducing backup time to 28 minutes and restore time to 14 minutes, fully meeting regulatory compliance."]})]})]})]}),e.jsxs("section",{id:"pitfalls-rules",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-rose-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"4. Senior Pitfalls & Production Best Practices"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Avoid dangerous table locking and uncoordinated backup preparation mistakes."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 1: Omitting --single-transaction on Production"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Running ",e.jsx("code",{children:"mysqldump"})," without ",e.jsx("code",{children:"--single-transaction"})," acquires global read locks, blocking all application write traffic for the duration of the dump."]}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Always use --single-transaction for online InnoDB hot backups."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 2: Forgetting to Prepare Incremental Backups"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Copying raw XtraBackup incremental files directly to the data directory without running ",e.jsx("code",{children:"--prepare"})," leads to immediate database crash and corrupted tablespaces."]}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Always prepare base + incremental chains with --apply-log-only."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 1: Pipe mysqldump into Streaming Compression"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Stream ",e.jsx("code",{children:"mysqldump"})," output directly into ",e.jsx("code",{children:"zstd"})," or ",e.jsx("code",{children:"pigz"})," to save 70% disk space without generating giant intermediate uncompressed files."]}),e.jsx("div",{className:"text-xs text-slate-400",children:"Reduces disk I/O bottlenecks and optimizes backup network transfer."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 2: Match Backup Tool to Dataset Size"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Follow the 50GB rule: use ",e.jsx("code",{children:"mysqldump"})," for small datasets (< 50GB) and Percona XtraBackup for enterprise datasets (> 100GB)."]}),e.jsx("div",{className:"text-xs text-slate-400",children:"Guarantees predictable restore times and meets enterprise RTO targets."})]})]})]}),e.jsxs("section",{id:"printable-note",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"5. Printable Study Note & Teacher Advice"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Download complete printable notes and review key takeaways from Sir Sukanta Hui."})]}),e.jsx(u,{title:"Topic 1: Backup Classification: Logical vs Physical, Cold vs Hot vs Warm, Full vs Incremental",content:h}),e.jsx(d,{note:"Understand the trade-offs of each backup classification: Logical dumps (mysqldump) are portable and human-readable, making them perfect for small databases (< 50GB) and cross-version migrations. Physical backups (Percona XtraBackup) are binary-bound, but restore in minutes on multi-terabyte databases. Never lock your production database with warm or cold backups—always leverage InnoDB MVCC snapshots and background redo log streaming!"})]}),e.jsxs("section",{id:"faq-section",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"6. Frequently Asked Questions (30 Deep-Dive Questions)"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Master the technical nuances of logical vs physical backups, non-blocking MVCC snapshots, and incremental LSN restoration."})]}),e.jsx(p,{title:"Backup Classification & Strategy FAQs",questions:m})]})]})]})};export{f as default};
