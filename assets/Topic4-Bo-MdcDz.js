import{b as d,j as e,bg as r}from"./vendor-react-core-Doz9nIC6.js";import{T as c}from"./TeacherSukantaHui-CEPuAfsb.js";import{F as p}from"./FAQTemplate-BHhlgA96.js";import{P as I}from"./PlainTextPrint-C08xhKA4.js";import"./vendor-icons-DGCamHnX.js";const m=[{question:"What is LIST Partitioning in MySQL and how does it determine row placement?",shortAnswer:"LIST Partitioning assigns rows to physical partitions based on whether a column's value matches one of a **discrete set of explicit values** defined in a `VALUES IN (value_1, value_2, ...)` list.",explanation:"Unlike RANGE which matches intervals, LIST matches explicit enumerated categorical values.",hint:"Assigns rows based on explicit enumerated values defined in VALUES IN lists.",level:"basic",codeExample:`CREATE TABLE client_branches (
  branch_id INT NOT NULL,
  region_id INT NOT NULL,
  branch_name VARCHAR(50),
  PRIMARY KEY (branch_id, region_id)
) ENGINE = InnoDB
PARTITION BY LIST (region_id) (
  PARTITION p_bengal VALUES IN (10, 11, 12),
  PARTITION p_delhi  VALUES IN (20, 21, 22),
  PARTITION p_mumbai VALUES IN (30, 31, 32)
);`},{question:"What is the difference between `PARTITION BY LIST (expr)` and `PARTITION BY LIST COLUMNS (col1, ...)`?",shortAnswer:"`LIST (expr)` requires the expression to **return an integer value**; `LIST COLUMNS` allows direct partitioning on **`VARCHAR`**, **`CHAR`**, **`DATE`**, or **`DATETIME`** columns directly without needing integer foreign key mappings.",explanation:"`LIST COLUMNS` is the modern standard for categorical string partitioning in MySQL 8.0.",hint:"LIST requires integer expressions; LIST COLUMNS allows direct string (VARCHAR) partitioning.",level:"basic",codeExample:`-- Direct string category partitioning:
PARTITION BY LIST COLUMNS (city_name) (
  PARTITION p_bengal VALUES IN ('Kolkata', 'Barrackpore', 'Howrah'),
  PARTITION p_delhi  VALUES IN ('New Delhi', 'Noida', 'Gurgaon')
);`},{question:"Can the same value appear in more than one partition's `VALUES IN` list?",shortAnswer:"**No**, all values across all partition lists must be **strictly disjoint (mutually exclusive)**; if a value is duplicated across two partitions, MySQL rejects table creation with `ERROR 1495 (HY000): Multiple definition of same constant in list partitioning`.",explanation:"Guarantees unambiguous row routing to exactly one physical partition.",hint:"Values must be strictly unique across all partition definitions (no duplicates).",level:"basic",codeExample:"-- Error 1495 if 'Kolkata' is listed in both p_bengal and p_east."},{question:"What happens when an `INSERT` provides a row with a value that does NOT match any defined `VALUES IN` list?",shortAnswer:"The insert operation **fails immediately with `ERROR 1526 (HY000): Table has no partition for value ...`**, and the transaction is aborted.",explanation:"Incoming categorical values must map to an existing defined partition list.",hint:"Fails with Error 1526 because the unmapped value has no destination partition.",level:"basic",codeExample:"-- Inserting city 'Bangalore' into a table with only Bengal/Delhi/Mumbai fails with Error 1526!"},{question:"In Mamata & Susmita's Barrackpore retail store, regional branch sales were tracked across ₹1.2 Crores in inventory. How did Susmita use `LIST COLUMNS` to isolate Barrackpore and Kolkata store records into separate physical NVMe files?",shortAnswer:"She defined `PARTITION BY LIST COLUMNS (store_location)` with `PARTITION p_barrackpore VALUES IN ('Barrackpore', 'N.C.Pukur')` and `PARTITION p_kolkata VALUES IN ('Kolkata', 'Salt Lake')`; when local Barrackpore store queries ran, MySQL pruned searches to the single Barrackpore file, speeding up POS lookups by 8x.",explanation:"Localized storage partitions improved buffer pool hit ratios and query speed.",hint:"Partitioned by store_location using LIST COLUMNS for localized partition pruning.",level:"moderate",codeExample:`# Barrackpore Store Partitioning:
CREATE TABLE store_invoices (
  invoice_id BIGINT NOT NULL,
  store_location VARCHAR(30) NOT NULL,
  amount DECIMAL(10,2),
  PRIMARY KEY (invoice_id, store_location)
) PARTITION BY LIST COLUMNS (store_location) (
  PARTITION p_barrackpore VALUES IN ('Barrackpore', 'N.C.Pukur'),
  PARTITION p_kolkata     VALUES IN ('Kolkata', 'Salt Lake')
);`},{question:"In Abhronila & Debangshu's Kolkata fintech bank, corporate client ledgers supported multi-tenant banking across ₹500 Crores in volume. How did Debangshu use `LIST (tenant_id)` partitioning to provide physical data isolation between enterprise clients?",shortAnswer:"He assigned tier-1 enterprise clients their own dedicated high-volume physical partitions (e.g. `PARTITION p_client_reliance VALUES IN (1001)`), while grouping small-business tenants into a shared partition (`PARTITION p_smb VALUES IN (2001, 2002, 2003)`), ensuring VIP client queries never contended for disk pages with other tenants.",explanation:"Multi-tenant physical tiering delivered predictable latency SLAs for enterprise clients.",hint:"Assigned high-volume tenants to dedicated partitions and pooled smaller tenants together.",level:"expert",codeExample:`PARTITION BY LIST (tenant_id) (
  PARTITION p_enterprise_1001 VALUES IN (1001),
  PARTITION p_enterprise_1002 VALUES IN (1002),
  PARTITION p_smb_pool        VALUES IN (2001, 2002, 2003, 2004)
);`},{question:"How are `NULL` values handled in LIST partitioned tables?",shortAnswer:"A row with a `NULL` partition key is **rejected with `ERROR 1526` unless a partition explicitly includes `NULL` in its list** (e.g. `PARTITION p_null VALUES IN (NULL)` or `VALUES IN (10, 11, NULL)`).",explanation:"Unlike RANGE (which automatically stores NULL in the lowest partition), LIST requires explicit declaration.",hint:"Rejected unless a partition explicitly declares VALUES IN (NULL).",level:"intermediate",codeExample:`PARTITION BY LIST (status_code) (
  PARTITION p_active   VALUES IN (1, 2),
  PARTITION p_inactive VALUES IN (0),
  PARTITION p_unknown  VALUES IN (NULL)
);`},{question:"How do you add a new categorical value (e.g. adding 'Hyderabad') to a LIST partitioned table online?",shortAnswer:"Either: 1. Add a new partition using `ALTER TABLE table_name ADD PARTITION (PARTITION p_south VALUES IN ('Hyderabad', 'Chennai'));`, or 2. Reorganize an existing partition to include the new city: `ALTER TABLE table_name REORGANIZE PARTITION p_south INTO (PARTITION p_south VALUES IN ('Bangalore', 'Hyderabad'));`.",explanation:"Expands partition definitions online without locking other partitions.",hint:"Use ALTER TABLE ADD PARTITION or REORGANIZE PARTITION to include new category values.",level:"intermediate",codeExample:`ALTER TABLE store_invoices ADD PARTITION (
  PARTITION p_south VALUES IN ('Hyderabad', 'Chennai', 'Bangalore')
);`},{question:"How does Partition Pruning operate when a query filters on `WHERE region_name = 'Kolkata'` on a LIST partitioned table?",shortAnswer:"The optimizer maps `'Kolkata'` to its enclosing partition (`p_bengal`), prunes all other regional partitions, and opens **only `regional_accounts#p#p_bengal.ibd`** on disk.",explanation:"Eliminates all cross-region partition scans instantly.",hint:"Prunes all other partitions and opens only the specific matching region partition file.",level:"basic",codeExample:`EXPLAIN SELECT * FROM regional_accounts WHERE region_name = 'Kolkata';
-- partitions: p_bengal`},{question:"What happens if a query uses an `IN (...)` list containing values belonging to multiple LIST partitions (e.g. `WHERE region_name IN ('Kolkata', 'New Delhi')`)?",shortAnswer:"The optimizer identifies the matching partitions (`p_bengal` and `p_delhi`) and prunes all others, scanning **only those two specific physical partitions** and skipping `p_mumbai` and `p_south`.",explanation:"Prunes multi-value queries to the minimal set of candidate partitions.",hint:"Prunes to scan only the union of partitions containing the queried values.",level:"basic",codeExample:`EXPLAIN SELECT * FROM regional_accounts WHERE region_name IN ('Kolkata', 'New Delhi');
-- partitions: p_bengal, p_delhi`},{question:"Can an `ENUM` column be used in `PARTITION BY LIST COLUMNS`?",shortAnswer:"**Yes**, an `ENUM` column can be used directly in `LIST COLUMNS (enum_col)`, matching its string representations (e.g. `VALUES IN ('ACTIVE', 'PENDING')`).",explanation:"Allows intuitive status and workflow partitioning.",hint:"Yes, ENUM columns can be used directly in LIST COLUMNS.",level:"intermediate",codeExample:`CREATE TABLE orders (
  order_id INT NOT NULL,
  status ENUM('PENDING', 'PROCESSING', 'COMPLETED', 'CANCELLED') NOT NULL,
  PRIMARY KEY (order_id, status)
) PARTITION BY LIST COLUMNS (status) (
  PARTITION p_open   VALUES IN ('PENDING', 'PROCESSING'),
  PARTITION p_closed VALUES IN ('COMPLETED', 'CANCELLED')
);`},{question:"What happens when an `UPDATE` changes a row's category from `'PENDING'` to `'COMPLETED'` on a LIST partitioned table?",shortAnswer:"InnoDB performs an **atomic cross-partition row transfer**: deletes the row from `p_open.ibd` and inserts it into `p_closed.ibd` within the same transaction.",explanation:"Row movement is completely transparent to client applications.",hint:"Atomically deletes row from p_open and inserts into p_closed within the transaction.",level:"intermediate",codeExample:"UPDATE orders SET status = 'COMPLETED' WHERE order_id = 101;"},{question:"Why should highly volatile workflow status columns (e.g. transitioning every 5 seconds) be partitioned with caution?",shortAnswer:"Because frequent status updates trigger constant cross-partition row deletions and insertions, increasing undo log generation, row lock duration, and B-Tree fragmentation across multiple `.ibd` files.",explanation:"Partitioning is best suited for stable categories or final state archiving.",hint:"Frequent status updates trigger heavy cross-partition delete/insert overhead.",level:"expert",codeExample:"-- Caution: Rapid status changes cause heavy cross-partition movement overhead."},{question:"What is `ALTER TABLE ... DROP PARTITION`'s effect on a LIST partitioned table?",shortAnswer:"It deletes the specified partition `.ibd` file and **all rows belonging to its categorical values permanently in milliseconds**, and removes those values from the table schema (subsequent inserts with those values will now fail with Error 1526).",explanation:"Instantly purges an entire geographic or tenant partition.",hint:"Deletes partition file and data permanently; values are removed from table schema.",level:"basic",codeExample:"ALTER TABLE regional_accounts DROP PARTITION p_mumbai;"},{question:"How do you split a single consolidated partition (e.g. `p_north` holding Delhi, Noida, and Punjab) into two distinct partitions?",shortAnswer:"Use `ALTER TABLE table_name REORGANIZE PARTITION p_north INTO (PARTITION p_delhi_ncr VALUES IN ('New Delhi', 'Noida'), PARTITION p_punjab VALUES IN ('Punjab', 'Chandigarh'));`.",explanation:"Reorganizes categorical values into separate physical files with zero data loss.",hint:"Use REORGANIZE PARTITION to redistribute values into smaller partitions without data loss.",level:"intermediate",codeExample:`ALTER TABLE regional_accounts REORGANIZE PARTITION p_north INTO (
  PARTITION p_delhi_ncr VALUES IN ('New Delhi', 'Noida'),
  PARTITION p_punjab    VALUES IN ('Punjab', 'Chandigarh')
);`},{question:"How does `WHERE region_name <> 'Kolkata'` prune on a LIST partitioned table with 10 regions across 5 partitions?",shortAnswer:"If `p_bengal` contains ONLY `'Kolkata'`, the optimizer prunes `p_bengal` and scans the remaining 4 partitions; if `p_bengal` contains `'Kolkata'` and `'Barrackpore'`, `p_bengal` must still be scanned for Barrackpore rows.",explanation:"Demonstrates inequality pruning behavior on discrete categorical lists.",hint:"Prunes partition only if all values in that partition are excluded by the inequality.",level:"expert",codeExample:"-- Inequality pruning requires partition values to be completely excluded."},{question:"What is Multi-Column LIST Partitioning (`PARTITION BY LIST COLUMNS (col1, col2)`) in MySQL 8.0?",shortAnswer:"Allows defining partition lists based on **tuples of values** across multiple columns (e.g. `VALUES IN (('IN', 'WB'), ('IN', 'DL'))`), enabling composite categorical routing.",explanation:"Enables multi-dimensional categorical partitioning across country and state codes.",hint:"Defines partition lists based on tuples of values across multiple columns.",level:"expert",codeExample:`PARTITION BY LIST COLUMNS (country_code, state_code) (
  PARTITION p_india_bengal VALUES IN (('IN', 'WB')),
  PARTITION p_india_delhi  VALUES IN (('IN', 'DL'))
);`},{question:"Can an `ALTER TABLE` statement convert a non-partitioned table into a LIST partitioned table?",shortAnswer:"**Yes**, provided all existing rows in the table match at least one of the defined `VALUES IN` lists (and the primary key contains the partition column); MySQL redistributes all rows into physical `.ibd` partition files.",explanation:"Converts legacy tables to partitioned storage online.",hint:"Yes, provided all existing rows match defined VALUES IN lists and PK includes partition column.",level:"intermediate",codeExample:`ALTER TABLE customers PARTITION BY LIST COLUMNS (region) (
  PARTITION p_bengal VALUES IN ('Kolkata', 'Barrackpore'),
  PARTITION p_others VALUES IN ('Delhi', 'Mumbai')
);`},{question:"How do you inspect which partition holds a specific category value in `information_schema.PARTITIONS`?",shortAnswer:"`SELECT PARTITION_NAME, PARTITION_DESCRIPTION, TABLE_ROWS FROM information_schema.PARTITIONS WHERE TABLE_NAME = 'regional_accounts';` (where `PARTITION_DESCRIPTION` lists the `VALUES IN` items).",explanation:"Displays partition mapping rules programmatically.",hint:"Query PARTITION_DESCRIPTION in information_schema.PARTITIONS.",level:"basic",codeExample:`SELECT PARTITION_NAME, PARTITION_DESCRIPTION 
FROM information_schema.PARTITIONS 
WHERE TABLE_NAME = 'regional_accounts';`},{question:"What is the primary operational advantage of LIST partitioning for regulatory data residency compliance (e.g. GDPR / RBI data localization)?",shortAnswer:"By defining `DATA DIRECTORY` on specific LIST partitions (e.g. storing `p_india` on local domestic NVMe disks and `p_eu` on Frankfurt storage), enterprise databases can physically isolate data by legal jurisdiction within a single logical database schema.",explanation:"Enforces physical storage compliance per geographic territory.",hint:"Allows placing individual regional partitions on distinct physical storage tiers via DATA DIRECTORY.",level:"expert",codeExample:`PARTITION p_india VALUES IN ('IN') DATA DIRECTORY = '/mnt/india_nvme',
PARTITION p_eu    VALUES IN ('EU') DATA DIRECTORY = '/mnt/eu_nvme'`},{question:"What is `ALTER TABLE ... TRUNCATE PARTITION p_bengal` used for on a LIST partitioned table?",shortAnswer:"It deletes all rows belonging to the Bengal partition instantly while **retaining the partition definition and its `VALUES IN` category mappings** in the table schema.",explanation:"Empties a category partition for fresh data loading without altering DDL definitions.",hint:"Empties partition data but keeps category mappings intact.",level:"basic",codeExample:"ALTER TABLE regional_accounts TRUNCATE PARTITION p_bengal;"},{question:"What happens if you attempt to create a LIST partitioned table without specifying any `VALUES IN` clauses?",shortAnswer:"MySQL rejects the DDL with a syntax error; every partition in a LIST partitioned table must contain an explicit `VALUES IN (...)` clause with at least one value.",explanation:"Mandatory syntax requirement for LIST partitioning.",hint:"Syntax error; every LIST partition must define an explicit VALUES IN list.",level:"basic",codeExample:"-- Every partition requires VALUES IN (...)."},{question:"How does `EXCHANGE PARTITION` work with LIST partitioned tables?",shortAnswer:"It swaps the physical `.ibd` file of a specific category partition (e.g. `p_bengal`) with a standalone non-partitioned staging table in **under 10 milliseconds**, provided all rows in the staging table match the `VALUES IN` list for `p_bengal`.",explanation:"Accelerates regional batch ingestion and staging table swaps.",hint:"Swaps tablespace file pointers with a standalone table matching the partition's category values.",level:"intermediate",codeExample:"ALTER TABLE regional_accounts EXCHANGE PARTITION p_bengal WITH TABLE bengal_staging;"},{question:"What happens if a standalone table passed to `EXCHANGE PARTITION` contains a row with `region_name = 'Mumbai'` when swapping with `p_bengal`?",shortAnswer:"MySQL rejects the exchange with `ERROR 1736 (HY000): Found row that does not match the partition rule`, preventing invalid data from entering the partition.",explanation:"Enforces partition rule validation before completing the tablespace swap.",hint:"Rejects exchange with Error 1736 if any row violates the partition's VALUES IN rule.",level:"expert",codeExample:"-- Error 1736: Staging rows must strictly match p_bengal values."},{question:"Can LIST partitioning be combined with HASH or KEY subpartitioning (Composite Partitioning)?",shortAnswer:"**Yes**, you can partition by `LIST` on regional categories (e.g. Bengal, Delhi) and subpartition each region by `HASH` or `KEY` on `customer_id` into 4 buckets, achieving two-tier physical distribution.",explanation:"Combines categorical routing with hash load balancing.",hint:"Yes, LIST-HASH and LIST-KEY composite partitioning is fully supported.",level:"intermediate",codeExample:`PARTITION BY LIST COLUMNS (region)
SUBPARTITION BY KEY (customer_id)
SUBPARTITIONS 4 (
  PARTITION p_bengal VALUES IN ('Kolkata', 'Barrackpore'),
  PARTITION p_delhi  VALUES IN ('Delhi', 'Noida')
);`},{question:"What is the maximum number of values that can be defined inside a single `VALUES IN (...)` list?",shortAnswer:"MySQL supports thousands of values in a `VALUES IN` list, limited only by maximum query packet and metadata buffer sizes; in production, lists typically range from 1 to 50 values per partition.",explanation:"Allows grouping dozens of cities or tenant IDs into a single regional partition.",hint:"Supports thousands of values; practically sized to 1-50 values per partition.",level:"basic",codeExample:"PARTITION p_metro VALUES IN ('Kolkata', 'Mumbai', 'Delhi', 'Chennai', 'Bengaluru', 'Hyderabad')"},{question:"How do you merge two separate LIST partitions (e.g. `p_howrah` and `p_kolkata`) into a single combined partition (`p_bengal`)?",shortAnswer:"Execute `ALTER TABLE table_name REORGANIZE PARTITION p_howrah, p_kolkata INTO (PARTITION p_bengal VALUES IN ('Kolkata', 'Howrah', 'Barrackpore'));`.",explanation:"Merges categorical partitions online with zero data loss.",hint:"Use REORGANIZE PARTITION to combine discrete partitions into a consolidated partition.",level:"intermediate",codeExample:`ALTER TABLE store_invoices REORGANIZE PARTITION p_howrah, p_kolkata INTO (
  PARTITION p_bengal VALUES IN ('Kolkata', 'Howrah', 'Barrackpore')
);`},{question:"Why should `LIST COLUMNS` be preferred over `LIST (region_id)` when building multi-tenant SaaS databases?",shortAnswer:"Because `LIST COLUMNS (tenant_slug)` allows storing human-readable tenant identifiers (e.g. `'tata'`, `'reliance'`) directly in DDL without maintaining internal surrogate integer ID mapping tables, simplifying application queries and database maintenance.",explanation:"Eliminates integer lookup joins and makes partition metadata self-documenting.",hint:"Allows using human-readable tenant strings directly without integer translation tables.",level:"basic",codeExample:`PARTITION BY LIST COLUMNS (tenant_slug) (
  PARTITION p_tata     VALUES IN ('tata_motors', 'tcs'),
  PARTITION p_reliance VALUES IN ('jio', 'retail')
);`},{question:"What is the recommended health check query to detect data skew across LIST partitioned tables?",shortAnswer:"`SELECT PARTITION_NAME, TABLE_ROWS, ROUND(DATA_LENGTH/1024/1024, 2) AS MB FROM information_schema.PARTITIONS WHERE TABLE_NAME = 'regional_accounts';`.",explanation:"Identifies whether one regional partition is growing disproportionately larger than others.",hint:"Query TABLE_ROWS and DATA_LENGTH across partitions in information_schema.PARTITIONS.",level:"basic",codeExample:`SELECT PARTITION_NAME, TABLE_ROWS, ROUND(DATA_LENGTH/1024/1024, 2) AS MB 
FROM information_schema.PARTITIONS 
WHERE TABLE_NAME = 'regional_accounts';`},{question:"What is the primary operational takeaway of Topic 4 in Module 004_007?",shortAnswer:"LIST Partitioning is the premier strategy for discrete categorical, regional, and multi-tenant datasets: standardize on **`PARTITION BY LIST COLUMNS (varchar_col)`** for direct string mapping, ensure all `VALUES IN` sets are **strictly disjoint**, handle `NULL` explicitly with **`VALUES IN (NULL)`**, leverage **`ALTER TABLE ADD/REORGANIZE PARTITION`** to expand categories online, and enforce **data residency tiering via `DATA DIRECTORY`** on specific regional partitions.",explanation:"Mastering LIST partitioning enables building high-performance multi-tenant and multi-region database architectures with complete physical data isolation.",hint:"Summarize LIST vs LIST COLUMNS, disjoint set rules, explicit NULL handling, dynamic partition additions, and data residency tiering.",level:"basic",codeExample:`-- Master LIST COLUMNS Blueprint:
CREATE TABLE enterprise_tenants (
  tenant_code VARCHAR(30) NOT NULL,
  account_id BIGINT NOT NULL,
  balance DECIMAL(12,2) NOT NULL,
  PRIMARY KEY (account_id, tenant_code)
) ENGINE = InnoDB
PARTITION BY LIST COLUMNS (tenant_code) (
  PARTITION p_bengal_hub VALUES IN ('Kolkata', 'Barrackpore', 'Howrah'),
  PARTITION p_delhi_hub  VALUES IN ('New Delhi', 'Noida', 'Gurgaon'),
  PARTITION p_mumbai_hub VALUES IN ('Mumbai', 'Pune', 'Thane'),
  PARTITION p_unassigned VALUES IN (NULL)
);

-- Pruned Query:
SELECT * FROM enterprise_tenants WHERE tenant_code = 'Kolkata';`}],N=`================================================================================
MYSQL 8.0 TABLE PARTITIONING & HORIZONTAL SHARDING STRATEGIES
TOPIC 4: LIST Partitioning – Discrete Categories, Regions, and Status Codes
================================================================================

1. WHAT IS LIST PARTITIONING?
--------------------------------------------------------------------------------
LIST partitioning assigns rows to physical partitions based on whether a column's value
matches one of a discrete set of enumerated values defined in a \`VALUES IN (...)\` list.

Syntax Variant         Characteristics                       Example
--------------------------------------------------------------------------------
\`LIST (int_expr)\`      Requires an expression returning      \`PARTITION BY LIST (region_id)\`
                       integer category codes.               \`VALUES IN (10, 11, 12)\`

\`LIST COLUMNS\`         Direct partitioning on \`VARCHAR\`,     \`PARTITION BY LIST COLUMNS (region_name)\`
                       \`CHAR\`, \`DATE\` strings without        \`VALUES IN ('Kolkata', 'Barrackpore')\`
                       requiring integer ID mappings.

2. CORE RULES OF LIST PARTITIONING
--------------------------------------------------------------------------------
1. **Disjoint Sets**: Each enumerated value can appear in EXACTLY ONE partition list.
   Duplicate values across partition lists trigger \`ERROR 1495 (HY000)\`.
2. **Explicit Matching**: Incoming rows must match a defined value; unmapped values
   fail with \`ERROR 1526 (HY000): Table has no partition for value ...\`.
3. **NULL Handling**: \`NULL\` is only accepted if a partition explicitly lists it
   (\`PARTITION p_null VALUES IN (NULL)\`).

3. ENTERPRISE RUNBOOK (REGIONAL MULTI-TENANT ARCHITECTURE)
--------------------------------------------------------------------------------
-- 1. Standard LIST COLUMNS Table:
CREATE TABLE regional_accounts (
  account_id BIGINT NOT NULL,
  region_name VARCHAR(30) NOT NULL,
  balance DECIMAL(12,2),
  PRIMARY KEY (account_id, region_name)
) ENGINE = InnoDB
PARTITION BY LIST COLUMNS (region_name) (
  PARTITION p_bengal VALUES IN ('Kolkata', 'Barrackpore', 'Howrah'),
  PARTITION p_delhi  VALUES IN ('New Delhi', 'Noida', 'Gurgaon'),
  PARTITION p_mumbai VALUES IN ('Mumbai', 'Pune', 'Thane')
);

-- 2. Querying with 100% Partition Pruning:
EXPLAIN SELECT * FROM regional_accounts WHERE region_name = 'Kolkata';
-- Prunes to scan ONLY partition 'p_bengal'!

4. PRODUCTION BEST PRACTICES
--------------------------------------------------------------------------------
1. Standardize on \`LIST COLUMNS (varchar_col)\` for intuitive categorical routing.
2. Group low-traffic regions into consolidated regional partitions.
3. Explicitly define a \`PARTITION p_null VALUES IN (NULL)\` if the column allows NULLs.
4. Use \`ALTER TABLE ... ADD PARTITION\` to add new regions dynamically online.
================================================================================
`,A=()=>{const[s,o]=d.useState("list1_columns_vs_standard"),i={list1_columns_vs_standard:{configName:"1. LIST vs LIST COLUMNS",title:"1. Standard Integer LIST vs Modern String LIST COLUMNS",badge:"Syntax Comparison",badgeColor:"emerald",sqlSnippet:`-- 📜 1. LEGACY INTEGER LIST (Requires Integer Expression / ID Mapping):
CREATE TABLE legacy_branches (
  branch_id INT NOT NULL,
  region_code INT NOT NULL,
  PRIMARY KEY (branch_id, region_code)
) PARTITION BY LIST (region_code) (
  PARTITION p_bengal VALUES IN (10, 11, 12),
  PARTITION p_delhi  VALUES IN (20, 21, 22)
);

-- ⚡ 2. MODERN LIST COLUMNS (Direct String Categories):
CREATE TABLE modern_branches (
  branch_id INT NOT NULL,
  region_name VARCHAR(30) NOT NULL,
  PRIMARY KEY (branch_id, region_name)
) PARTITION BY LIST COLUMNS (region_name) (
  PARTITION p_bengal VALUES IN ('Kolkata', 'Barrackpore', 'Howrah'),
  PARTITION p_delhi  VALUES IN ('New Delhi', 'Noida', 'Gurgaon'),
  PARTITION p_mumbai VALUES IN ('Mumbai', 'Pune', 'Thane')
);`,explanation:"LIST COLUMNS allows partitioning directly on VARCHAR strings, ENUMs, and dates without maintaining internal surrogate integer ID mapping tables, simplifying schema management and application queries.",keyTakeaways:["LIST COLUMNS supports direct VARCHAR and ENUM categorical values.","Eliminates foreign key integer lookup joins for partition routing.","Makes partition definitions human-readable and self-documenting."]},list2_disjoint_sets:{configName:"2. Disjoint Sets & Rules",title:"2. Disjoint Set Validation & Explicit NULL Handling",badge:"Validation Rules",badgeColor:"cyan",sqlSnippet:`-- 🧮 1. DISJOINT SET REQUIREMENT (No Duplicate Values Allowed):
-- Every value must appear in EXACTLY ONE partition definition!
-- Duplicating 'Kolkata' in two partitions causes Error 1495:
-- PARTITION p_east VALUES IN ('Kolkata') -- ❌ FAILS IF ALREADY IN p_bengal!

-- 📦 2. EXPLICIT NULL VALUE HANDLING:
-- Unlike RANGE, LIST rejects NULL values unless explicitly declared:
PARTITION BY LIST (status_code) (
  PARTITION p_active   VALUES IN (1, 2),
  PARTITION p_inactive VALUES IN (0),
  PARTITION p_unmapped VALUES IN (NULL) -- ✅ Accepts NULL records cleanly!
);`,explanation:"All values across all partition lists must be mutually exclusive. Unmapped values trigger Error 1526, and NULL values are rejected unless a partition explicitly defines VALUES IN (NULL).",keyTakeaways:["Values must be strictly unique across all partition lists (disjoint).","Unmapped incoming values fail with Error 1526.","NULL values require an explicit PARTITION ... VALUES IN (NULL) definition."]},list3_multitenant_isolation:{configName:"3. Multi-Tenant Tiering",title:"3. Multi-Tenant Physical Isolation & Regional Tiering",badge:"Multi-Tenant Architecture",badgeColor:"purple",sqlSnippet:`-- 🏢 DEDICATED PARTITIONS FOR ENTERPRISE TENANTS:
CREATE TABLE multitenant_ledger (
  tenant_code VARCHAR(30) NOT NULL,
  account_id BIGINT NOT NULL,
  balance DECIMAL(12,2),
  PRIMARY KEY (account_id, tenant_code)
) PARTITION BY LIST COLUMNS (tenant_code) (
  PARTITION p_enterprise_tata VALUES IN ('tata_motors', 'tcs'),
  PARTITION p_enterprise_jio  VALUES IN ('reliance_jio', 'reliance_retail'),
  PARTITION p_smb_pool        VALUES IN ('smb_store_1', 'smb_store_2', 'smb_store_3')
);

-- 🔍 100% PRUNED TENANT QUERY:
EXPLAIN SELECT * FROM multitenant_ledger WHERE tenant_code = 'tcs';
-- partitions = p_enterprise_tata (Zero disk I/O contention with other tenants!)`,explanation:"Multi-tenant SaaS architectures leverage LIST COLUMNS to isolate high-volume enterprise tenants into dedicated physical .ibd files, delivering dedicated IOPS and predictable query latencies.",keyTakeaways:["Isolates VIP enterprise tenants into dedicated physical storage partitions.","Pools smaller SMB tenants into shared partitions for storage efficiency.","Guarantees tenant query pruning with zero cross-tenant lock contention."]},list4_dynamic_additions:{configName:"4. Dynamic Expansion",title:"4. Adding New Categories Online via ADD / REORGANIZE",badge:"Dynamic Lifecycle",badgeColor:"rose",sqlSnippet:`-- 🚀 1. ADDING A NEW REGIONAL PARTITION ONLINE:
ALTER TABLE modern_branches ADD PARTITION (
  PARTITION p_south VALUES IN ('Bengaluru', 'Hyderabad', 'Chennai')
);

-- 🔄 2. EXPANDING AN EXISTING PARTITION TO INCLUDE NEW CITIES:
ALTER TABLE modern_branches REORGANIZE PARTITION p_bengal INTO (
  PARTITION p_bengal VALUES IN ('Kolkata', 'Barrackpore', 'Howrah', 'Durgapur', 'Siliguri')
);`,explanation:"New regional categories or tenant codes can be added online via ADD PARTITION, or existing partitions can be expanded to include new cities using REORGANIZE PARTITION without table locks.",keyTakeaways:["ADD PARTITION appends new discrete categories online.","REORGANIZE PARTITION expands existing categorical lists safely.","Executes in milliseconds with zero disruption to active transactions."]}},t=i[s];return e.jsxs("div",{className:"bg-slate-950 text-slate-100 min-h-screen py-10 px-4 sm:px-6 lg:px-8 font-sans",children:[e.jsxs("header",{className:"max-w-6xl mx-auto mb-10 text-center sm:text-left border-b border-slate-800 pb-8",children:[e.jsxs("div",{className:"flex flex-wrap items-center gap-3 mb-3",children:[e.jsx("span",{className:"px-3 py-1 bg-emerald-950/80 text-emerald-400 border border-emerald-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Module 004.7: Partitioning & Horizontal Sharding"}),e.jsx("span",{className:"px-3 py-1 bg-cyan-950/80 text-cyan-400 border border-cyan-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Topic 4 of 12"})]}),e.jsxs("h1",{className:"text-xl sm:text-2xl md:text-3xl font-bold text-white tracking-tight leading-tight",children:[e.jsx("span",{className:"text-emerald-400",children:"LIST Partitioning"}),": Categories, ",e.jsx("span",{className:"text-cyan-400",children:"Regions"})," & Multi-Tenant Data"]}),e.jsxs("p",{className:"mt-4 text-base sm:text-lg text-slate-400 max-w-4xl leading-relaxed",children:["Mastering discrete categorical partitioning in MySQL 8.0: understanding ",e.jsx("code",{children:"LIST COLUMNS"})," string routing, enforcing disjoint set validation rules, isolating multi-tenant enterprise data into dedicated tablespaces, and managing partition lifecycles."]})]}),e.jsxs("main",{className:"max-w-6xl mx-auto space-y-16",children:[e.jsxs("section",{id:"list-pillars",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"1. The Four Pillars of LIST Partitioning"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Core mechanisms enabling categorical data division, multi-tenant isolation, and explicit set routing."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6",children:[e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-emerald-400 font-bold uppercase",children:"Pillar 1"}),e.jsx("h3",{className:"font-bold text-white text-base",children:"LIST COLUMNS"}),e.jsxs("p",{className:"text-xs text-slate-300 leading-relaxed",children:["Direct partitioning on ",e.jsx("code",{children:"VARCHAR"})," and ",e.jsx("code",{children:"ENUM"})," strings without integer ID mapping tables."]})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-bold uppercase",children:"Pillar 2"}),e.jsx("h3",{className:"font-bold text-white text-base",children:"Disjoint Sets"}),e.jsx("p",{className:"text-xs text-slate-300 leading-relaxed",children:"All values across all partition lists must be mutually exclusive with zero duplicate mappings."})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-purple-700/60 bg-purple-950/20 space-y-2 shadow-lg shadow-purple-950/40",children:[e.jsx("span",{className:"text-xs font-mono text-purple-400 font-bold uppercase",children:"Pillar 3"}),e.jsx("h3",{className:"font-bold text-purple-300 text-base",children:"Tenant Isolation"}),e.jsxs("p",{className:"text-xs text-slate-200 leading-relaxed",children:["Isolates enterprise SaaS tenants into dedicated physical ",e.jsx("code",{children:".ibd"})," files for guaranteed IOPS."]})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-rose-700/60 bg-rose-950/20 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-rose-400 font-bold uppercase",children:"Pillar 4"}),e.jsx("h3",{className:"font-bold text-rose-300 text-base",children:"Explicit NULLs"}),e.jsxs("p",{className:"text-xs text-slate-200 leading-relaxed",children:["Requires an explicit ",e.jsx("code",{children:"PARTITION ... VALUES IN (NULL)"})," to prevent insert crashes on null records."]})]})]})]}),e.jsxs("section",{id:"interactive-workbench",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"2. Interactive LIST Partitioning Workbench"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Test and observe integer LIST vs string LIST COLUMNS, disjoint set validation rules, multi-tenant isolation schemas, and dynamic additions."})]}),e.jsx("div",{className:"flex flex-wrap gap-3",children:Object.keys(i).map(a=>{const n=i[a],l=s===a;return e.jsx("button",{onClick:()=>o(a),className:r("px-4 py-2.5 rounded-xl font-medium text-xs sm:text-sm transition-all duration-200 border",l?"bg-emerald-500/10 border-emerald-500 text-emerald-400 shadow-lg shadow-emerald-950/40":"bg-slate-900/60 border-slate-800 text-slate-400 hover:text-slate-200 hover:bg-slate-900"),children:n.configName},a)})}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-6",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-3 border-b border-slate-800 pb-4",children:[e.jsxs("div",{children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-semibold uppercase tracking-wider",children:"LIST Partitioning Architecture"}),e.jsx("h3",{className:"text-lg sm:text-xl font-bold text-white mt-1",children:t.title})]}),e.jsx("span",{className:r("px-3 py-1 rounded-full text-xs font-mono font-semibold border w-fit",t.badgeColor==="emerald"&&"bg-emerald-950/80 text-emerald-300 border-emerald-700",t.badgeColor==="cyan"&&"bg-cyan-950/80 text-cyan-300 border-cyan-700",t.badgeColor==="purple"&&"bg-purple-950/80 text-purple-300 border-purple-700",t.badgeColor==="rose"&&"bg-rose-950/80 text-rose-300 border-rose-700"),children:t.badge})]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:t.explanation}),e.jsxs("div",{className:"space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"Partitioning DDL & Categorical Mapping:"}),e.jsx("pre",{className:"p-4 rounded-xl bg-slate-950 border border-slate-800 font-mono text-xs text-emerald-300 overflow-x-auto leading-relaxed",children:t.sqlSnippet})]}),e.jsxs("div",{className:"space-y-2 pt-2 border-t border-slate-800/80",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"Architectural Takeaways:"}),e.jsx("ul",{className:"grid grid-cols-1 md:grid-cols-3 gap-3 text-xs text-slate-300",children:t.keyTakeaways.map((a,n)=>e.jsxs("li",{className:"p-3 rounded-lg bg-slate-950/70 border border-slate-800/60 flex items-start gap-2",children:[e.jsx("span",{className:"text-emerald-400 font-bold",children:"✓"}),e.jsx("span",{children:a})]},n))})]})]})]}),e.jsxs("section",{id:"case-studies",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-purple-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"3. Real-World Engineering Scenarios in Bengal"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"LIST partitioning case studies in Barrackpore and Kolkata demonstrating localized regional POS pruning and multi-tenant banking data isolation."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-emerald-400"}),"Case 1: Mamata & Susmita – Regional Branch Pruning in Barrackpore Retail"]}),e.jsx("span",{className:"text-xs font-mono text-emerald-400 bg-emerald-950/80 px-2.5 py-1 rounded-full border border-emerald-800",children:"8x Faster Lookups"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Barrackpore, retail store POS cashiers billed ₹1.2 Crores in sales across multiple retail store outlets. Susmita configured ",e.jsx("code",{children:"PARTITION BY LIST COLUMNS (store_location)"})," with ",e.jsx("code",{children:"PARTITION p_barrackpore VALUES IN ('Barrackpore', 'N.C.Pukur')"})," and ",e.jsx("code",{children:"PARTITION p_kolkata VALUES IN ('Kolkata', 'Salt Lake')"}),". Local store queries pruned searches to the single Barrackpore file, speeding up cashier lookups by 8x."]})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-cyan-400"}),"Case 2: Abhronila & Debangshu – Multi-Tenant Tiering in Kolkata Bank"]}),e.jsx("span",{className:"text-xs font-mono text-cyan-400 bg-cyan-950/80 px-2.5 py-1 rounded-full border border-cyan-800",children:"Dedicated IOPS"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Kolkata, managing core banking across ₹500 Crores in volume for multi-tenant corporate clients, Debangshu used ",e.jsx("code",{children:"LIST (tenant_id)"})," to assign tier-1 enterprise clients their own dedicated physical partitions (e.g. ",e.jsx("code",{children:"PARTITION p_corp_tata VALUES IN (1001)"}),"). This guaranteed that high-volume enterprise transactions never suffered from disk I/O page eviction caused by other tenants."]})]})]})]}),e.jsxs("section",{id:"pitfalls-rules",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-rose-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"4. Senior Pitfalls & Production Best Practices"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Avoid duplicate categorical definitions across partitions and unmapped NULL insertions."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 1: Duplicate Values Across Lists"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Listing the same value in more than one partition's ",e.jsx("code",{children:"VALUES IN"})," list causes MySQL to reject table creation with Error 1495."]}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Ensure all partition value sets are strictly disjoint."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 2: Unhandled NULL Insertions"]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:"Inserting a row with a NULL partition key on a LIST partitioned table crashes with Error 1526 unless explicitly mapped."}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Explicitly declare a PARTITION ... VALUES IN (NULL) if column allows NULLs."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 1: Use LIST COLUMNS for Strings"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Standardize on ",e.jsx("code",{children:"PARTITION BY LIST COLUMNS (varchar_col)"})," to eliminate integer surrogate ID translation tables."]}),e.jsx("div",{className:"text-xs text-slate-400",children:"Improves query clarity and makes partition definitions self-documenting."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 2: Leverage DATA DIRECTORY for Residency"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Assign ",e.jsx("code",{children:"DATA DIRECTORY"})," to specific regional LIST partitions to satisfy legal data localization and GDPR residency mandates."]}),e.jsx("div",{className:"text-xs text-slate-400",children:"Enforces physical storage boundaries within a single logical schema."})]})]})]}),e.jsxs("section",{id:"printable-note",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"5. Printable Study Note & Teacher Advice"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Download complete printable notes and review key takeaways from Sir Sukanta Hui."})]}),e.jsx(I,{title:"Topic 4: LIST Partitioning: Partitioning by Discrete Categories, Regions, and Status Codes",content:N}),e.jsx(c,{note:"LIST Partitioning is your secret weapon for multi-tenant SaaS and regional data tiering! Always standardize on LIST COLUMNS so you can map strings like 'Kolkata' and 'Barrackpore' directly. Remember that all partition value lists must be strictly disjoint with zero duplicate entries across lists, incoming rows must match a mapped category to avoid Error 1526, and you must explicitly define VALUES IN (NULL) if your column permits NULL records!"})]}),e.jsxs("section",{id:"faq-section",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"6. Frequently Asked Questions (30 Deep-Dive Questions)"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Master the technical nuances of LIST vs LIST COLUMNS, disjoint set validation, multi-tenant isolation, NULL routing, and dynamic category expansion."})]}),e.jsx(p,{title:"LIST Partitioning & Categorical Routing FAQs",questions:m})]})]})]})};export{A as default};
