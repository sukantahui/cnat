import{b as d,j as e,bg as r}from"./vendor-react-core-Doz9nIC6.js";import{T as c}from"./TeacherSukantaHui-CC0AKmkm.js";import{F as p}from"./FAQTemplate-CkSqDH4B.js";import{P as h}from"./PlainTextPrint-C08xhKA4.js";import"./vendor-icons-BtJHuk7w.js";const m=[{question:"What is the `ha_partition` handler in MySQL and what is its role in table partitioning?",shortAnswer:"The `ha_partition` is a specialized **Storage Handler Proxy** inside the MySQL Server layer that intercepts SQL operations on partitioned tables, evaluates the partitioning expression, and delegates read/write operations to the appropriate underlying storage engine instances (`ha_innobase`).",explanation:"Acts as a traffic router between the MySQL SQL parser and individual partition tablespaces.",hint:"Proxy storage handler that routes SQL operations to individual partition storage engine handlers.",level:"basic",codeExample:"// SQL Query -> ha_partition (Evaluates Partition Function) -> ha_innobase (Target Partition)"},{question:"How does the storage engine process an `INSERT` statement into a partitioned table?",shortAnswer:"1. The SQL parser passes the row values to `ha_partition`; 2. `ha_partition` evaluates the partitioning expression (e.g. `YEAR(order_date)`) to determine the target partition ID $p_i$; 3. It invokes `ha_innobase::write_row()` on partition $p_i$'s dedicated InnoDB handler instance to write the row into `$table#p#p_i.ibd`.",explanation:"Directs new records straight to the correct physical file on disk.",hint:"Evaluates partition expression, identifies partition ID, and calls write_row on that partition's handler.",level:"intermediate",codeExample:`INSERT INTO sales_records (order_id, order_date, amount) VALUES (101, '2025-04-12', 4500.00);
-- ha_partition evaluates YEAR('2025-04-12') = 2025 -> Writes to p2025.ibd`},{question:"What exact internal steps occur when an `UPDATE` modifies a row's partition key column to a value belonging to a different partition?",shortAnswer:"InnoDB executes an atomic **cross-partition row transfer**: 1. Reads the existing row and acquires an exclusive row lock; 2. Calls `delete_row()` on the original partition handler; 3. Calls `write_row()` on the target partition handler with new values; 4. Commits both operations atomically within the same transaction.",explanation:"Transparent to the user, but incurs higher lock and undo log overhead than a single-partition update.",hint:"Atomic delete from source partition followed by insert into destination partition within same transaction.",level:"expert",codeExample:`-- Changes partition from p2024 to p2025:
UPDATE sales_records SET order_date = '2025-02-01' WHERE order_id = 101;`},{question:"What physical file naming convention is used by InnoDB for partitioned tables on disk in MySQL 8.0?",shortAnswer:"Each partition is stored as an independent `.ibd` file named **`tableName#p#partitionName.ibd`** inside the database schema directory (e.g. `/var/lib/mysql/retail_db/orders#p#p2025.ibd`).",explanation:"Allows the operating system to manage, back up, and allocate disk space per partition independently.",hint:"tableName#p#partitionName.ibd inside the database directory.",level:"basic",codeExample:`# Directory listing in /var/lib/mysql/kolkata_bank:
# -rw-r----- 1 mysql mysql  15728640 Aug 25 15:30 accounts#p#p_north.ibd
# -rw-r----- 1 mysql mysql  18874368 Aug 25 15:30 accounts#p#p_south.ibd`},{question:"In Mamata & Susmita's Barrackpore retail store, POS cashiers processed ₹1.2 Crores in sales transactions. How did partition-level row locking allow cashier updates in `p2025` without blocking analytical queries reading `p2024`?",shortAnswer:"Because InnoDB row-level record locks (`X`/`S` locks) are stored in the buffer pool and associated with specific pages inside `pos_audit_logs#p#p2025.ibd`; analytical queries reading historical data in `pos_audit_logs#p#p2024.ibd` accessed completely independent physical pages with zero lock contention.",explanation:"Partition-level physical isolation enables high concurrent write throughput.",hint:"Row locks are isolated to the specific partition's pages, allowing concurrent access to other partitions.",level:"moderate",codeExample:"-- Cashier writes to p2025 while Accountant reads p2024 with ZERO lock wait!"},{question:"In Abhronila & Debangshu's Kolkata fintech bank, how did the `open_files_limit` setting become critical after RANGE-partitioning 50 core tables into 100 monthly partitions each across ₹500 Crores in banking records?",shortAnswer:"Partitioning 50 tables into 100 partitions created $50 	imes 100 = 5,000$ independent `.ibd` physical files; because MySQL opens file descriptors for each active partition tablespace, Debangshu had to increase `open_files_limit` to `65536` and `table_open_cache` to `10000` to prevent `Error 24: Too many open files` crashes.",explanation:"Partitioned tables multiply filesystem descriptor consumption.",hint:"Each partition is a separate .ibd file; requires increasing open_files_limit and table_open_cache.",level:"expert",codeExample:`[mysqld]
open_files_limit = 65536
table_open_cache = 10000
innodb_open_files = 10000`},{question:"Where is partition metadata stored in MySQL 8.0 vs legacy MySQL 5.7?",shortAnswer:"In MySQL 8.0, partition metadata is stored transactionally inside the unified **MySQL Data Dictionary (`mysql.ibd`)**; legacy `.par` (partition definition) and `.frm` files have been completely eliminated.",explanation:"Guarantees atomic, crash-safe DDL operations on partitioned tables.",hint:"Stored transactionally in the MySQL 8.0 Data Dictionary; legacy .par files are removed.",level:"intermediate",codeExample:"-- All partition metadata is crash-safe in mysql.ibd."},{question:"What is `information_schema.PARTITIONS` and what critical columns should be monitored?",shortAnswer:"It provides real-time metadata for all partitioned tables; key columns to monitor are: `PARTITION_NAME`, `PARTITION_METHOD`, `PARTITION_EXPRESSION`, `TABLE_ROWS`, `DATA_LENGTH`, `INDEX_LENGTH`, and `DATA_FREE` (fragmentation).",explanation:"Enables programmatic health audits and data skew detection.",hint:"System catalog view showing TABLE_ROWS, DATA_LENGTH, and DATA_FREE per partition.",level:"basic",codeExample:`SELECT PARTITION_NAME, PARTITION_METHOD, TABLE_ROWS, 
       ROUND(DATA_LENGTH / 1024 / 1024, 2) AS DATA_MB,
       ROUND(INDEX_LENGTH / 1024 / 1024, 2) AS INDEX_MB
FROM information_schema.PARTITIONS 
WHERE TABLE_NAME = 'bank_ledgers';`},{question:"How does the storage engine handle secondary indexes on a partitioned table in MySQL?",shortAnswer:"All secondary indexes in MySQL are **Local Partitioned Indexes**: every physical partition `.ibd` file contains its own independent secondary B-Tree index covering only the rows residing within that specific partition (MySQL does NOT support Global Indexes).",explanation:"Ensures index updates remain localized within a single partition file.",hint:"All secondary indexes are strictly Local Partitioned Indexes confined to each partition's .ibd file.",level:"intermediate",codeExample:"-- Local index: p2025.ibd has an idx_customer covering only 2025 orders."},{question:"Why does the absence of Global Indexes in MySQL explain why all Unique Keys must include the partition column?",shortAnswer:"Because secondary indexes are strictly local to each partition `.ibd` file; if a unique key did not include the partition column, verifying uniqueness during an `INSERT` would require scanning all 100 partitions' local index trees, destroying write performance. Requiring the partition key allows the storage engine to enforce uniqueness locally within that single partition.",explanation:"The core architectural rationale behind Error 1503.",hint:"Enables enforcing uniqueness locally within the partition without checking all other partitions.",level:"expert",codeExample:"-- Local uniqueness enforcement requires partition key in unique constraint."},{question:"What is `ha_partition::index_read_map()` and how is it used during query execution?",shortAnswer:"It is the internal C++ handler method called by the storage engine to look up a key in a partition's local B-Tree index; if pruning is active, it is invoked only on the matching partition handler; if unpruned, it is invoked sequentially across all partition handlers.",explanation:"The low-level index lookup entry point in MySQL handler architecture.",hint:"Internal handler method for performing indexed lookups on specific partition handlers.",level:"expert",codeExample:"// ha_partition routes index_read_map() to target ha_innobase instance."},{question:"What happens when a query executes `SELECT ... ORDER BY col LIMIT 10` on an unpruned partitioned table?",shortAnswer:"The `ha_partition` handler reads the top 10 rows from **every individual partition handler**, buffers them in memory, and performs a multi-way **Priority Queue Merge Sort** to return the global top 10 rows to the client.",explanation:"Increases CPU and temporary memory consumption during unpruned sorted queries.",hint:"Reads top rows from every partition and performs a priority queue merge sort.",level:"expert",codeExample:"-- Multi-partition merge sort across p2023, p2024, p2025, p_future."},{question:"What is `innodb_file_per_table`'s impact on table partitioning?",shortAnswer:"When `innodb_file_per_table = ON` (default), each partition is created as a distinct physical `.ibd` file on disk; if set to `OFF`, all partitions are stored inside the monolithic system tablespace (`ibdata1`), losing the ability to reclaim disk space via `DROP PARTITION`.",explanation:"`innodb_file_per_table = ON` is mandatory for effective partition storage management.",hint:"Mandatory for creating separate .ibd files per partition and reclaiming space on drop.",level:"intermediate",codeExample:"SHOW VARIABLES LIKE 'innodb_file_per_table'; -- Must be ON"},{question:"What is `ALTER TABLE ... CHECK PARTITION p1` used for?",shortAnswer:"It instructs InnoDB to scan and verify the physical page integrity and B-Tree index structure of partition `p1`, reporting any block corruption or checksum errors without locking other partitions.",explanation:"Provides granular per-partition database health checks.",hint:"Verifies page integrity and B-Tree consistency for a specific partition.",level:"basic",codeExample:"ALTER TABLE sales_records CHECK PARTITION p2025;"},{question:"What is `ALTER TABLE ... OPTIMIZE PARTITION p1` used for?",shortAnswer:"It rebuilds the physical `.ibd` tablespace for partition `p1` online, defragmenting clustered and secondary B-Tree pages and reclaiming unused free disk space (`DATA_FREE`) after heavy delete/update churn.",explanation:"Reclaims disk space without rebuilding the entire multi-gigabyte table.",hint:"Rebuilds and defragments a single partition to reclaim free disk space.",level:"intermediate",codeExample:"ALTER TABLE sales_records OPTIMIZE PARTITION p2024;"},{question:"What is `ALTER TABLE ... REPAIR PARTITION p1` in MySQL 8.0?",shortAnswer:"For InnoDB tables, `REPAIR PARTITION` is treated as a partition rebuild (similar to `OPTIMIZE PARTITION`), verifying and reconstructing index structures for that specific partition.",explanation:"InnoDB uses transactional crash recovery, so repair operations rebuild tablespaces.",hint:"Rebuilds and reconstructs index structures for the specified partition.",level:"basic",codeExample:"ALTER TABLE sales_records REPAIR PARTITION p2024;"},{question:"What is `ALTER TABLE ... ANALYZE PARTITION p1` used for?",shortAnswer:"It samples index key distributions inside partition `p1`'s `.ibd` tablespace and updates index cardinality statistics in `mysql.innodb_index_stats`, helping the query optimizer generate accurate execution plans.",explanation:"Updates optimizer cardinality statistics for an individual partition.",hint:"Updates index statistics and cardinality for a single partition.",level:"basic",codeExample:"ALTER TABLE sales_records ANALYZE PARTITION p2025;"},{question:"How does `ALTER TABLE ... EXCHANGE PARTITION` work at the storage engine level?",shortAnswer:"It **swaps the physical `.ibd` tablespace file** of a partitioned table's partition with a standalone non-partitioned table's `.ibd` file via metadata pointer swaps in **under 10 milliseconds**, enabling instant ETL data ingestion or historical archival.",explanation:"High-speed data interchange with zero row-by-row copying.",hint:"Swaps physical tablespace pointers between a partition and a standalone table in milliseconds.",level:"expert",codeExample:"ALTER TABLE sales_records EXCHANGE PARTITION p2024 WITH TABLE orders_2024_archive;"},{question:"What prerequisite is MANDATORY for `ALTER TABLE ... EXCHANGE PARTITION` to succeed?",shortAnswer:"The standalone table must have the **exact same table structure, column types, indexes, and storage engine (InnoDB)** as the partitioned table, must NOT be partitioned itself, and all rows in the standalone table must satisfy the partition's boundary rules.",explanation:"Guarantees schema symmetry before swapping tablespace pointers.",hint:"Identical schema, column types, indexes, storage engine, and row value compliance.",level:"expert",codeExample:"-- Standalone table must match partitioned table schema exactly."},{question:"What is the difference between Subpartitioning by HASH vs KEY in storage mechanics?",shortAnswer:"`SUBPARTITION BY HASH` uses an explicit integer column or user SQL expression modulo the number of subpartitions; `SUBPARTITION BY KEY` uses MySQL internal hashing (based on MD5/password hash) on any column type (including `VARCHAR`, `UUID`, etc.) to distribute subpartitions.",explanation:"KEY subpartitioning handles non-integer column data types natively.",hint:"HASH uses integer modulo expressions; KEY uses MySQL internal hash supporting strings and UUIDs.",level:"intermediate",codeExample:"SUBPARTITION BY KEY (customer_uuid) SUBPARTITIONS 4"},{question:"What happens if an `INSERT` statement provides a row with a `NULL` value in the partitioning column?",shortAnswer:"Under `RANGE` partitioning, `NULL` is treated as **less than any non-NULL value**, and the row is automatically stored in the **lowest-valued partition** (e.g. `p0`). Under `LIST` partitioning, the row is rejected unless a partition explicitly lists `NULL` (e.g. `PARTITION p_null VALUES IN (NULL)`).",explanation:"Handles NULL values deterministically according to partitioning rules.",hint:"RANGE stores NULL in the lowest partition; LIST requires explicit VALUES IN (NULL).",level:"intermediate",codeExample:"PARTITION p_null VALUES IN (NULL)"},{question:"How does MySQL storage engine allocate Auto-Increment values on partitioned tables?",shortAnswer:"Auto-Increment allocation is managed globally at the table level in memory; successive `INSERT` operations receive monotonically increasing IDs regardless of which physical partition the row is routed to.",explanation:"Maintains globally unique sequential IDs across all physical partitions.",hint:"Auto-increment is managed globally at the table level across all partitions.",level:"basic",codeExample:"-- order_id increments globally: 1 (p2024), 2 (p2025), 3 (p2024), 4 (p2025)..."},{question:"Can an individual partition have its own unique index that does NOT exist on other partitions?",shortAnswer:"**No**, all partitions in a partitioned table share the **exact same table definition, column types, and index schema**; you cannot create an index on `p2025` that does not exist on `p2024`.",explanation:"Enforces homogeneous schema across all underlying physical storage files.",hint:"All partitions must share the identical index structure and column schema.",level:"basic",codeExample:"-- Schema changes apply to all partitions uniformly."},{question:"What is `innodb_buffer_pool_size`'s relationship with partitioned tables during heavy OLTP operations?",shortAnswer:"Because each partition's clustered B-Tree is smaller, the active working set (e.g. current month partition) fits completely into the buffer pool RAM; dirty pages are flushed per tablespace, reducing checkpoint flushing pressure across unrelated cold historical partitions.",explanation:"Improves buffer pool caching efficiency and reduces I/O write amplification.",hint:"Active partition fits inside RAM, reducing dirty page flushing pressure on cold historical data.",level:"intermediate",codeExample:`[mysqld]
innodb_buffer_pool_size = 32G`},{question:"What is `FLUSH TABLES ... FOR EXPORT`'s behavior on partitioned tables?",shortAnswer:"It flushes dirty pages for all partitions to disk, locks the tables, and creates `.cfg` metadata files for **every individual partition**, allowing physical cold-backup extraction of specific partition `.ibd` files.",explanation:"Enables transportable tablespace exports for partitioned tables.",hint:"Flushes and creates .cfg metadata files for all partition tablespaces.",level:"expert",codeExample:"FLUSH TABLES sales_records FOR EXPORT;"},{question:"What happens if a disk failure corrupts only one partition `.ibd` file (e.g. `orders#p#p2022.ibd`)?",shortAnswer:"Only queries attempting to read from `p2022` will fail with an I/O error; queries accessing other healthy partitions (e.g. `p2025.ibd`) continue to execute normally with **zero downtime for current production operations**.",explanation:"Limits blast radius of storage block corruption to the affected partition file.",hint:"Isolates storage corruption: healthy partitions continue serving queries without downtime.",level:"intermediate",codeExample:"-- Corruption in p2022 does not stop active POS billing in p2025!"},{question:"How do you inspect the physical disk space used by each partition on the Linux filesystem?",shortAnswer:"Run `ls -lh /var/lib/mysql/<database_name>/<table_name>#p#*.ibd` or query `DATA_LENGTH` in `information_schema.PARTITIONS`.",explanation:"Provides instant filesystem-level verification of partition file sizes.",hint:"Check file sizes with ls -lh in the database data directory.",level:"basic",codeExample:"ls -lh /var/lib/mysql/kolkata_retail/sales_records#p#*.ibd"},{question:"Why does `TRUNCATE TABLE` on a partitioned table operate at line speed?",shortAnswer:"Because MySQL drops and recreates the physical `.ibd` files for every partition in metadata, taking milliseconds to reset a 500GB table without reading or deleting rows one by one.",explanation:"Instant table reset via filesystem recreation.",hint:"Recreates physical partition files in metadata instantly without scanning rows.",level:"basic",codeExample:"TRUNCATE TABLE sales_records;"},{question:"What is the primary operational takeaway of Topic 1 in Module 004_007?",shortAnswer:"Table partitioning is orchestrated by the **`ha_partition` proxy handler**, which evaluates the partition expression to route SQL operations to dedicated **`ha_innobase` instances** managing independent **`tableName#p#partitionName.ibd`** tablespaces; this delivers **partition-isolated row locking**, localized secondary indexes, and sub-10ms **`EXCHANGE PARTITION`** data swaps, but requires monitoring **`open_files_limit`** and sizing **`innodb_buffer_pool_size`** for active working partitions.",explanation:"Mastering storage engine partitioning mechanics enables DBAs to optimize physical I/O layout and manage high-concurrency VLDB tables with zero lock contention.",hint:"Summarize ha_partition proxy, .ibd file layout, local secondary indexes, cross-partition atomic updates, and file descriptor limits.",level:"basic",codeExample:`-- Master Storage Engine Partition Inspection:
SELECT PARTITION_NAME, TABLE_ROWS, DATA_LENGTH, INDEX_LENGTH 
FROM information_schema.PARTITIONS 
WHERE TABLE_SCHEMA = 'kolkata_bank' AND TABLE_NAME = 'ledger';`},{question:"How do you change the storage engine of a partitioned table (e.g. from MyISAM to InnoDB)?",shortAnswer:"Execute `ALTER TABLE table_name ENGINE = InnoDB;`; MySQL rebuilds all underlying physical partition files into InnoDB `.ibd` tablespaces online.",explanation:"Converts all partitions to the target storage engine uniformly.",hint:"Execute ALTER TABLE table_name ENGINE = InnoDB.",level:"basic",codeExample:"ALTER TABLE sales_records ENGINE = InnoDB;"}],x=`================================================================================
MYSQL 8.0 TABLE PARTITIONING & HORIZONTAL SHARDING STRATEGIES
TOPIC 1: Partitioning Mechanics – How the Storage Engine Handles Partitioned Tables
================================================================================

1. THE ARCHITECTURE OF HA_PARTITION & INNODB HANDLERS
--------------------------------------------------------------------------------
In MySQL 8.0, partitioned tables are orchestrated through a specialized handler proxy:
  [SQL Parser / Optimizer]
            │
            ▼
  [ha_partition (Partition Handler Proxy)]
            │ (Evaluates Partitioning Expression)
     ┌──────┴──────┐
     ▼             ▼
  [ha_innobase]  [ha_innobase]  ──> (Underlying InnoDB Engine Instances)
  (Partition 0)  (Partition 1)
     │             │
     ▼             ▼
  [table#p#p0.ibd] [table#p#p1.ibd] (Independent Physical Tablespaces)

2. QUERY EXECUTION PATHWAY IN PARTITIONED TABLES
--------------------------------------------------------------------------------
Operation           Internal Execution Mechanics
--------------------------------------------------------------------------------
\`INSERT\`            \`ha_partition\` evaluates expression (e.g. \`YEAR(order_date)\`),
                    identifies partition ID $p_i$, and delegates insert to $p_i$'s InnoDB handler.

\`SELECT\` (Pruned)   Optimizer evaluates \`WHERE\` clause, identifies matching partition $p_i$,
                    and opens *only* the specific \`$p_i.ibd\` tablespace.

\`SELECT\` (Unpruned) \`ha_partition\` opens all partition handlers, scans each \`.ibd\` file,
                    and merges result streams (Scatter-Gather penalty).

\`UPDATE\` (Same Part)\`ha_innobase\` updates row in place inside the same \`.ibd\` tablespace.

\`UPDATE\` (Cross Part)InnoDB executes an atomic **delete from old partition** followed by
                    **insert into new partition** within the same transaction.

3. PHYSICAL DISK LAYOUT & DATA DICTIONARY
--------------------------------------------------------------------------------
- Each partition has its own \`.ibd\` file: \`db_name/table_name#p#partition_name.ibd\`.
- Partition metadata is stored transactionally in MySQL 8.0 Data Dictionary (\`mysql.ibd\`).
- No legacy \`.par\` metadata files exist in MySQL 8.0.

4. LOCKING & CONCURRENCY CHARACTERISTICS
--------------------------------------------------------------------------------
- **Row-Level Record Locks**: Isolated to the specific partition's \`.ibd\` tablespace.
- Concurrent transactions modifying different partitions experience ZERO lock contention!
- DDL partition operations (\`ADD PARTITION\`, \`DROP PARTITION\`) acquire brief Metadata Locks (\`MDL\`).

5. PRODUCTION BEST PRACTICES
--------------------------------------------------------------------------------
1. Ensure \`innodb_file_per_table = ON\` to allow independent \`.ibd\` partition management.
2. Monitor open file descriptors via \`open_files_limit\` for tables with many partitions.
3. Minimize cross-partition updates by choosing stable, immutable partitioning columns.
4. Inspect partition storage usage via \`information_schema.PARTITIONS\`.
================================================================================
`,E=()=>{const[s,o]=d.useState("mechanic1_handler_proxy"),i={mechanic1_handler_proxy:{mechanicName:"1. ha_partition Proxy",title:"1. Storage Engine Architecture & ha_partition Proxy Handler",badge:"Handler Pipeline",badgeColor:"emerald",sqlSnippet:`// ⚙️ HOW SQL QUERIES FLOW THROUGH THE ENGINE:
// [Client Query: INSERT INTO sales_records VALUES (101, '2025-04-12', 4500)]
//       │
//       ▼
// [MySQL Server SQL Parser / Optimizer]
//       │
//       ▼
// [ha_partition (Partition Handler Proxy)]
//       │ (Evaluates YEAR('2025-04-12') = 2025 -> Target: Partition 1)
//       ▼
// [ha_innobase Instance for Partition 1]
//       │
//       ▼
// [Physical Filesystem: /var/lib/mysql/sales_records#p#p2025.ibd]`,explanation:"The ha_partition proxy handler sits between the SQL parser and individual InnoDB engine instances. It evaluates the partitioning expression to route reads, writes, and row-level locks directly to the target partition tablespace.",keyTakeaways:["ha_partition acts as a smart router above underlying ha_innobase handlers.","Evaluates partitioning expressions to identify the target partition ID.","Delegates write_row and index_read_map to specific partition tablespaces."]},mechanic2_cross_partition_update:{mechanicName:"2. Cross-Partition Updates",title:"2. Cross-Partition Atomic Row Movement Mechanics",badge:"Atomic 2PC",badgeColor:"cyan",sqlSnippet:`-- ⚡ UPDATING PARTITION KEY TO MOVE ROW BETWEEN PARTITIONS:
UPDATE sales_records SET order_date = '2025-01-10' WHERE order_id = 101;

-- 🔍 INTERNAL 4-STEP STORAGE ENGINE EXECUTION:
-- 1. InnoDB locks row in p2024 (sales_records#p#p2024.ibd).
-- 2. ha_innobase calls delete_row() on partition p2024.
-- 3. ha_innobase calls write_row() with new date on partition p2025.
-- 4. Commits both operations atomically within the same transaction!`,explanation:"When an UPDATE changes the partition key, InnoDB executes an atomic delete from the old partition followed by an insert into the new partition within a single transaction, guaranteeing data integrity across files.",keyTakeaways:["Executes an atomic delete from old partition and insert into new partition.","Row movement occurs within the same InnoDB ACID transaction boundary.","Partition keys should be stable to minimize cross-partition movement overhead."]},mechanic3_local_indexes:{mechanicName:"3. Local Secondary Indexes",title:"3. Local Partitioned Indexes vs Global Index Absences",badge:"Local B-Trees",badgeColor:"purple",sqlSnippet:`-- 📑 SECONDARY INDEX CREATION ON PARTITIONED TABLE:
CREATE INDEX idx_customer ON sales_records (customer_id);

-- 💡 HOW MYSQL HANDLES SECONDARY INDEXES:
-- Every partition (.ibd file) maintains its OWN LOCAL B-TREE INDEX!
-- p2024.ibd -> Contains idx_customer for 2024 rows only.
-- p2025.ibd -> Contains idx_customer for 2025 rows only.
-- MySQL does NOT have Global Indexes; all indexes are strictly LOCAL!`,explanation:"All secondary indexes in MySQL are Local Partitioned Indexes confined to each individual partition's .ibd file. This is why unique constraints must include the partition key to enable local uniqueness verification.",keyTakeaways:["All secondary indexes are strictly Local Partitioned Indexes.","Each partition maintains an independent secondary B-Tree on disk.","Enables local index updates without locking unrelated historical partitions."]},mechanic4_exchange_partition:{mechanicName:"4. EXCHANGE PARTITION",title:"4. Sub-10ms Data Swaps via EXCHANGE PARTITION",badge:"Zero-Copy Swap",badgeColor:"rose",sqlSnippet:`-- 🚀 SWAPPING A PARTITION WITH A STANDALONE TABLE IN UNDER 10ms:
-- Step 1: Create identical standalone staging table:
CREATE TABLE orders_2024_staging LIKE sales_records;
ALTER TABLE orders_2024_staging REMOVE PARTITIONING;

-- Step 2: Swap physical tablespace pointers instantly:
ALTER TABLE sales_records EXCHANGE PARTITION p2024 WITH TABLE orders_2024_staging;

-- 💡 Result: Physical .ibd file pointers swapped in metadata with ZERO row copying!`,explanation:"EXCHANGE PARTITION swaps tablespace file pointers between a partition and a standalone table in under 10 milliseconds, enabling instant ETL ingestion, table re-indexing, and rapid historical data archiving.",keyTakeaways:["Swaps tablespace metadata pointers at line speed (<10ms).","Eliminates slow row-by-row data copying during ETL operations.","Requires identical schemas, indexes, and column types on both tables."]}},t=i[s];return e.jsxs("div",{className:"bg-slate-950 text-slate-100 min-h-screen py-10 px-4 sm:px-6 lg:px-8 font-sans",children:[e.jsxs("header",{className:"max-w-6xl mx-auto mb-10 text-center sm:text-left border-b border-slate-800 pb-8",children:[e.jsxs("div",{className:"flex flex-wrap items-center gap-3 mb-3",children:[e.jsx("span",{className:"px-3 py-1 bg-emerald-950/80 text-emerald-400 border border-emerald-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Module 004.7: Partitioning & Horizontal Sharding"}),e.jsx("span",{className:"px-3 py-1 bg-cyan-950/80 text-cyan-400 border border-cyan-800 rounded-full text-xs font-mono font-semibold uppercase tracking-wider",children:"Topic 1 of 12"})]}),e.jsxs("h1",{className:"text-3xl sm:text-5xl font-extrabold text-white tracking-tight leading-tight",children:[e.jsx("span",{className:"text-emerald-400",children:"Partitioning Mechanics"}),": Storage Engine Internals & ",e.jsx("span",{className:"text-cyan-400",children:"ha_partition"})]}),e.jsxs("p",{className:"mt-4 text-base sm:text-lg text-slate-400 max-w-4xl leading-relaxed",children:["Mastering low-level storage engine partitioning mechanics in MySQL 8.0: understanding the ",e.jsx("code",{children:"ha_partition"})," proxy handler pipeline, atomic cross-partition row transfers, local partitioned B-Trees, and sub-10ms ",e.jsx("code",{children:"EXCHANGE PARTITION"})," tablespace swaps."]})]}),e.jsxs("main",{className:"max-w-6xl mx-auto space-y-16",children:[e.jsxs("section",{id:"mechanics-pillars",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"1. The Four Pillars of Storage Engine Partitioning"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Low-level storage architecture enabling transparent routing, file isolation, and instant data interchange."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6",children:[e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-emerald-400 font-bold uppercase",children:"Pillar 1"}),e.jsx("h3",{className:"font-bold text-white text-base",children:"ha_partition Proxy"}),e.jsxs("p",{className:"text-xs text-slate-300 leading-relaxed",children:["Routes SQL operations to specific underlying ",e.jsx("code",{children:"ha_innobase"})," partition engine instances."]})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-slate-800 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-bold uppercase",children:"Pillar 2"}),e.jsx("h3",{className:"font-bold text-white text-base",children:"Atomic Transfers"}),e.jsx("p",{className:"text-xs text-slate-300 leading-relaxed",children:"Cross-partition updates execute an atomic delete + insert sequence within a single transaction."})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-purple-700/60 bg-purple-950/20 space-y-2 shadow-lg shadow-purple-950/40",children:[e.jsx("span",{className:"text-xs font-mono text-purple-400 font-bold uppercase",children:"Pillar 3"}),e.jsx("h3",{className:"font-bold text-purple-300 text-base",children:"Local B-Trees"}),e.jsxs("p",{className:"text-xs text-slate-200 leading-relaxed",children:["Secondary indexes are strictly localized to each partition's physical ",e.jsx("code",{children:".ibd"})," file."]})]}),e.jsxs("div",{className:"p-5 rounded-2xl bg-slate-900/90 border border-rose-700/60 bg-rose-950/20 space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-rose-400 font-bold uppercase",children:"Pillar 4"}),e.jsx("h3",{className:"font-bold text-rose-300 text-base",children:"Exchange Swap"}),e.jsx("p",{className:"text-xs text-slate-200 leading-relaxed",children:"Swaps tablespace pointers with standalone tables in <10ms for instant ETL ingestion."})]})]})]}),e.jsxs("section",{id:"interactive-workbench",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"2. Interactive Storage Engine Mechanics Workbench"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Test and observe ha_partition proxy routing, atomic cross-partition updates, local secondary index structures, and EXCHANGE PARTITION commands."})]}),e.jsx("div",{className:"flex flex-wrap gap-3",children:Object.keys(i).map(a=>{const n=i[a],l=s===a;return e.jsx("button",{onClick:()=>o(a),className:r("px-4 py-2.5 rounded-xl font-medium text-xs sm:text-sm transition-all duration-200 border",l?"bg-emerald-500/10 border-emerald-500 text-emerald-400 shadow-lg shadow-emerald-950/40":"bg-slate-900/60 border-slate-800 text-slate-400 hover:text-slate-200 hover:bg-slate-900"),children:n.mechanicName},a)})}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-6",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-3 border-b border-slate-800 pb-4",children:[e.jsxs("div",{children:[e.jsx("span",{className:"text-xs font-mono text-cyan-400 font-semibold uppercase tracking-wider",children:"Engine Mechanics Feature"}),e.jsx("h3",{className:"text-lg sm:text-xl font-bold text-white mt-1",children:t.title})]}),e.jsx("span",{className:r("px-3 py-1 rounded-full text-xs font-mono font-semibold border w-fit",t.badgeColor==="emerald"&&"bg-emerald-950/80 text-emerald-300 border-emerald-700",t.badgeColor==="cyan"&&"bg-cyan-950/80 text-cyan-300 border-cyan-700",t.badgeColor==="purple"&&"bg-purple-950/80 text-purple-300 border-purple-700",t.badgeColor==="rose"&&"bg-rose-950/80 text-rose-300 border-rose-700"),children:t.badge})]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:t.explanation}),e.jsxs("div",{className:"space-y-2",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"Engine Pipeline & SQL Commands:"}),e.jsx("pre",{className:"p-4 rounded-xl bg-slate-950 border border-slate-800 font-mono text-xs text-emerald-300 overflow-x-auto leading-relaxed",children:t.sqlSnippet})]}),e.jsxs("div",{className:"space-y-2 pt-2 border-t border-slate-800/80",children:[e.jsx("span",{className:"text-xs font-mono text-slate-400 font-semibold",children:"Architectural Takeaways:"}),e.jsx("ul",{className:"grid grid-cols-1 md:grid-cols-3 gap-3 text-xs text-slate-300",children:t.keyTakeaways.map((a,n)=>e.jsxs("li",{className:"p-3 rounded-lg bg-slate-950/70 border border-slate-800/60 flex items-start gap-2",children:[e.jsx("span",{className:"text-emerald-400 font-bold",children:"✓"}),e.jsx("span",{children:a})]},n))})]})]})]}),e.jsxs("section",{id:"case-studies",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-purple-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"3. Real-World Engineering Scenarios in Bengal"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Partition mechanics case studies in Barrackpore and Kolkata demonstrating partition lock isolation and file descriptor tuning."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-emerald-400"}),"Case 1: Mamata & Susmita – Zero Lock Contention in Barrackpore Retail"]}),e.jsx("span",{className:"text-xs font-mono text-emerald-400 bg-emerald-950/80 px-2.5 py-1 rounded-full border border-emerald-800",children:"Lock Isolation"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Barrackpore, retail store POS cashiers billed ₹1.2 Crores in sales while accountants ran heavy quarterly reporting queries. Because InnoDB stores record locks inside specific page headers within ",e.jsx("code",{children:"pos_sales#p#p2025.ibd"}),", cashiers wrote transactions with zero lock wait time while the accounting report scanned historical pages inside ",e.jsx("code",{children:"pos_sales#p#p2024.ibd"}),"."]})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800 space-y-4",children:[e.jsxs("div",{className:"flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-slate-800 pb-3",children:[e.jsxs("h3",{className:"text-base font-bold text-white flex items-center gap-2",children:[e.jsx("span",{className:"w-2.5 h-2.5 rounded-full bg-cyan-400"}),"Case 2: Abhronila & Debangshu – File Descriptor Tuning in Kolkata Bank"]}),e.jsx("span",{className:"text-xs font-mono text-cyan-400 bg-cyan-950/80 px-2.5 py-1 rounded-full border border-cyan-800",children:"Descriptor Sizing"})]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed",children:["In Kolkata, managing core banking across ₹500 Crores in daily volume, partitioning 50 tables into 100 monthly partitions created 5,000 physical ",e.jsx("code",{children:".ibd"})," files. Debangshu increased ",e.jsx("code",{children:"open_files_limit = 65536"})," and ",e.jsx("code",{children:"table_open_cache = 10000"}),", ensuring the operating system could maintain open file handles across all active partition tablespaces without thread bottlenecks."]})]})]})]}),e.jsxs("section",{id:"pitfalls-rules",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-rose-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"4. Senior Pitfalls & Production Best Practices"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Avoid frequent cross-partition updates and running out of open file descriptors."})]}),e.jsxs("div",{className:"grid grid-cols-1 md:grid-cols-2 gap-6",children:[e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 1: Highly Volatile Partition Keys"]}),e.jsx("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:"Partitioning on a column that is frequently updated (e.g. status) triggers constant cross-partition row moves, multiplying row locks and undo logging."}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Choose immutable or rarely updated columns (like created_at or tenant_id)."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-rose-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"⚠️"})," Pitfall 2: Neglecting open_files_limit"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Creating hundreds of partitions without increasing ",e.jsx("code",{children:"open_files_limit"})," causes 'Too many open files' OS errors under peak query concurrency."]}),e.jsx("div",{className:"text-xs font-mono text-emerald-400 p-2 bg-slate-950 rounded border border-slate-800",children:"Rule: Set open_files_limit = 65536 in my.cnf for partitioned database servers."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 1: Leverage EXCHANGE PARTITION for ETL"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Use ",e.jsx("code",{children:"ALTER TABLE ... EXCHANGE PARTITION"})," to load massive bulk data or re-index historical tables with sub-10ms metadata swaps."]}),e.jsx("div",{className:"text-xs text-slate-400",children:"Eliminates row-by-row batch insertion overhead entirely."})]}),e.jsxs("div",{className:"p-6 rounded-2xl bg-slate-900/80 border border-slate-800",children:[e.jsxs("h3",{className:"text-base font-bold text-emerald-400 mb-3 flex items-center gap-2",children:[e.jsx("span",{children:"✓"})," Best Practice 2: Regular information_schema Audits"]}),e.jsxs("p",{className:"text-xs sm:text-sm text-slate-300 leading-relaxed mb-3",children:["Query ",e.jsx("code",{children:"information_schema.PARTITIONS"})," periodically to detect row count skew and reclaim fragmented free disk space via ",e.jsx("code",{children:"OPTIMIZE PARTITION"}),"."]}),e.jsx("div",{className:"text-xs text-slate-400",children:"Maintains optimal storage efficiency and even data distribution."})]})]})]}),e.jsxs("section",{id:"printable-note",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-emerald-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"5. Printable Study Note & Teacher Advice"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Download complete printable notes and review key takeaways from Sir Sukanta Hui."})]}),e.jsx(h,{title:"Topic 1: Partitioning Mechanics: How the Storage Engine Handles Partitioned Tables",content:x}),e.jsx(c,{note:"Understanding how the storage engine handles partitioned tables under the hood gives you total mastery over VLDB systems! Remember the hierarchy: the SQL parser delegates to ha_partition, which evaluates your expression and routes writes directly to the specific ha_innobase instance and its dedicated .ibd file on disk. Secondary indexes are strictly local to each partition, cross-partition updates perform an atomic delete + insert sequence, and EXCHANGE PARTITION allows you to swap entire tablespaces in under 10ms!"})]}),e.jsxs("section",{id:"faq-section",className:"space-y-6",children:[e.jsxs("div",{className:"border-l-4 border-cyan-500 pl-4",children:[e.jsx("h2",{className:"text-2xl sm:text-3xl font-bold text-white tracking-tight",children:"6. Frequently Asked Questions (30 Deep-Dive Questions)"}),e.jsx("p",{className:"text-slate-400 mt-1 text-sm sm:text-base",children:"Master the technical nuances of the ha_partition handler proxy, atomic cross-partition row moves, local secondary indexes, and EXCHANGE PARTITION swaps."})]}),e.jsx(p,{title:"Partitioning Storage Engine Mechanics FAQs",questions:m})]})]})]})};export{E as default};
